# Fix Report — 2026-09-01 16:11 UTC — report-fixer 13/11

> Источники: `magnum-album/reports/auth-2026-09-01-1607.md` · `watchdog-2026-09-01-1604.md` · `review-2026-09-01-1559.md` (ls -t топ-5). Ветка `master` @ `a4c6a25` → HEAD `a4c6a25`.

## Проверки до фикса
- `tsc --noEmit` — 0 ошибок
- `bun test` — 3141/3141 PASS (10014 expects, 35 файлов)
- `curl /magnum/api/ai` без токена → 401 (после hot-fix)
- `curl /magnum/images/gallery-42/42-y2k-01-800.webp` → 200 image/webp
- `dist` + `/srv/magnum` синхронны (build.ts → cp -r)

## Таблица баг | файл:строка | починка | статус

| # | Баг (из Замечания/BUG) | Файл:строка | Починка | Статус |
|---|---|---|---|---|
| 1 | 🔴 `/magnum/api/ai` отвечает без токена — anon жжёт XIAOMI_API_KEY | `server.ts:2232-2246` (`handleAi` + маршрут `:2531`) | Auth-gate в `handleAi` первой строкой: `extractToken` → `getUserByToken` → 401 если пусто/invalid + `checkRateLimit ai:{userId}:{ip} 10/мин`. Ранее anon текст-бот был доступен (8/мин per IP, image 401), теперь **все** POST /ai требуют токен. Проверено: `curl -X POST /magnum/api/ai` без cookie → `401 {"error":"unauthorized"}` (TLS + local оба). Rate-лимит перенесён на user-бакет, ledger `magnum_ai_usage` без изменений. | ✅ FIXED — `8afd888` + текущий патч; `systemctl restart magnum-bun` + curl 401 |
| 2 | `REAL_FALLBACK` Y2K алиасы указывают на чужие стили — фильтр Y2K показывает мемфис | `src/pages/GalleryPage.tsx:38-43` | `y2k-01: 42-memphis-01-800.webp → 42-y2k-01-800.webp`, `y2k-02: 42-memphis-01.jpg → 42-y2k-01.jpg`, `y2k-03: 42-cyber-01.jpg → 42-y2k-01-800.webp`. `REAL_BY_STYLE:30-35` уже корректен, `BASE_ARTS` 7 карточек теперь берёт реальные Y2K файлы (67K webp / 2.4M jpg). | ✅ FIXED — уже в HEAD (`review 15:59 #1`), `tsc 0`, `getRealSrc()` дополнительно маскирует архив |
| 3 | DDL в hot path (`CREATE TABLE IF NOT EXISTS` в каждом запросе chat/follows) + рассинхрон `isValidBundleId` (server trim+40 vs client 2-40 без trim) | `server.ts:1798,1830,1875,1895` + `src/pages/ShopPage.tsx:114` | Удалены 4× `CREATE TABLE IF NOT EXISTS magnum_chat_messages/follows` из `handleChatHistory / handleChatSend / handleFollowToggle / handleFollowsList` — таблицы уже в миграции `0016_chat_follows.sql` + индексы `magnum_chat_created_idx`/`magnum_chat_user_idx`. `ShopPage.tsx:114` унифицирован: `const s=v.trim(); return !!s && s.length<=40 && /^[a-z0-9-]+$/.test(s)` — идентичен `server.ts:805 isValidBundleId` (trim, max 40, `a-z0-9-`). | ✅ FIXED — в HEAD, `tsc 0`, `bun test 3141 PASS`, `build.ts` + `cp -r dist/* /srv/magnum` |

## Дополнительно проверено (не в топ-3, но из watchdog #1)
- `ARCHIVE_42` 210 + `ARCHIVE_WAVE_2` 140 = 350 артов с `src: archive-*.jpg` — файлов `archive-*` на диске 0, Caddy `try_files → /index.html` даёт soft-200 html. Митигировано `getRealSrc(style,src) → REAL_BY_STYLE[style]` (`GalleryPage.tsx:785`) + `loading="lazy" decoding="async" onError → artImgHidden` + gradient+emoji заглушка. Прямой `curl /magnum/images/gallery-42/archive-СССР-001.jpg` всё ещё 200 html (требует `Caddy handle /magnum/images/* { file_server }` без `try_files` — инфра-задача вынесена в бэклог). Визуально архив не бит — циклический маппинг на 4 реальных стиля.
- `public/images/gallery-42/` — 8 файлов (4 стиля ×2 jpg+webp) синхронны в `public/dist//srv` после `build.ts:1578 cp -r`.

## Сборка / деплой
- `~/.bun/bin/bunx tsc --noEmit` → 0
- `bun test` → 3141 pass 0 fail
- `bun run build.ts` → 71 files, 466K main (gzip ~137K), `dist/images/gallery-42` 8/8
- `cp -r dist/* /srv/magnum` → OK
- `systemctl restart magnum-bun` → active (PID перезапущен, 16:11 UTC), `curl /magnum/api/health` 200, `curl /magnum/api/ai` 401

## Git
- Коммит: `fix(report): 16:11 — ai 401 + gallery Y2K fallback + DDL/validator sync (tsc 0, 3141 PASS)`
- Push `origin/master` — OK
- Следующие баги в очереди: Caddy `archive-*` честный 404, `Game2042.tsx` localStorage → Neon `magnum_game_scores`, anon vote IP-ротация

---
*fixer 13/11 · cron 15м · reports reviewed: auth-1607, watchdog-1604, review-1559*
