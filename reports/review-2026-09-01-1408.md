# Review 2026-09-01 14:08 UTC — 7/10 (улучшенный 20м)

**Окно:** 10 минут (14:08 UTC, `git log --since="10 minutes ago"`) · **Коммитов:** 7 · **Строк:** +861 −18 = **879** · **Рейтинг:** **2/10** (5000+=10, 879/500≈1.8 → 2) · **Ветка:** `master` · **HEAD:** `76bfdf8`

> Рейтинг по строкам: <500=0, 500=1, 1000=2, 2000=4, 3000=6, 5000=10. Текущее окно — низкая активность, добивка API/тестов/доков, без крупных фич.

## Коммиты (10м)

| hash | сообщение | + − | файлы |
|------|-----------|-----|-------|
| `76bfdf8` | health: 14:06 — OK (local 200, ext FAIL hairpin / 200 via resolve) | +58 −0 | reports/health-2026-09-01-1406.md |
| `fcb3c92` | feat(api): presave stats + mining collect/top + coins top/health — rate limit + validation | +81 −0 | server.ts |
| `2dee8ef` | test: 14:03 — 2868 passed (15/15, tsc 0) +12 gallery/api/games | +229 −9 | drizzle/migrations/meta/_journal.json, reports/test-2026-09-01-1403.md, server.ts, tests/gallery-api-games.test.ts |
| `1ae5526` | watchdog: 14:02 — ✅ OK all 200, 6 gallery, 2862 tests, no restart | +62 −0 | reports/watchdog-2026-09-01-1402.md |
| `4f93a34` | feat(rhythm): pause P/Space + missStreak hint + pausedTime fix — RHYTHM v3 | +37 −9 | src/pages/games/RhythmGame.tsx, tests/rhythm-enhanced.test.ts |
| `459c489` | hype: 14:01 — +3 идей (duel lobby 2-4 / eco boss / limited drop) +8 queue | +66 −0 | docs/hype-queue.md, reports/hype-2026-09-01-1401.md |
| `5740e03` | docs: changelog 14:00 — 0.3.0 Roulette42×10 + GSAP tri-shadow + Auth gate + Gallery fix, 2850 tests, 4/10 | +328 −0 | CHANGELOG.md, server.ts, src/pages/ShopPage.tsx |

<details><summary>git log --since="10 minutes ago" --numstat --oneline</summary>

```
76bfdf8 health: 14:06 — OK (local 200, ext FAIL hairpin / 200 via resolve) | 58 0 reports/health-2026-09-01-1406.md
fcb3c92 feat(api): presave stats + mining collect/top + coins top/health — rate limit + validation | 81 0 server.ts
2dee8ef test: 14:03 — 2868 passed (15/15, tsc 0) +12 gallery/api/games | 7 0 drizzle/migrations/meta/_journal.json | 99 0 reports/test-2026-09-01-1403.md | 3 9 server.ts | 120 0 tests/gallery-api-games.test.ts
1ae5526 watchdog: 14:02 — ✅ OK all 200, 6 gallery, 2862 tests, no restart | 62 0 reports/watchdog-2026-09-01-1402.md
4f93a34 feat(rhythm): pause P/Space + missStreak hint + pausedTime fix — RHYTHM v3 | 23 9 src/pages/games/RhythmGame.tsx | 14 0 tests/rhythm-enhanced.test.ts
459c489 hype: 14:01 — +3 идей (duel lobby 2-4 / eco boss / limited drop) +8 queue | 16 0 docs/hype-queue.md | 50 0 reports/hype-2026-09-01-1401.md
5740e03 docs: changelog 14:00 — 0.3.0 Roulette42×10 + GSAP tri-shadow + Auth gate + Gallery fix, 2850 tests, 4/10 | 26 0 CHANGELOG.md | 188 0 server.ts | 114 0 src/pages/ShopPage.tsx
```

</details>

## Статус

| Проверка | Результат |
|----------|-----------|
| **tsc --noEmit** | ✅ 0 ошибок (exit 0, `/tmp/review.log` — пустой `---TSC---` блок) |
| **tests (`~/.bun/bin/bun test`)** | ✅ **2868 passed, 0 failed** · 15 файлов · 8523 expect() · 1.45s (см. `/tmp/review.log` tail-50) |
| **gallery файлы** | 6 файлов на диске: `42-agit-01-800.webp` 119K, `42-cyber-01-800.webp` 133K, `42-memphis-01-800.webp` 77K, + 3× jpg 2.4–3.4M (`ls -lh public/images/gallery-42/`) |
| **gallery curl (реальные 4)** | `42-agit-01-800.webp` **200** `application/octet-stream` · `42-cyber-01-800.webp` **200** · `42-memphis-01-800.webp` **200** · `42-agit-01.jpg` **200** `image/jpeg` — 4/4 живые (через `127.0.0.1:3000/magnum/images/...`) |
| **gallery curl (archive phantom)** | `archive-СССР-001.jpg` **200** `text/html; charset=utf-8` — **soft-404** (SPA fallback `index.html`, не картинка). `grep -c gallery-42 GalleryPage.tsx` = 375 вхождений, на диске только 6 файлов, `archive-*` нет — 12+ архивных id (`182–193`) бьют в HTML |
| **gallery src** | `src/pages/GalleryPage.tsx:32-44` — `REAL_BY_STYLE`/`REAL_FALLBACK` маппят 6 базовых артов на реальные webp — корректно. Но `arch-*-001`…`012` (строки 182+) хардкодят `archive-*.jpg` без fallback |
| **health** | `GET http://127.0.0.1:3000/magnum/` **200** · `GET http://127.0.0.1:3000/magnum/api/ideas` **200** (65 идей, первый `id:65` votes 142) · `/api/ideas` без префикса 404 — ожидаемо, каноника `/magnum/api/*` |
| **services** | `systemctl is-active magnum-bun` **active** (since 14:06:28, PID 144749, `PORT=3000`) · `docker ps` **magnum-caddy Up 43 minutes** (`:30646` http, `:30645` https → `172.18.0.1:3000`) · `ss -tlnp` `*:3000` LISTEN `bun` |

> 🔴 **КРИТИЧНО — галерея soft-404 (не health)**
> - `public/images/gallery-42/archive-*.jpg` отсутствуют (6 реальных файлов vs 12 archive-id в `GalleryPage.tsx:182–193` + 350 в истории). Запрос `archive-СССР-001.jpg` отдаёт **200 text/html** (SPA `index.html` 7192B) вместо `image/*` — в `<img>` битая иконка, `curl 200` маскирует ошибку.
> - Фикс: маппить archive через `REAL_BY_STYLE[style]` циклично или сгенерить превью, добавить `onError` → `gradient`+`emoji` fallback, и в `server.ts`/`Caddyfile` вынести `/magnum/images/*` в `file_server` без `try_files /index.html`, чтобы честный **404** вместо soft-404.

## Замечания (3 конкретно, файл:строка)

### 1) `src/pages/GalleryPage.tsx:182` — `archive-СССР-001.jpg` soft-404, нет фолбэка
```ts
{ id: "arch-СССР-001", src: "/magnum/images/gallery-42/archive-СССР-001.jpg", ... }
```
На диске `archive-*` нет (`ls public/images/gallery-42/archive* → No such file`). Все archive-карточки (строки 182–194, 12 шт сейчас, ранее 350) получают `text/html` вместо картинки. Базовые `BASE_ARTS` (46–171) уже исправлены через `REAL_FALLBACK` — нужно то же для archive: `src: REAL_BY_STYLE[style]` циклично или `ARCHIVE_SRC = (id,style) => REAL_BY_STYLE[style]`. Добавить `<img onError={e=>e.currentTarget.style.display='none'}>` и рендерить `gradient`+`emoji` как плейсхолдер. Проверить: `curl -I http://127.0.0.1:3000/magnum/images/gallery-42/archive-СССР-001.jpg` должен давать `404` после фикса статики, не `200 text/html`.

### 2) `server.ts:781–784` — `handlePresaveStats` делает 4 последовательных `await sql` вместо `Promise.all`
```ts
const totalRows = await sql`SELECT count(*)...`;
const dayRows = await sql`SELECT count(*)... WHERE created_at > now() - interval '24 hours'`;
const weekRows = await sql`SELECT ... '7 days'`;
const topRows = await sql`SELECT url, count(*)... LIMIT 5`;
```
Каждый запрос ждёт предыдущий, суммарная латентность ×4. В том же файле `handleHealth:821` уже использует `Promise.all([users, coins, mining, presave, ideas])` — применить тот же паттерн: `const [totalRows, dayRows, weekRows, topRows] = await Promise.all([...])`. Также `handleCoinsTop:213` и `handlePresaveStats:787` ловят `token` без валидации `Bearer` — если `Authorization: Bearer <token>` пустой, `getUserByToken("")` может бросить, сейчас это в `try {}` — оставить, но добавить `if (!token?.startsWith("Bearer "))` guard.

### 3) `src/pages/games/RhythmGame.tsx:230` — `pausedTime` накапливается, но `pauseStartRef` не сбрасывается при `reset/restart`
```ts
else if (state === "paused") { pausedTimeRef.current += performance.now() - pauseStartRef.current; setState("playing"); }
```
`pauseStartRef.current` ставится в `togglePause` при `playing → paused`, но при `reset` (`startTimeRef.current = performance.now(); pausedTimeRef.current = 0` в строке 223) `pauseStartRef` остаётся старым. Если игрок нажмёт `P` сразу после рестарта без паузы — ок, но если пауза была прервана `restart`, `pausedTimeRef` уже 0, а `pauseStartRef` висит. Фикс: в `reset/start` добавить `pauseStartRef.current = 0;` и в `togglePause` проверять `if (state==="playing") pauseStartRef.current = performance.now();`. Также `missStreak` hint на строке 506 показывается только `>=3`, но не сбрасывается на `success` в `hitNote`? В коде 192 `missStreakRef.current = 0` при старте, а в `hitNote` (324) `missStreakRef.current++` на miss и `setCombo(0)`, но на perfect/great нет сброса — подсказка зависнет до рестарта. Добавить `onHit: missStreakRef.current = 0; setMissStreak(0)`.

## Похвала

`src/pages/games/RhythmGame.tsx:228–243` + `tests/rhythm-enhanced.test.ts` — **пауза RHYTHM v3 образцовая**: `togglePause` + `pausedTimeRef` вычитается из `now` (строки 243,316,493,501 — везде `performance.now() - startTimeRef.current - pausedTimeRef.current`), canvas замирает, `Space/P/Esc` с `preventDefault`, подсказка `missStreak>=3` с советом «бей раньше + Легко окна шире», +6 тестов зелёные. Закрывает UX-дыру без хаков с `requestAnimationFrame` — чисто и тестируемо. Аналогично `server.ts:760`/`fcb3c92` — `presave stats` с `total/day/week/top` + `myClicks` по `user_id`, rate limit `6/min` и валидация — аккуратный API без N+1.

## Следующий шаг (идеи)

1. **Галерея:** завести `getArchiveSrc = (style, i) => REAL_BY_STYLE[style]` для всех `arch-*`, убрать `archive-*.jpg`, добавить `loading="lazy"` + `onError` fallback на `gradient`, в Caddy/serve вынести `/magnum/images/*` в отдельный `handle` с `file_server` без SPA fallback.
2. **API:** `handlePresaveStats` → `Promise.all`, добавить индекс `CREATE INDEX ON magnum_presave_clicks (created_at)` для `day/week` запросов, если таблица растёт.
3. **Rhythm:** сброс `pauseStartRef` + сброс `missStreak` на успешный хит, покрыть тестом `pause → restart → hit` без дрейфа времени.

---
*Сгенерировано cron-ревьюером 7/10 · 2026-09-01 14:08 UTC · tsc 0 · bun test 2868/2868 · gallery real 4/4 200, archive soft-404 text/html · health 200/200 · magnum-bun active · magnum-caddy Up 43m · /tmp/review.log приложен · рейтинг 2/10 (879 строк)*
