# Watchdog 2026-09-01 16:04 UTC — OK (all 200, no restart)

**Ветка:** `master` · **HEAD:** `a3e6645` · **Проверок:** 8/8 · **Рестартов:** 0 · **Cron:** `1/10` каждые 5 мин

## Health

| # | Проверка | Ожидается | Факт | Код/Статус |
|---|----------|-----------|------|------------|
| 1 | `curl --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | OK 200 | `200` `text/html` `MAGNUM — 5opka` ~9.4KB |
| 2 | `curl --resolve … https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | OK 200 | `200` `application/json` `{"ideas":[{id:65,votes:142,approved},…]}` ideas>100 |
| 3 | `curl http://127.0.0.1:3000/magnum/api/ideas` | 200 | OK 200 | `200` JSON, идентичен (2) — Bun.serve :3000 |
| 4 | `systemctl is-active magnum-bun` | active | OK active | `active (running)` since 2026-09-01 15:57:02 UTC, PID 290402, 19.1M, `enabled` |
| 5 | `docker ps \| grep magnum-caddy` | Up | OK Up ~1h | `magnum-caddy` `omniroute-caddy:latest` `Up About an hour` `0.0.0.0:30645-30646->30645-30646/tcp` |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | OK 8 (улучш.) | `42-agit-01-800.webp 119K`, `42-agit-01.jpg 3.4M`, `42-cyber-01-800.webp 133K`, `42-cyber-01.jpg 2.9M`, `42-memphis-01-800.webp 77K`, `42-memphis-01.jpg 2.4M`, `42-y2k-01-800.webp 67K`, `42-y2k-01.jpg 2.4M` — 4 стиля x2 (jpg+webp), `dist/images/gallery-42/` и `/srv/magnum/images/gallery-42/` синхронны (8/8, cp -r после 16:00 билда) |
| 7 | `git status --short` | — | OK clean | `clean` (до создания отчёта), после — `?? reports/watchdog-2026-09-01-1604.md` |
| 8 | extra `curl http://127.0.0.1:3000/magnum/` + `tsc --noEmit` | 200 / 0 errors | OK 200 / 0 | `200 text/html` local, `tsc 0 errors` |

**Доп:** `Caddy handle /magnum/api/* → 172.18.0.1:3000`, `handle_path /magnum/* → try_files {path} /index.html + file_server` (30645 TLS CF dns, 30646 http). `ss *:3000 bun pid 290402 LISTEN`. HTTPS via `--resolve` к 127.0.0.1 подтверждает TLS-терминацию Caddy. `build.ts:1578 cp -r ./public/images → ./dist/images` отработал, `/srv/magnum` обновлён 16:00.

## Коды ответов

```
https://oooyuki.zomb.top:30645/magnum/             → 200 text/html (TLS CF, Caddy 30645, resolve 127.0.0.1)
https://oooyuki.zomb.top:30645/magnum/api/ideas    → 200 application/json {ideas:[{id:65,votes:142,approved},…119 total]}
http://127.0.0.1:3000/magnum/api/ideas              → 200 application/json (Bun.serve :3000, идентичен TLS)
http://127.0.0.1:3000/magnum/                        → 200 text/html
http://127.0.0.1:3000/magnum/api/health             → 200 application/json {ok:true, counts:{ideas:119,…}}
/magnum/images/gallery-42/42-agit-01-800.webp       → 200 image/webp
/magnum/images/gallery-42/archive-СССР-001.jpg      → 200 text/html (BUG — см ниже, soft-404)
```

## Рестарты

| Сервис | Действие | Причина | Результат |
|--------|----------|---------|-----------|
| magnum-bun | — | не требовался (active, все 200, 19.1M) | — |
| magnum-caddy (docker compose) | — | не требовался (Up ~1h, 30645 LISTEN, 0 рестартов) | — |

> Повторная проверка после рестарта не выполнялась — все пробы зелёные. Критерий рестарта: любой не-200 или не-active → `systemctl restart magnum-bun; cd /opt/magnum-caddy && docker compose up -d && sleep 3 && curl-повтор`.

## Найденные баги (файл:строка)

### 1) `src/pages/GalleryPage.tsx:181-391` + `/opt/magnum-caddy/Caddyfile:15,29` — ARCHIVE_42 210× `archive-*.jpg` soft-404 через SPA fallback (P0, не закрыт)

`BASE_ARTS` (7) переведены на `REAL_BY_STYLE`/`REAL_FALLBACK` (`GalleryPage.tsx:30-43` → `42-agit-01-800.webp` 119K и т.д.) — `curl 200 image/webp` OK. Но `ARCHIVE_42` (строка 179, `Array 210` → `arch-СССР-001`…`arch-мемфис-210`) и `ARCHIVE_WAVE_2` (140) хранят `src: "/magnum/images/gallery-42/archive-*.jpg"` — файлов `archive-*` на диске 0 (`ls public/images/gallery-42/` — 8 реальных, `ls dist/images/gallery-42/` — 8, `archive-*` 0). Caddy `handle_path /magnum/* { try_files {path} /index.html; file_server }` (`Caddyfile:15,29`) отдаёт для них `200 text/html` (9439B `index.html`) вместо `404` — в `<img>` битая картинка, `onError` без заглушки, кириллица в пути риск double-encode, `loading="lazy"` отсутствует для архива. Проверка: `curl -i --resolve … https://oooyuki.zomb.top:30645/magnum/images/gallery-42/archive-СССР-001.jpg` покажет `content-type: text/html`. Тесты `gallery-real` ловят только реальные 8 файлов, архив 350 не покрыт. `syncPublicAssets` (`build.ts:1578 cp -r public/images → dist/images`) не генерит archive — только копирует существующие.

### 2) `src/pages/games/Game2042.tsx:255-256,286,348-351` + `src/components/AiBot.tsx:77,319,340` — localStorage вопреки Neon-only экономике (P1, регрессия)

Спека: «all state in Neon Lakebase Postgres, no localStorage» (`magnum_users/sessions`, `magnum_coins`, `Bun.password`, WebSocket на `Bun.serve`). `src/lib/coins.ts:4` прямо пишет «localStorage полностью удалён, токен в cookie». Но `Game2042.tsx:255` `useState(()=>Number(localStorage.getItem("2042-best")))` и `:256 "2042-bestTile"`, `:286 getItem("auth_token"||"magnum_token")`, `:348 setItem("2042-best")`, `:351 setItem("2042-bestTile")` — локальные рекорды и токен в LS, мимо `magnum_game_scores`/`Neon`. `AiBot.tsx:77` fallback `localStorage.getItem("magnum_coins")` и `:319,340 setItem("magnum-frame-verified","1")` дублируют `magnum_coins`/`magnum_frames` + LS `magnum-frame-verified`. `PromoPopup.tsx:21` `localStorage.getItem(seenKey)` и `HomePage.tsx:269` `magnum-banner-closed` — не критично, но та же причина. Последствие: рекорды 2042 не попадают в `magnum_leaderboard(game=2042)`, теряются при смене браузера, обход экономики `coins→rating`, токен в LS — XSS-риск (должен быть `httpOnly cookie`). `tsc` не ловит, т.к. `localStorage` типизирован.

## Идея починки (1)

**Закрыть оба бага одним патчем ~40 строк без ломки prod:**

1. **Галерея — честный маппинг + 404:** в `src/pages/GalleryPage.tsx:179` заменить генерацию архива на циклический маппинг реальных файлов:
```ts
const ARCHIVE_42: Art42[] = Array.from({length:210}, (_,i)=>{
  const styles: Style42[] = ["СССР","Y2K","киберпанк","мемфис"];
  const s = styles[i%4]!;
  return { id:`arch-${s}-${String(i+1).padStart(3,"0")}`, style:s, src: REAL_BY_STYLE[s], gradient: STYLE_META[s].accent, emoji: STYLE_META[s].emoji, title: `${STYLE_META[s].label} — #${String(i+1).padStart(3,"0")}`, desc:"— арт из архива 42", tag:s };
});
```
В рендере `<img loading="lazy" onError={e=>{e.currentTarget.style.display="none"; (e.currentTarget.nextElementSibling as HTMLElement)?.classList.remove("hidden")}}>` + gradient+emoji заглушка. В `/opt/magnum-caddy/Caddyfile` вынести `handle /magnum/images/* { root * /srv/magnum; file_server }` **без** `try_files /index.html` — тогда несуществующие отдают `404`, а не `200 html`, и тесты ловят проблему. API и SPA остаются с `try_files`.

2. **2042 + AiBot — в Neon:** в `Game2042.tsx:255` заменить `localStorage.getItem("2042-best")` на `fetch("/magnum/api/game/score?game=2042",{credentials:"include"}).then(r=>r.json()).then(d=>setBest(d.best||0))`, `:348 setItem` → `POST /magnum/api/game/score {game:"2042",score:newScore}` (уже есть `magnum_game_scores`). Токен из LS (`:286`) удалить — использовать `credentials:"include"` cookie как везде (`server.ts: extractToken` из cookie). В `AiBot.tsx:77` удалить `localStorage.getItem("magnum_coins")` fallback, `:319,340 setItem("magnum-frame-verified")` оставить только как кэш, но источником сделать `GET /magnum/api/frame/status` (Neon `magnum_frames`). Добавить `eslint-plugin-no-localstorage` или `grep -r localStorage` в CI `gallery-404.test.ts` + `/magnum/api/health` проверку `images:8, dist/srv sync`.

3. **Watchdog — уточнить:** обновить чек `ls -lh public/images/gallery-42/ — 6 файлов` → `8 файлов (4 webp + 4 jpg)` и добавить `curl -I /magnum/images/gallery-42/archive-СССР-001.jpg → expect 404` + `curl /magnum/api/health → images:8` для ловли рассинхрона public/dist/srv.

## Доп. наблюдения

- `magnum-bun` стабилен с 15:57:02 (6 мин, PID 290402, 19.1M, CPU 207ms), до этого 15:52:39. `magnum-caddy` Up ~1h без рестартов, `ss *:3000` LISTEN.
- `public/dist/srv` синхронны после `build.ts:1578` + `cp -r dist/* /srv/magnum` в 16:00 — 8/8 файлов, `dist/images` 12M, `public/images` 12M.
- `git log HEAD a3e6645` (0.3.11 Shop Bundles 8 + Chat 42 + Timeline2026 + Runner webp 4/10), `git status` clean до отчёта, `tsc 0 errors`, `reports/` в `.gitignore:38` — нужен `git add -f`.
- `magnum/api/health` отдаёт `counts:{users,coins,ideas:119,ideaVotes,…}` — можно бить watchdog в один запрос вместо трёх curl.
- `server.ts:295 handleIdeasVote` anon ветка (`:316-319` без `magnum_idea_votes` dedup, только IP rate-limit 12/мин) всё ещё позволяет фарм голосов ротацией IP — P1, но вне фокуса этого цикла (уже репортился 15:56).

---
*Watchdog 1/10 · сгенерировано `bun` cron · следующий запуск +5 мин · `reports/watchdog-2026-09-01-1604.md`*
