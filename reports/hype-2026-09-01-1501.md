# HYPE 2026-09-01 15:01 — MAGNUM 3/10

> Хайп-генератор · бэклог 4 фичи · 3 новые идеи · Neon OK · очередь +5

## POST /magnum/api/ideas

Авторизован как `hypebot42` (id 20). 3 идеи успешно созданы в Neon `magnum_ideas`:

- id 95 `DUEL CHAIN 42 — WS 2-4 + chain-combo x5 + wager 42/142/420` → 201
- id 96 `ECO REKA 42 — 8Q Томь/Кузбасс + streak 7дн + freeze 420` → 201
- id 97 `FRAME PRESAVE VERIFIED 42 — БРАТ-БОТ mimo-v2.5 vision + conic-gold` → 201

`curl -s http://localhost:3000/magnum/api/ideas | jq .ideas[-3:]` подтверждает. Всего идей в БД: 78.

---

## Новые идеи (3)

| # | идея | приоритет | монетки | спека (3 строки) |
|---|------|-----------|---------|-------------------|
| 1 | **DUEL CHAIN 42 — WS 2-4 + chain-combo x5 + wager** | P0 | wager 0/42/142/420, win `+wager*2` +42 ELO, lose -12, draw возврат, топ-3 +1420 + crown | WS арена 2-4 на `Bun.serve ws /magnum/api/ws`: `lobby:create`→ABCD→`join`→`ready`→10с chain-дуэль. chain <0.35с +20% на клик капа x5, CPS>20 suspect бан + throttle 30/сек, heartbeat 25с + реконнект fallback hot-seat. Сезон 7дн `magnum_leaderboard(game=duel42)` + `magnum-duel-history` LS топ-3 `conic-gold` + crown. |
| 2 | **ECO REKA 42 — 8Q Томь/Кузбасс + streak 7дн + freeze** | P1 | 42 Нормис 0-3 / 142 Братуха 4-6 / 420 Легенда 7-8 (1×/сутки) +1420 босс 8/8, freeze 420 | 8Q про Томь/Кузбасс/42+эко + факты MAGNUM 8K/200K VPN 28.04 CLAY РЗТ73 NOVA РЗТ80/XXL86, экран `n/8` + «А знаешь ли…» + conic бейдж. Недельный стрик 7дн `magnum-eco-challenge:{weekId,streak}` пропуск→сгорает freeze 420 1×/нед, босс-вс 8/8. Шаринг OG 1080×1080 canvas + Web Share API +42. |
| 3 | **FRAME PRESAVE VERIFIED 42 — mimo-v2.5 vision + conic-gold** | P0 | бесплатно (verified), cross -42 в forge | Скрин пресейва → `POST /magnum/api/ai` mimo-v2.5 vision → эвристика «засчитан»/«легенда» без «не вижу» → `magnum_frames` + LS `magnum-frame-verified=1` + `frame-date` ISO, рамка `conic-gold spin 3s` + `shadow 0 0 16 gold`, GSAP scale spring + glow pulse 2s, cross -42 в shop + verified glow. |

### Детализация

#### 1. DUEL CHAIN 42 — WS 2-4 + chain-combo x5 + wager
- **Описание:** Закрывает backlog #1 + chain-механика. Хост «Создать арену» → `ws.send({type:"lobby:create",wager})` → сервер `room:ABCD` + `ws.subscribe('room:ABCD')` + `roomPublic()` → до 3 братух `join ABCD` → `ready` → авто-старт 10с chain-дуэль. Ставка 0/42/142/420 из `magnum-coins` (`getCoins()>=wager` guard, `addCoins(-wager)` atomic перед WS). Игра: полноэкранный кликер 10с, счёт `raw clicks` + chain: если интервал <0.35с → цепочка, каждый следующий клик +20% к score (1.2→1.4→1.6→1.8→2.0 капа x5), индикатор `CHAIN xN` burst. Анти-чит: `CPS>20` или `>150 clicks/10с` → `suspect:true` бан раунда + не пишет в leaderboard, тост «братуха, ты робот?». Результат `broadcast {type:"finish",scores}` → winner `+wager*2 +42 ELO`, loser `-12`, draw возврат. Сезон 7дн `magnum_leaderboard(game=duel42)` Neon + `magnum-duel-history` LS 20 записей, топ-3 `conic-gold + crown`.
- **Цена:** `0` френдли или `42/142/420`; победитель `+wager*2` + `+42 ELO` + `+42 dust` при chain>=3, лузер `-12 ELO`, draw возврат ставок. Топ-3 сезона `+1420` + `chain-crown-42` + `conic-gold` рамка на неделю. Chain-бонус не даёт монет напрямую, только score→ELO.
- **GSAP:** `gsap.context` `LobbyGrid` stagger `y:24→0 0.12 power2.out`, `ChainStage` shake `x:±6 0.08s` на клик + `scale 1→1.15 0.1`, chain `CHAIN xN` `scale 0→1.4→1 back.out(1.7)` + `y:-12` burst + `navigator.vibrate(15+N*8)`, `Transit 3-2-1` `scale 2→1 + opacity 1→0 0.5`, конфетти 160 canvas `particle(x,y,vx,vy,life)` + RAF, `prefers-reduced-motion` → `animation:none`.

#### 2. ECO REKA 42 — 8Q Томь/Кузбасс + streak 7дн + freeze
- **Описание:** Закрывает backlog #2 + недельный челлендж. База — `EcoQuizPage` 8Q (Томь/Кузбасс/42/эко) + факты MAGNUM (ТУСА МЕДУЗА 14.08 — 8K клипов/200K просмотров, VPN 28.04 в чартах, CLAY 03.04 РЗТ73, SUPER PUPER NOVA РЗТ80/XXL86). UI: прогресс `n/8` + `progress width n/8*100%` + карточка «А знаешь ли ты…» (пропаганда: «Томь — артерия Кемерово, 42 братухи чистят берега»). Результат: уровни Нормис 0-3 / Братуха 4-6 / Легенда 7-8 → `conic-gradient` бейдж + `addCoins(42/142/420)` + шаринг + график `magnum-eco-history` 10 попыток. Челлендж 7дн: LS `magnum-eco-challenge:{weekId:YYYY-Www,streak,lastDate}` — каждый день 1 задание (сортировка/берег Томи/воздух Кузбасса) + факт MAGNUM, пропуск→стрик сгорает, заморозка 420 1×/нед (`magnum-eco-freeze-used` guard weekId). Финал воскресенье — босс-квиз 8Q → `streak*42+420` если 7/7 + `+1420` за 8/8 босс + OG шаринг 1080×1080 canvas (`toDataURL`) + Web Share API + `magnum-share-claimed` +42 1×.
- **Цена:** `42` (Нормис) / `142` (Братуха) / `420` (Легенда) — 1×/сутки guard `lastDate`, `1420` за босс 8/8 + бейдж `Эко-Легенда 42` + `conic-gold` рамка. Freeze `420`, шаринг `+42`.
- **GSAP:** `QuestionCard` stagger `y:18→0 0.1 power2.out`, `progress` tween `width 0→n/8*100% 0.4 power2.out`, `BadgeReveal` `scale 0→1 0.6 spring back.out(1.7)` + `conic spin 3s linear infinite`, `StreakCalendar` `stagger y:10 0.06`, `prefers-reduced-motion` → `opacity 0→1 0.2` без spin.

#### 3. FRAME PRESAVE VERIFIED 42 — mimo-v2.5 vision + conic-gold + cross
- **Описание:** Закрывает backlog #4 + cross с #3. Скрин пресейва → `POST /magnum/api/ai` mimo-v2.5 vision → эвристика «засчитан»/«легенда» без «не вижу» → сервер `magnum_frames` + LS `magnum-frame-verified=1` + `frame-date` ISO, рамка `conic-gradient gold spin 3s` + `box-shadow 0 0 16 gold` вокруг аватара. `PresaveCheckPage`: загрузка `input file` → preview + `fetch /magnum/api/ai` + лоадер «БРАТ-БОТ смотрит…» + результат «ВЕРИФИЦИРОВАН» с конфетти. Cross: `verified=1` → `-42` скидка на 1 покупку/нед в forge + glow в дуэли/эко, пустое «Собери vault, братуха!» + CTA если нет скинов.
- **Цена:** Бесплатно (требует скрин пресейва с vision-подтверждением). Cross: `-42` на 1 покупку/нед в forge, +verified glow в дуэли/эко + conic-gold рамка на аватар навсегда (до ресета сезона).
- **GSAP:** `UploadCard` `scale 0.96→1 0.4 power2.out`, `BotPeek` `y:8→0 0.3 back.out(1.7)` + `rotate -2→0`, `FrameReveal` `scale 0→1 0.6 spring back.out(1.7)` + `conic spin 3s` + `glow pulse 2s ease-in-out infinite`, `prefers-reduced-motion` → `animation:none`.

---

## Интеграция

- Файлы: `src/pages/games/DuelChain.tsx` + `src/lib/duelChain.ts` + `src/lib/ws.ts` + `server.ts:websocket lobby 2-4 chain x5`, `src/pages/EcoRekaPage.tsx` + `src/lib/ecoReka.ts` + `src/components/EcoBadge.tsx` + `src/components/ShareCard.tsx`, `src/pages/PresaveFramePage.tsx` + `src/lib/presave.ts`
- LS: `magnum-duel-history/leaderboard/chain`, `magnum-eco-challenge/history/freeze-used/share-claimed`, `magnum-frame-verified/frame-date`, `magnum-coins`, `magnum-arena-season`
- Neon: `magnum_frames`, `magnum_leaderboard(game=duel42)`, `magnum_eco_challenge`, `magnum_ideas` (ids 95-97)
- Кросс: победа дуэли chain→`StreakCalendar` + `RecapQuest`, эко-Легенда→скидка 42 в forge + verified glow, verified frame→glow в дуэли + эко бейдж
- Rate: `ws 30/сек` throttle + `ideas:post 10/мин` + `magnum-eco 1/сутки` + `presave 10/мин`

> Следующий тик 15:15 — ротация forge + проверка голосов `magnum_ideas` (ids 95-97) + season reset 7дн.
