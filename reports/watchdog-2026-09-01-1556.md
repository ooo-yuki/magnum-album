# Watchdog 2026-09-01 15:56 UTC — OK (all 200, no restart)

**Ветка:** `master` · **HEAD:** `3920a0f` · **Проверок:** 8/8 · **Рестартов:** 0 · **Cron:** `1/10` каждые 5 мин

## Health

| # | Проверка | Ожидается | Факт | Код/Статус |
|---|----------|-----------|------|------------|
| 1 | `curl --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | OK 200 | `200` `text/html` `MAGNUM — 5opka` 9439B |
| 2 | `curl --resolve … https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | OK 200 | `200` `application/json` `{"ideas":[{"id":65,"votes":142,"status":"approved"},…]}` ideas>0 |
| 3 | `curl http://127.0.0.1:3000/magnum/api/ideas` | 200 | OK 200 | `200` JSON, идентичен (2) — Bun.serve :3000 |
| 4 | `systemctl is-active magnum-bun` | active | OK active | `active (running)` since 2026-09-01 15:52:39 UTC, PID 287286, 72.5M, `enabled` |
| 5 | `docker ps \| grep magnum-caddy` | Up | OK Up 55m | `magnum-caddy` `omniroute-caddy:latest` `Up 55 minutes` `0.0.0.0:30645-30646->30645-30646/tcp` |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | WARN 8 (улучш.) | `42-agit-01-800.webp 119K`, `42-agit-01.jpg 3.4M`, `42-cyber-01-800.webp 133K`, `42-cyber-01.jpg 2.9M`, `42-memphis-01-800.webp 77K`, `42-memphis-01.jpg 2.4M`, `42-y2k-01-800.webp 67K`, `42-y2k-01.jpg 2.4M` — 4 стиля x2 (пары jpg+webp), было 6 -> стало 8 (+Y2K) |
| 7 | `git status --short` | — | WARN dirty* | `M drizzle/migrations/meta/_journal.json`, `M drizzle/schema.ts`, `M server.ts`, `?? drizzle/migrations/0016_chat_follows.sql` — появились после 15:55 checks, до проверки было clean |
| 8 | extra `curl http://127.0.0.1:3000/magnum/` + `tsc --noEmit` | 200 / 0 errors | OK 200 / 0 | `200 text/html` local, `tsc 0 errors`, Bun PID 287286 LISTEN :3000 |

**Доп:** `Caddy handle /magnum/api/* → 172.18.0.1:3000`, `handle_path /magnum/* → try_files {path} /index.html + file_server` (30645 TLS CF, 30646 http). `ss *:3000 bun pid 287286`. HTTPS via `--resolve` к 127.0.0.1 подтверждает TLS-терминацию Caddy.

## Коды ответов

```
https://oooyuki.zomb.top:30645/magnum/             → 200 text/html (TLS CF, Caddy 30645, resolve 127.0.0.1)
https://oooyuki.zomb.top:30645/magnum/api/ideas    → 200 application/json {ideas:[{id:65,votes:142,approved},…]}
http://127.0.0.1:3000/magnum/api/ideas              → 200 application/json (Bun.serve :3000, идентичен TLS)
http://127.0.0.1:3000/magnum/                        → 200 text/html 9439B
/magnum/images/gallery-42/42-agit-01-800.webp       → 200 image/webp (проверен ранее, Caddy file_server)
```

## Рестарты

| Сервис | Действие | Причина | Результат |
|--------|----------|---------|-----------|
| magnum-bun | — | не требовался (active, все 200, 72.5M) | — |
| magnum-caddy (docker compose) | — | не требовался (Up 55m, 30645 LISTEN, 0 рестартов) | — |

> Повторная проверка после рестарта не выполнялась — все пробы зеленые. Критерий рестарта: любой не-200 или не-active → `systemctl restart magnum-bun; cd /opt/magnum-caddy && docker compose up -d && sleep 3 && curl-повтор`.

## Найденные баги (файл:строка)

### 1) `src/pages/GalleryPage.tsx:181-390` — ARCHIVE_42 210x `archive-*.jpg` soft-404 через SPA fallback (P0, не закрыт)

`BASE_ARTS` (7) уже переведены на `REAL_FALLBACK` (`42-agit-01-800.webp` 119K и т.д.) — curl 200 image/webp. Но `ARCHIVE_42` (строка 179, 210 элементов `arch-СССР-001`…`arch-мемфис-210`) хранит `src: "/magnum/images/gallery-42/archive-*.jpg"` — файлов `archive-*` на диске нет (`ls public/images/gallery-42/` — 8 реальных, `archive-*` 0). Caddy `try_files {path} /index.html` отдает для них `200 text/html` (9439B `index.html`) вместо `404` — в `<img>` битая картинка, `onError` без заглушки, кириллица в пути (`archive-СССР-…`) риск double-encode, `loading="lazy"` отсутствует. Проверка: `curl -i …/archive-СССР-001.jpg` покажет `content-type: text/html`. Тесты `gallery-real` ловят только 3/6 реальных файлов, архив не покрыт.

### 2) `server.ts:295-323` — `handleIdeasVote` anon fallback без persistent dedup, фарм голосов по IP-ротации (P1, частично пофикшен)

`handleIdeasPost` (строка 266) требует `extractToken → getUserByToken → 401`, а `handleIdeasVote` (строка 295) — `if (!checkRateLimit(`+"`"+`ideas:vote:${ip}:${id}`+"`"+`, 12, 60_000)) return 429;` + опциональный `token`. Для `authed` есть dedup via `magnum_idea_votes(user_id,idea_id)` + `409 already voted` (строки 300-312) — OK. Но для anon (строки 315-318) — `UPDATE magnum_ideas SET votes = votes+1 WHERE id=${id}` без записи в `magnum_idea_votes`, только IP rate-limit 12/мин. Аноним с ротацией IP/proxy накрутит `votes` до бесконечности, обходя экономику `coins→votes`. Нет `UNIQUE(anon_ip,idea_id)`, нет списания монет, нет `anon_id` cookie. Рядом `SHOP_PRICES`/`coins` требуют auth — идеи-аноним нет.

## Идея починки (1)

**Галерея + голоса — один патч на 35 строк закрывает обе дыры, без ломки prod:**

1. **Галерея — dead archive → живой циклический маппинг:** в `src/pages/GalleryPage.tsx:179` заменить ручную генерацию 210 объектов на:
```ts
const ARCHIVE_42: Art42[] = Array.from({length:210}, (_,i)=>{
  const styles: Style42[] = ["СССР","Y2K","киберпанк","мемфис"];
  const s = styles[i%4]!;
  return { id:`arch-${s}-${String(i+1).padStart(3,"0")}`, style:s, src: REAL_BY_STYLE[s], gradient: STYLE_META[s].accent, emoji: STYLE_META[s].emoji, title: `${STYLE_META[s].label} — #${String(i+1).padStart(3,"0")}`, desc: "— арт из архива 42", tag: s };
});
```
В рендере `<img loading="lazy" onError={e=>{e.currentTarget.style.display="none"; (e.currentTarget.nextElementSibling as HTMLElement)?.classList.remove("hidden")}}>` + `gradient+emoji` заглушка. Альтернатива — сгенерить 12 превью `400.webp` и маппить `i%12`.

2. **Caddy — честный 404 для статики:** в `/opt/magnum-caddy/Caddyfile` вынести `handle /magnum/images/* { root * /srv/magnum; file_server }` **без** `try_files /index.html` — тогда несуществующие файлы отдают `404`, а не `200 html`, и тесты `gallery-real` ловят проблему. API и SPA остаются с `try_files`.

3. **Голоса — закрыть anon-фарм:** в `server.ts:295` убрать anon-ветку или заменить на `if(!authed) return 401` (требовать вход, как WS `server.ts:2233` теперь `401`). Если гостевые голоса нужны — `SET-Cookie anon_id=uuid` + `CREATE TABLE magnum_idea_votes_anon(anon_id text, idea_id int, PRIMARY KEY(anon_id,idea_id))` + `checkRateLimit(`+"`"+`ideas:vote:${anon_id}:${id}`+"`"+`,1,86400000)` + `balance -=1 coin` для связи с экономикой. Миграция: `CREATE TABLE IF NOT EXISTS magnum_idea_votes(user_id int REFERENCES magnum_users(id), idea_id int REFERENCES magnum_ideas(id), created_at timestamptz default now(), PRIMARY KEY(user_id,idea_id));`

## Доп. наблюдения

- `magnum-bun` стабилен 2м 52с после рестарта 15:52:39 (PID 287286), до этого Up 55м без падений. `magnum-caddy` Up 55m без рестартов.
- `public/images/gallery-42/` теперь 8 файлов (добавлен `42-y2k-01.*` 67K/2.4M) — прогресс с 6 → 8, все 4 стиля покрыты парами. Рекомендация — докинуть еще 4 файла `42-y2k-02`, `42-agit-02` и т.д. чтобы `BASE_ARTS` 7 не дублировал.
- `git status` стал dirty после 15:55 — `drizzle/*` + `server.ts` модифицированы, вероятно параллельный cron (auth/data) пушил миграцию `0016_chat_follows.sql`. Нужна синхронизация кронов или `git stash` перед коммитом watchdog.
- `tsc 0 errors`, WS `/magnum/api/ws` теперь требует auth `401` (пофикшен в 2233) — OK.
- `reports/` в `.gitignore` — watchdog пишет локально, нужен `git add -f` для пуша репортов.

---
*watchdog cron 1/10 · 2026-09-01 15:56 UTC · GalleryPage 210 archive soft-404 + anon vote IP-ротация · рестартов 0 · HEAD 3920a0f*
