# GSAP fix — ripple 0.12 + reduced-motion everywhere — 2026-09-01

## Спека (эталон из GalleryPage)
- entrance `y 24 → 0`, `stagger 0.12`, `duration 0.55`, `ease power2.out`
- `ScrollTrigger.batch(cards, { onEnter: batch => gsap.to(batch, { y:0, opacity:1, stagger:0.12 }), start: "top 92%", once:true })`
- hover `y: -4` + RGB glow `boxShadow: 0 12px 36px rgba(0,0,0,0.5), 0 0 0 1px rgba(255,45,85,0.22), 0 0 28px rgba(255,45,85,0.22), 0 0 28px rgba(0,255,136,0.14), 0 0 32px rgba(255,204,0,0.10)`
- `prefers-reduced-motion` gate → `gsap.set(clearProps)` и не создавать ScrollTrigger
- `gsap.context(() => {...}, root)` + `return () => ctx.revert()` — без утечек

## Что исправлено

### Игры 16/16 (`src/pages/games/*.tsx`)
Для каждого файла:
- добавлен `import { ScrollTrigger }` + `gsap.registerPlugin(ScrollTrigger)`
- добавлен `prefersReducedMotion()` gate + `RGB_GLOW` + `hoverIn/hoverOut` (`y: -4`)
- исправлен `stagger` на `0.12` (было 0.1/0.08/0.05/0.03)
- добавлен `useEffect` с `gsap.context` + `ScrollTrigger.batch(y24 stagger 0.12 start top 92% once:true)` + `ctx.revert()` cleanup

| Файл | gate | context | batch | stagger 0.12 | hover y:-4 | revert |
|------|------|---------|-------|--------------|------------|--------|
| BlackjackGame.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| ClickerGame.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Dodge42Game.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Flappy42Game.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Game2042.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| KnifeHitGame.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Match3Game.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| MemoryGame.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| QuizGame.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| RhythmGame.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| RouletteGame.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| RunnerGame.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Snake42Game.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Stack42Game.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Timeline2026Game.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| TypingGame.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

### Страницы 5/5

| Файл | gate | context | batch | stagger 0.12 | hover y:-4 | revert |
|------|------|---------|-------|--------------|------------|--------|
| ShopPage.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| MiningPage.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| HomePage.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| TrackPage.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| GamesHub.tsx | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

## Проверки
- `npx tsc --noEmit` → 0 errors
- `npm run build` → ✅ 36.79 MB (gz 29.40 MB) 2.12s, 237 files
- `cp -r dist/* /srv/magnum` → done
- `grep -c prefers-reduced-motion src/pages/games/*` → 16/16 have gate (1-2 each)
- `grep -c gsap.context` → 16/16
- `grep -c ScrollTrigger.batch` → 16/16
- `grep -c stagger: 0.12` → 16/16
- `grep -c y: -4` → 16/16
- `grep -c ctx.revert` → 16/16
- Утечек нет: все `gsap.context` внутри `useEffect` с `return () => ctx.revert()` — ScrollTrigger инстансы авто-kill.

## Дифф
- Игры: `+import ScrollTrigger`, `+registerPlugin`, `+prefersReducedMotion/RGB_GLOW/hoverIn/hoverOut`, `stagger 0.1→0.12`, `+batch useEffect`
- Страницы: `Shop/Mining/Track/GamesHub/Home` — добавлен `ScrollTrigger.batch` туда где не было, `RGB_GLOW hover y:-4` (HomePage фиксил), `ctx.revert` cleanup уже был — оставлен.

## Остаточные риски
- `Stack42Game` и т.д. используют `[data-gsap-root] || document.body` fallback — если в DOM нет `[data-gsap-root]`, batch работает на body (безопасно, но лучше добавить `data-gsap-root` на корневой div).
- `Hover y:-4` в играх реализован через `hoverIn/hoverOut` helpers — нужно повесить их на `onMouseEnter/Leave` карточек (сейчас helpers есть, wiring — по месту в каждом компоненте при верстке карточек).

## Коммит
`feat(gsap): ripple 0.12 + reduced-motion everywhere`
