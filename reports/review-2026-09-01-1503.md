# Review 2026-09-01 15:03 UTC — MAGNUM 3/10

> Авто-ревьюер каждые 10 минут (улучшенный 20м). Окно: 10 минут (`git log --since="10 minutes ago"`), рейтинг по строкам 5000+ → 10/10. Проверки: `tsc`, `bun test`, галерея `ls+curl+grep`, `health`, `systemctl+docker`.

## Рейтинг: 3/10

- **Строк изменено (git numstat 10м):** 853 (added 800 / removed 53) → **3/10** (0-49=0, 50-199=1, 200-499=2, 500-999=3, 1000-1499=4, 1500-1999=5, 2000-2999=6, 3000-3999=7, 4000-4999=8, 5000+=10)
- **Коммитов за 10м (git вернул):** 10 (строго по часам 14:53-15:03 → 6 коммитов, но `git log --since="10 minutes ago"` отдал 10 включая 14:55+ 14:51-отчёты из-за часового дрейфа — считаем факт 800 added)
- **Окно:** 14:53:00 — 15:03:00 UTC (строго 10м), факт `head -120` @15:03
- **Предыдущий рейтинг 14:55:** 6/10 (2992 строки за 20м) → падение до 3/10: нет крупных фичей, только perf+gsap+fix+отчёты
- **Uncommitted:** `src/pages/games/Game2042.tsx` модифицирован (lore+submit/score) — не закоммичен, влияет на следующий рейтинг

## Коммиты (10м, `git log --since="10 minutes ago" --numstat --oneline | head -120`)

| hash | сообщение | + / - | файлы |
|------|-----------|-------|-------|
| `5d785d0` | perf: 15:01 — lazy 5 eager pages (About42/Track/LastFit/Game/GamesHub) 528→472KB + stale cleanup | +117 -0 / +15 -13 | reports/perf-2026-09-01-1501.md, src/App.tsx |
| `2c24480` | hype: 15:01 — +3 идей (DUEL CHAIN x5 + ECO REKA + FRAME VERIFIED) | +7 -1 / +52 -0 | docs/hype-queue.md, reports/hype-2026-09-01-1501.md |
| `01e5536` | feat(gsap): AuthStatus entrance+neon+modal + Layout context + PageTransition white-screen harden | +67 -10 / +21 -11 / +12 -7 | src/components/AuthStatus.tsx, src/components/Layout.tsx, src/components/PageTransition.tsx |
| `26cc8b3` | fix(ws): 401 без токена — закрыт auth bypass (raw socket 101→401) | +6 -9 | server.ts |
| `fd132a4` | watchdog: 14:59 — OK (200/200/200 active Up, 8 gallery, 3017 tests, health resilient) | +103 -0 | reports/watchdog-2026-09-01-1459.md |
| `d113ea9` | review: 14:55 — rating 6/10 | +93 -0 | reports/review-2026-09-01-1455.md |
| `f3e0446` | docs: changelog investor update | +36 -0 | CHANGELOG.md |
| `3f1314a` | feat(content): games unified scoring + referrals + duel history — /magnum/api/games/{submit,top,my} + /referral/{code,redeem} + /duel/history + Neon magnum_game_scores/referrals/duel_history + vault persist | +42 -0 / +36 -1 / +29 -0 / +164 -1 | drizzle/migrations/0010_game_scores_referrals.sql, drizzle/migrations/meta/_journal.json, drizzle/schema.ts, server.ts |
| `8f852ef` | feat(games): Memory 42 — THEMES 4×12 + DIFFICULTY 3 + combo FEVER x5 + хинты 3× + свайп ↔/↑ + стрелки+Enter + WebAudio fever/hint + GSAP win + localStorage best | +275 -76 | src/pages/games/MemoryGame.tsx |
| `29e2edf` | watchdog: 14:51 — OK (200/200/200 active Up, 8 gallery, 3017 tests, 2 bugs) | +82 -0 | reports/watchdog-2026-09-01-1451.md |

> Строго 10м (14:53-15:03): `5d785d0` 132-13 + `2c24480` 59-1 + `01e5536` 100-28 + `26cc8b3` 6-9 + `fd132a4` 103 + `d113ea9` 93 = **493 added / 51 removed = 544 total** без учёта `f3e0446/3f1314a/8f852ef` (они 14:51-14:50, git их отдал из-за границы). Чистого кода за 10м: `src/App.tsx` 15 + `AuthStatus/Layout/PageTransition` ~100 + `server.ts` 6 + `MemoryGame` вне окна = ~121 строка кода → 1/10 без отчётов, 3/10 с отчётами.

## Статусы

| Проверка | Ожидаемо | Факт | Статус |
|----------|----------|------|--------|
| `bunx tsc --noEmit` | 0 errors | 0 errors (`TSC_EXIT:0`) | ✅ |
| `bun test` | pass | 3017 pass / 0 fail (25 files, 9241 expect, 1.36s) | ✅ |
| `ls public/images/gallery-42/` | реальные файлы | 8 файлов: 42-agit-01.{jpg 3.4M +800.webp 119K}, 42-cyber-01.{jpg 2.9M +800.webp 133K}, 42-memphis-01.{jpg 2.4M +800.webp 77K}, 42-y2k-01.{jpg 2.4M +800.webp 67K} (12M total) | ✅ (8/8) |
| `curl gallery real` | 200 image/* | 200 image/webp 42-agit-01-800.webp, 200 image/webp 42-cyber-01-800.webp, 200 image/webp 42-memphis-01-800.webp, 200 image/webp 42-y2k-01-800.webp | ✅ 4/4 |
| `curl gallery archive` | 200 image или 404 | 200 **text/html** (9106 байт index.html, `Content-Type: text/html; charset=utf-8`) для `archive-СССР-001.jpg` и `archive-Y2K-002.jpg` — SPA fallback `try_files {path} /index.html` маскирует 404 (проверено `curl -I` + `curl | head -c 300` → `<!DOCTYPE html>`) | ⚠️ WARN (210 archive src без файлов) |
| `curl /magnum/` | 200 | 200 (localhost:3000) | ✅ |
| `curl /magnum/api/ideas` | 200 JSON | 200 `{"ideas":[{"id":65,...` | ✅ |
| `systemctl is-active magnum-bun` | active | active | ✅ |
| `docker ps magnum-caddy` | Up | Up 2 minutes (magnum-caddy) | ✅ |
| `git status` | clean | modified `src/pages/games/Game2042.tsx` (уровень лора+submit/score, не закоммичен) + untracked report | ⚠️ |

**Итог: 🟢 OK — критичные сервисы UP, tsc/tests зелёные, 8 реальных файлов 200. Критичного 404/health!=200 нет (но галерея archive — скрытый 404 под 200 html, см. замечание #1).**

## Замечания (3 конкретных, файл:строка)

### 1) `src/pages/GalleryPage.tsx:181-390` — ARCHIVE_42 210 записей указывают в несуществующие файлы, SPA скрывает 404
210 строк `src: "/magnum/images/gallery-42/archive-СССР-001.jpg"` … `archive-мемфис-210.jpg` не имеют файлов в `public/images/gallery-42/` (`ls` 8 файлов, проверено `ls -lh …/archive-СССР-001.jpg → No such file`). Из-за `Caddyfile:8 try_files` запрос отдаёт `200 text/html` (index.html 9106 байт), а не 404 — в браузере `<img>` сломается (битая иконка поверх градиента/emoji). `onError` не навешан, `MOCK_POOL` не используется для ARCHIVE. Нарушает «Галерея только реальные файлы». **Фикс:** либо генерить 210 реальных webp, либо патчить `Caddyfile` на `try_files` без fallback для `/images/`, либо в `GalleryPage.tsx:540+` добавить `onError={e=>{e.currentTarget.style.display='none'}}` + fallback на `REAL_BY_STYLE[style]` (как у `BASE_ARTS:52/62/72`).

### 2) `src/pages/games/MemoryGame.tsx:293,494-496` — `localStorage` best вместо Neon `magnum_game_scores`
`const [best, setBest] = useState(()=>Number(localStorage.getItem("memory42-best-normal"))||0)` и `localStorage.setItem(key,String(finalScore))` — рекорд только в браузере, не уходит в `/magnum/api/games/submit` (который появился в `3f1314a` для 2042). Спека: «all state in Neon, no localStorage, multiplayer via WebSocket on Bun.serve». Memory 42 — единственная из 14 игр с локальным best, остальные (Game2042 `278 submitScore`, Blackjack и т.д.) уже шлют в Neon. **Фикс:** добавить `submitScore('memory', finalScore, diff)` как в `Game2042.tsx:278` + читать `GET /magnum/api/games/my?game=memory` для `best`, оставить `localStorage` только как кэш-фолбэк.

### 3) `server.ts` (коммит `26cc8b3` 6+9 строк) + `src/pages/games/Game2042.tsx:278-295` — ws fix неполон, submit без проверки
`grep -n "upgrade|WebSocket|ws" server.ts` — 0 совпадений с `upgrade`/WebSocket, только `magnum_*` таблицы; фикс 6 строк не показывает `if (!token) ws.close(4401)` — проверь что закрывает именно `Bun.serve` upgrade, а не HTTP 401 (raw socket дал 101→401 в описании, но кода нет). Одновременно `Game2042.tsx:280-283` шлёт токен из `localStorage.getItem("auth_token")||"magnum_token"` без проверки `submitted` race: `setSubmitted(true)` ставится до `fetch`, но при `score<100` return без `setSubmitted` — можно спамить сабмиты <100, а при 5xx нет retry. **Фикс:** в `server.ts` показать явный `if (req.headers.get("upgrade")==="websocket" && !extractToken(req)) return new Response("Unauthorized",{status:401})` перед `server.upgrade`, в `Game2042.tsx:278` добавить `if (!tk) return` + `catch` с `setSubmitted(false)`.

## Похвала (1 конкретная)

### `src/App.tsx:20-24` + `src/components/AuthStatus.tsx:1-67` + `src/components/Layout.tsx` + `src/components/PageTransition.tsx` — perf+GSAP чисто
`5d785d0` перевёл 5 eager страниц (About42/Track/LastFit/Game/GamesHub) в `lazy(() => import(...))` — main `528→472KB` (-56KB, -10.6%) без белых экранов благодаря `01e5536`: `AuthStatus` завёрнут в `gsap.context` + `set()+to()` вместо `fromTo` (IIFE-safe), `Layout` и `PageTransition` с `800ms failsafe` и `reduced-motion` gate, `ctx.revert()` на unmount. tsc 0, 243 чанка, build health зелёный — образцовый perf+accessibility паттерн, который стоит скопировать на остальные 9 heavy pages.

## Приложение: сырые логи (`/tmp/review.log` head)

```
5d785d0 perf: 15:01 — lazy 5 eager pages 528→472KB
2c24480 hype: 15:01 — +3 идей (DUEL CHAIN x5 + ECO REKA + FRAME VERIFIED)
01e5536 feat(gsap): AuthStatus entrance+neon+modal + Layout context + PageTransition
26cc8b3 fix(ws): 401 без токена — закрыт auth bypass
fd132a4 watchdog: 14:59 — OK (200/200/200 active Up, 8 gallery, 3017 tests)
d113ea9 review: 14:55 — rating 6/10
tsc: 0 errors
bun test: 3017 pass 0 fail
gallery curl: 4/4 real 200 image/webp, archive 2/2 → 200 text/html (SPA fallback)
health: /magnum/ 200, /magnum/api/ideas 200
services: magnum-bun active, magnum-caddy Up 2 minutes
```

---
*Сгенерировано cron-ревьюером 7/10 @ 2026-09-01 15:03 UTC — `git log --since="10 minutes ago" --numstat --oneline | head -120 > /tmp/review.log; bunx tsc --noEmit; bun test; ls gallery; curl 4×200; curl health; systemctl+docker`*
