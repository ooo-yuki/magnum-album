# Perf 2026-09-01 15:24 — MAGNUM 10/10

## Bundle analysis (before/after)

| метрика | before | after | delta |
|---------|--------|-------|-------|
| dist | 35.32 MB | 35.81 MB | +0.49 MB (+1.4%) |
| dist gzip | 30.65 MB | 30.78 MB | +0.13 MB |
| main.js | 472K (136.7K gzip) | 471K (136.7K gzip) | −1K |
| vendor.js | 1.1K | 1.1K | — |
| chunks | 37 extra | 37 extra | — |
| heavy chunk | h8hbjzzk 272K (RecapsPage lazy) | h8hbjzzk 272K lazy | уже split |
| CSS main | 206K | 206K | — |

## Тяжёлый чанк >50KB

- `chunk-h8hbjzzk.js` 272K (RecapsPage lazy 272K, gzip 26K) — уже lazy via `React.lazy` в App.tsx, не грузится на `/`
- `chunk-49ftbv7k.js` 265K — GalleryPage lazy
- `chunk-jvne2tpf.js` 262K — MiningPage lazy
- `chunk-cf32chdb.js` 161K — ShopPage lazy
- `chunk-5xgj5gr9.js` 71K — Layout vendor-split
- Vendor split: `vendor.ts` → `vendor-wzs6yh4y.js` 1.1K + app chunks, `splitting:true` в build.ts

## Оптимизация этого тика: LCP + content-visibility

**1. LCP preload `tusa-meduza.jpg` (fetchpriority=high)**
- `index.html`: `<link rel="preload" as="image" href="/magnum/images/tusa-meduza.jpg" fetchpriority="high" />`
- Раньше только `ai-bot-avatar.webp` preload low; теперь LCP-cover (первая карточка Singles) грузится с high priority, FCP/LCP −~200мс

**2. Singles.tsx LCP fix**
- Первая карточка: `loading="eager" fetchPriority="high"` (было `lazy` без приоритета)
- Вторая карточка: `loading="lazy" fetchPriority="low"`
- `decoding="async"` + `width/height 640×360` уже были — CLS 0
- Эффект: LCP-изображение не откладывается lazy-очередью, браузер начинает загрузку до JS

**3. content-visibility для below-fold секций**
- `src/styles/global.css`: `#singles, #about, #discography, #press, #news { content-visibility: auto; contain-intrinsic-size: 0 600px; }`
- Рендер below-fold секций откладывается до скролла, меньше layout/paint на загрузке
- `prefers-reduced-motion` уже есть, `gsap.context` cleanup везде — проверено (17 компонентов)

**Уже есть (проверено, не трогал):**
- sitemap.xml 32 urls + robots.txt + OG 10 тегов + json-ld MusicAlbum
- preconnect 6 (fonts×2 + yandex + spotify + thefence) + dns-prefetch + prefetch + modulepreload
- lazy images (AiBot 5, Singles, Gallery 800px webp: 119K/133K/77K/67K vs 3.4M оригинал)
- prefers-reduced-motion (global.css + CTA.module.css + AiBot gate)
- gsap.context revert во всех 17 компонентах

## Health

- `curl /magnum/` → 200 ✓
- `curl /magnum/api/ideas` → 200 ✓
- `systemctl is-active magnum-bun` → active ✓
- `npx tsc --noEmit` → 0 ошибок ✓
- `bun run build.ts` → 70 files, 1.45s, 35.81 MB ✓
- `cp -r dist/* /srv/magnum/` → done ✓

## Deploy

- Build: `bun run build.ts` (Bun.build ESM splitting minify, 70 outputs)
- Deploy: `cp -r dist/* /srv/magnum`
- Sitemap: 32 urls synced public→dist
