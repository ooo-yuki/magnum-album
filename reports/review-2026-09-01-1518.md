# MAGNUM Review — 2026-09-01 15:18 UTC (20м, ревьюер)

**Период:** 14:58–15:18 UTC (20м) · ветка `main` @ `9fd2c23`
**Коммитов в окне:** **22** (из них 12 продуктивных + 10 отчётов/watchdog)
**Рейтинг:** **6/10** — **2293 строк** (ADDED 2180 / REMOVED 113) · uncommitted diff +272 (Flappy 203 + Rhythm 68 + CSS 1) → итого 2565 строк в работе · норма 5000+ = 10/10 · шкала: 0-49=0, 50-199=1, 200-499=2, 500-999=3, 1000-1499=4, 1500-1999=5, 2000-2999=6, 3000-3999=7, 4000-4999=8, 5000+=10
**Статус сборки:** `tsc` PASS (0 ошибок) · `tests` FAIL (3050/3051, 1 fail) · `gallery` WARN (8 файлов реальны, 210 archive — HTML-fallback) · `health` PASS (200/200/200) · `services` PASS (magnum-bun active, caddy Up)

---

## Коммиты (20м, `git log --since="20 minutes ago"`)

| Хэш | Сообщение | + | − | Файлы |
|-----|-----------|---|---|-------|
| `9fd2c23` | docs: changelog investor update | 63 | 0 | CHANGELOG.md |
| `26dd77e` | feat(recaps): Freakland Create день 1 YpiqNshMn0E 35:24 — Завоз/удочка (803 сегм, +2 идеи 101/102) | 18 | 0 | src/pages/RecapsPage.tsx |
| `02eb99f` | docs: readme keeper — Tests 3051 (30 files), 26 tables, 2042 DAILY/FEVER, mining, gallery 8 | 11 | 10 | README.md |
| `19afd96` | review: 15:16 — rating 2/10 | 87 | 0 | reports/review-2026-09-01-1516.md |
| `41f777e` | auth: 15:15 — check | 103 | 0 | reports/auth-2026-09-01-1515.md |
| `6e73296` | watchdog: 15:12 — OK (200/200/200 active/Up, gallery 8/6, 2 bugs Y2K+daily) | 94 | 0 | reports/watchdog-2026-09-01-1512.md |
| `1aa15ab` | fix(tests): gallery build 400B→600B — gsap shim 514B | 2 | 2 | tests/gallery-api-games.test.ts |
| `0c47e1f` | feat(api): mining exchange 10:1 + eco rating 0-10 + idea comments + presave leaderboard + WS chat — Neon | 291 | 2 | drizzle/migrations/0011 (+17),0012 (+23), schema.ts (+28), server.ts (+151), EcoPage.tsx (+57) |
| `2c53379` | test: 15:10 — 3051 passed | 234 | 0 | reports/test-2026-09-01-1510.md (+107), tests/new-coverage-1530.test.ts (+127) |
| `1233f63` | docs: changelog 15:06 — 0.3.6 WS-фикс + 2042 DAILY/FLOAT | 59 | 0 | CHANGELOG.md |
| `c8b93fb` | watchdog: 15:06 — OK (200/200/200 active/Up, 8 gallery) | 93 | 0 | reports/watchdog-2026-09-01-1506.md |
| `ed478b2` | feat(games): 2042 float +X pop + haptics + spawn GSAP + 42 pulse | 29 | 1 | Game2042.tsx (+27), Game2042.module.css (+2) |
| `eb2d654` | test: 15:05 — 3035 passed (fix nested useEffect + TILE_LORE) | 224 | 0 | reports/test-2026-09-01-1505.md + tests/* (+131) |
| `3dcd532` | feat(games): 2042 DAILY CHALLENGE + seededBoard mulberry32 + TILE_LORE 2026 | 146 | 47 | Game2042.tsx (+81/−47), tests/game2042-fix.test.ts (+65) |
| `01e5536` | feat(gsap): AuthStatus entrance+neon+modal + Layout + PageTransition | 100 | 28 | AuthStatus.tsx, Layout.tsx, PageTransition.tsx |
| `26cc8b3` | fix(ws): 401 без токена — закрыт auth bypass (101→401) | 6 | 9 | server.ts |

**Uncommitted (не входит в 2293, войдёт в 0.3.8):** `Flappy42Game.tsx` 158/45 + `RhythmGame.tsx` 64/0 + `Flappy42Game.module.css` 1/0 + `CHANGELOG.md` 62/0 → **285/45 = 330 строк**.

---

## Статусы

| Проверка | Результат | Детали |
|----------|-----------|--------|
| **tsc --noEmit** | ✅ PASS | 0 ошибок (exit 0) |
| **tests (bun test)** | ⚠️ FAIL 3050/3051 | 1 fail: `flappy42: component renders > renders play button` [1.20ms] · 9424 expects · 28 файлов · 2.06s · остальные 3050 pass (Snake42, TILE_LORE, economy RARITY, health/SPA/build) |
| **gallery — файлы** | ✅ 8 файлов | `public/images/gallery-42/`: 42-agit-01.{jpg,800.webp} 3.4M/119K, 42-cyber-01.{jpg,800.webp} 2.9M/133K, 42-memphis-01.{jpg,800.webp} 2.4M/77K, 42-y2k-01.{jpg,800.webp} 2.4M/67K — 12M суммарно |
| **gallery — src check** | ⚠️ WARN дубликаты + HTML fallback | `grep src:.*gallery-42` → 356 вхождений (REAL 17 + archive 210 + REAL_FALLBACK 6) · `curl 200` для 5 реальных webp/jpg ✅ · но `archive-СССР-001.jpg` и `archive-Y2K-002.jpg` отдают `200` с `size 9238` одинаковым — это SPA `index.html` fallback (нет физических `archive-*.jpg`), в проде `<img>` покажет битую страницу |
| **gallery — dist sync** | ✅ | `dist/images/gallery-42/` содержит те же 8 файлов (119K/3.4M и т.д.), `build.ts` hash+splitting ок |
| **health — magnum/** | ✅ 200 | `curl https://oooyuki.zomb.top:30645/magnum/` → 200 (caddy → bun 3000) |
| **health — api/ideas** | ✅ 200 | `curl https://.../magnum/api/ideas` → 200 · `bun3000` прямой `http://127.0.0.1:3000/magnum/api/ideas` → 200 |
| **services — magnum-bun** | ✅ active | `systemctl is-active` → active · pid 240034 · LISTEN :3000 |
| **services — caddy** | ✅ Up 17m | `docker ps` → `magnum-caddy Up 17 minutes` · LISTEN :30645 (docker-proxy) |
| **git status** | ⚠️ 3 modified | `M src/pages/games/Flappy42Game.tsx`, `M src/pages/games/Flappy42Game.module.css`, `M src/pages/games/RhythmGame.tsx` (+ `M CHANGELOG.md` уже закоммичен в 9fd2c23, но локально ещё `RecapsPage.tsx` clean) |

---

## Замечания (3 конкретных, файл:строка)

### 1. `src/pages/GalleryPage.tsx:39` + `:42` — дубликат REAL_FALLBACK, Y2K без своего файла
```ts
"y2k-01": "/magnum/images/gallery-42/42-memphis-01-800.webp", // 39
"memphis-01": "/magnum/images/gallery-42/42-memphis-01-800.webp", // 42 — тот же путь
```
Оба ключа указывают на **memphis**, хотя `42-y2k-01-800.webp` (67K) и `42-y2k-01.jpg` (2.4M) физически существуют с 14:16 (см. `ls -lh`). В итоге фильтр `Y2K` показывает мемфис-арт, а Y2K-арт недоступен. Плюс `y2k-02` (`:40`) → `42-memphis-01.jpg`, а `y2k-03` (`:43`) → `42-cyber-01.jpg` — перекрёстные алиасы. Исправить:
```ts
"y2k-01": "/magnum/images/gallery-42/42-y2k-01-800.webp",
"y2k-02": "/magnum/images/gallery-42/42-y2k-01.jpg",
```
И убрать дубликат `memphis-01` или завести отдельный `42-memphis-02` если нужен второй мемфис.

### 2. `src/pages/games/Flappy42Game.tsx:166` + `:293` — localStorage вместо Neon (нарушение ТЗ)
```ts
const [diffId, setDiffId] = useState<DifficultyId>(() => localStorage.getItem("flappy42-diff") ... ); // 166
useEffect(() => { try { localStorage.setItem("flappy42-diff", diffId); } catch {} }, [diffId]); // 293
```
Аналогично `flappy42-skin` (`:167/:294`) и `flappy42-best` (`:168/:398/416/427`). ТЗ: *all state in Neon* (`magnum_users/sessions`, `magnum_game_scores`, `magnum_coins`), `no localStorage` для прогресса. Сейчас `coinMul` из `DIFFICULTIES` (norm 1 / hard 1.5 / turbo 2) нигде не уходит в `POST /magnum/api/games/submit` — множитель теряется при релогине и не начисляется в Neon. Заменить на `fetch /magnum/api/games/submit { game:"flappy42", score, diffId, skinId }` + чтение `GET /magnum/api/games/scores?game=flappy42` для `best`, как сделано в `Game2042.tsx:411` (DAILY submit). Иначе инкогнито/второй девайс = сброс прогресса.

### 3. `src/pages/games/RhythmGame.tsx:310` + `src/pages/GalleryPage.tsx:181` — HTML-fallback вместо 404 + localStorage best без Neon
- **RhythmGame.tsx:301/312** — `LS_BEST="rhythm42-best"` и `LS_TUT` через `localStorage`, та же проблема что в п.2; плюс `useEffect` на `:310` ставит `setInterval tipIdx 3200ms` корректно, но второй `useEffect` (`:313`) пишет `best` в localStorage только при `win/fail` — гонка если `best` ещё не загружен (closure stale). И `fetch("/magnum/api/presave/click", {body:{src:"rhythm_win"}})` на `:327` шлёт без `credentials: "include"` и без обработки 401/429 — молчаливый дроп.
- **GalleryPage.tsx:181** — `ARCHIVE_42` 210 записей (`archive-СССР-001.jpg` … `archive-Y2K-058.jpg` …) генерируются циклом, но `public/images/gallery-42/archive-*.jpg` **не существуют** — `curl` возвращает 200 с `size 9238` (совпадает для обоих тестовых URL), т.е. Caddy/Bun отдаёт `index.html` через SPA fallback (`try_files`). В браузере `<img src="...archive-...">` попытается отрисовать HTML как JPEG → broken image + консоль `Failed to load`. Нужен `onError` fallback на `REAL_BY_STYLE[style]` (как уже есть `getFallback()` на `:786`) или реальный `dist/images/gallery-42/archive-*.jpg` sync, иначе 210 битых картинок в галерее.

---

## Похвала (1 конкретная)

**`server.ts: handleMiningExchange` (`0c47e1f` +151 строк) — образцовый серверный обмен:** валидация `mining_amount 10..10000` + `multiple of 10` + `rate limit 8/мин` + `auth verify` + Neon транзакция `magnum_mining_exchanges` + списание `magnum_mining` + начисление `magnum_coins` + запись `magnum_transactions reason= mining_exchange` с 3 индексами — редкий пример где экономика (10:1) реализована атомарно и покрыта тестами (`new-coverage-1530.test.ts` +127). Так же `EcoPage.tsx: +57` корректно фетчит `GET /magnum/api/eco/tiers` и показывает 11 tiers — держит планку.

---

## Идеи для следующих правок (2)

1. **Закрыть flappy fail + Neon-миграцию прогресса (0.3.8):** починить `tests/flappy42.test.ts` (`renders play button` — селектор ожидает `[data-testid="play"]` а в `Flappy42Game.tsx` кнопка без testid, добавить `data-testid="flappy-play"`); одновременно вынести `flappy42-diff/skin/best` и `rhythm42-best/tut` в `magnum_game_scores` (`POST /games/submit` уже есть для 2042) + убрать прямой `localStorage` (оставить только `localStorage` для `tipIdx` ротации как UI-кэш, не прогресс). Это снимет последний `tests FAIL` и нарушение ТЗ за один PR (~40 строк).

2. **Галерея: починить archive fallback + оживить Y2K (P1):** а) поправить `REAL_FALLBACK` маппинг (п.1) + добавить `img onError={e=>e.currentTarget.src=getFallback(style)}` для архивных 210 записей; б) сгенерировать реальные `archive-*.jpg` 800.webp (хотя бы 1 проход `sharp`/`cwebp` по 4 стилям × 1 файл) или заменить `archive` src на уже существующие 8 файлов через `modulo` маппинг — сейчас 210 × 200 HTML = 210 битых картинок, но `curl 200` маскирует проблему в watchdog. После — обновить `watchdog` check-list с 6→8 и добавить проверку `content-type image/*` а не только `http_code`.

---
*Ревьюер: Hermes 24/7 · `bunx tsc --noEmit` 0 · `bun test` 3050/3051 · `curl gallery 7/7 200` (5 real + 2 archive HTML) · `health 200/200/200` · `magnum-bun active` `caddy Up` · next review +20м*
