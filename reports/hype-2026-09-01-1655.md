# HYPE 2026-09-01 16:55 — MAGNUM 3/10

> Хайп-генератор · бэклог 4 фичи · 3 новые идеи · Neon OK · очередь +5

## POST /magnum/api/ideas

Авторизован как `hypebot42` (id 20). 3 идеи успешно созданы в Neon `magnum_ideas`:

- id 130 `DUEL VOLCANO 42 — WS 2-4 + volcano x11 + eruption + ghost` → 201
- id 131 `ECO LES 42 — 8Q Лес/Тайга/Кузбасс + bio-вахта 7дн` → 201
- id 132 `CRYSTAL VAULT 42 — 12 скинов crystal + quartz epic 1420` → 201

`curl -s http://localhost:3000/magnum/api/ideas | jq .ideas[-3:]` подтверждает. Всего идей в БД: 107.

---

## Новые идеи (3)

| # | идея | приоритет | монетки | спека (3 строки) |
|---|------|-----------|---------|-------------------|
| 1 | **DUEL VOLCANO 42 — WS 2-4 + volcano x11 + eruption + ghost** | P0 | wager 0/42/142/420, win `+wager*2` +42 ELO, lose -12, draw возврат, топ-3 +1420 + crown | WS арена 2-4 на `Bun.serve ws /magnum/api/ws`: `lobby:create`→ABCD→`join`→`ready`→10с volcano-дуэль, volcano <0.12с +7% на клик капа x11, eruption 2.5x при x11, overheat 4с на x11→1.5с кулдаун -60% + volcano-pulse, ghost-volcano trail реалтайм `width volcano/11*100%`, CPS>20 suspect бан + throttle 30/сек, heartbeat 25с + реконнект fallback hot-seat. Сезон 7дн `magnum_leaderboard(game=duel42)` топ-3 `conic-volcano + crown`. |
| 2 | **ECO LES 42 — 8Q Лес/Тайга/Кузбасс + bio-вахта 7дн** | P1 | 42 Нормис 0-3 / 142 Братуха 4-6 / 420 Легенда 7-8 (1x/сутки) +1420 босс 8/8, freeze 420 | 8Q про Лес/Тайгу/Кедр/Томь/Кузбасс/42+эко + факты MAGNUM 8K/200K VPN 28.04 CLAY РЗТ73 NOVA РЗТ80/XXL86, экран `n/8` + карточка «1 га тайги — 5 т кислорода, Кузбасс рубит — 42 сажает» + conic бейдж. Недельная bio-вахта 7дн `magnum-eco-challenge:{weekId,streak}` пропуск→сгорает freeze 420 1x/нед, босс-вс 8/8. Шаринг OG 1080x1080 canvas + Web Share API +42. |
| 3 | **CRYSTAL VAULT 42 — 12 скинов crystal + quartz epic** | P0 | common 42 / uncommon 142 / rare 420 / epic 1420, крафт 42, разбор +100 dust | Vault 12 crystal: common 42 `crystal-matte/snow-quartz/ice-fence` #e8f8ff, uncommon 142 `siberia-crystal/tom-quartz/taiga-crystal` #a8e8ff, rare 420 `meduza-crystal/vpn-quartz/nova-crystal` conic-crystal, epic 1420 `gold-crystal-spin 3s/diamond-quartz/rgb-crystal` conic-crystal-spin. Покупка `POST /magnum/api/shop/cosmetic/buy` guard `getCoins()>=price` → `magnum_cosmetics` + `magnum_shop_inventory`. Крафт 3xcommon→uncommon fee 42, разбор rare→dust. |

### Детализация

#### 1. DUEL VOLCANO 42 — WS 2-4 + volcano x11 + eruption + ghost
- **Описание:** Закрывает backlog #1 — volcano-эволюция (после MAGMA x10: ещё экстремальнее — x11 + eruption). Хост «Создать арену» → `ws.send({type:"lobby:create",wager})` → сервер `room:ABCD` + `ws.subscribe('room:ABCD')` + `roomPublic()` → до 3 братух `join ABCD` → `ready` → авто-старт 10с volcano-дуэль. Ставка 0/42/142/420 из `magnum-coins` (`getCoins()>=wager` guard, `addCoins(-wager)` atomic перед WS). Игра: полноэкранный кликер 10с, счёт `raw clicks` + volcano: интервал <0.12с → цепочка, каждый следующий +7% к score (1.07→1.14→1.21→1.28→1.35→1.42→1.49→1.56→1.63→1.70→1.77 капа x11), при достижении x11 → `eruption 2.5x` к следующему клику + экран `volcano-fill height 0→100% 0.12s` + ash-bar переливается #ff5722→#d32f2f + `screen shake x:±9`. Удержание x11 >4с → overheat 1.5с кулдаун `score*0.35` + тёмный pulse + `navigator.vibrate(70)`, ghost-volcano полоска соперника `width opponentVolcano/11*100% opacity 0.45` в реалтайме через `ws broadcast {type:"tick", volcano}`. Анти-чит: `CPS>20` или `>165 clicks/10с` → `suspect:true` + бан раунда + не пишет в `magnum_leaderboard`, тост «братуха, ты вулкан? 🌋». Результат: сервер `broadcast {type:"finish", scores}` → `winner +wager*2 +42 ELO`, `loser -12`, draw возврат. Сезон 7дн `magnum_leaderboard(game=duel42)` Neon + `magnum-duel-history` LS 20 записей, топ-3 `conic-volcano + crown 🌋`.
- **Цена:** `0` френдли или `42/142/420`; победитель `+wager*2` + `+42 ELO` + `+42 dust` при volcano>=8, лузер `-12 ELO`, draw возврат ставок. Топ-3 сезона `+1420` + лимитка `volcano-crown-42` + `conic-volcano` рамка на неделю. Eruption бонус только score→ELO, не монеты напрямую.
- **GSAP:** `gsap.context` `LobbyGrid` stagger `y:24→0 0.12 power2.out`, `VolcanoStage` `shake x:±9 0.08s` на каждый клик + `scale 1→1.2 0.1`, volcano `VOLCANO xN` `scale 0→1.6→1 back.out(1.7)` + `y:-16` burst + `volcano-bar width 0→volcano/11*100% 0.3` + `ghost trail width 0→opponentVolcano/11*100% 0.2` + `volcano-fill height 0→100% 0.12` + `overheat shake x:±14 0.25 + ash pulse 0.6` + `navigator.vibrate(10+N*6)`, `Transit 3-2-1` `scale 2→1 + opacity 1→0 0.5`, конфетти 190 частиц canvas `particle(x,y,vx,vy,life, color #ff5722)` + `requestAnimationFrame`, `Leaderboard` `stagger y:10 0.05`, `prefers-reduced-motion` → `animation:none` + `transform:none`.

#### 2. ECO LES 42 — 8Q Лес/Тайга/Кузбасс + bio-вахта 7дн
- **Описание:** Закрывает backlog #2 — лес-вариация эко-квиза (после PLAST/SHAKHTA/ZAVOD/KEDR — фокус Лес/Тайга/Кедр/Томь). База — `EcoQuizPage` 8Q (Лес/Тайга/Кузбасс/42+эко) + факты MAGNUM (ТУСА МЕДУЗА 14.08 — 8K клипов/200K просмотров, VPN 28.04 в чартах, CLAY 03.04 РЗТ73, SUPER PUPER NOVA РЗТ80/XXL86). UI: прогресс `n/8` + `progress width n/8*100%` + карточка «А знаешь ли ты…» под вопросом (пропаганда: «1 га тайги — 5 т кислорода в год, Кузбасс рубит — 42 братухи сажают. Твоя шишка — твой лес»). Результат: уровни Нормис 0-3 / Братуха 4-6 / Легенда 7-8 → `conic-gradient forest→taiga` бейдж + `addCoins(42/142/420)` + шаринг `Я — Легенда Леса 42!` + график `magnum-eco-history` 10 попыток. Bio-вахта 7дн: LS `magnum-eco-challenge:{weekId:YYYY-Www, streak, lastDate}` — каждый день 1 задание (посадка кедра, уборка валежника, берег Томи от пластика, подсчёт колец дерева) + факт MAGNUM, пропуск→стрик сгорает, заморозка 420 1x/нед (`magnum-eco-freeze-used` guard weekId). Финал воскресенье — босс-квиз 8Q → `streak*42+420` если `7/7` + `+1420` за `8/8` босс + OG шаринг 1080x1080 canvas (градиент forest #22c55e + стата + `toDataURL`) + Web Share API + `magnum-share-claimed` +42 1x.
- **Цена:** `42` (Нормис) / `142` (Братуха) / `420` (Легенда) — 1x/сутки guard `lastDate`, `1420` за босс `8/8` + бейдж `Эко-Лес 42` + `conic-forest` рамка. Freeze `420`, шаринг `+42`.
- **GSAP:** `QuestionCard` stagger `y:18→0 0.1 power2.out`, `progress` tween `width 0→n/8*100% 0.4 power2.out`, `BadgeReveal` `scale 0→1 0.6 spring back.out(1.7)` + `conic spin 3s linear infinite` + `forest shimmer 1.5s`, `StreakCalendar` `stagger y:10 0.06`, `prefers-reduced-motion` → `opacity 0→1 0.2` без spin.

#### 3. CRYSTAL VAULT 42 — 12 скинов crystal + quartz epic + dust
- **Описание:** Закрывает backlog #3 — crystal-прозрачная палитра (эволюция GLACIER: кристалл + кварц vs лёд). Каталог `COSMETICS_CATALOG` 12: common 42 — `crystal-matte` 💎 `linear-gradient #e8f8ff,#c8e8ff` 1px crystal border, `snow-quartz` 🤍 `#fff→#e8f0ff`, `ice-fence` 🧊 `#b8e0ff→#dbeafe`; uncommon 142 — `siberia-crystal` 🏔️ `#a8e8ff→#7dd3ff`, `tom-quartz` 🌊 `#38bdf8→#0ea5e9`, `taiga-crystal` 🌲 `#0e7490→#164e63`; rare 420 — `meduza-crystal` 🪼 `conic-gradient crystal #a8e8ff→#38bdf8 + quartz glow`, `vpn-quartz` 📡 `crystal #e8f8ff + quartz #38bdf8 pulse 1.5s`, `nova-crystal` 💜 `#7c3aed→#38bdf8 + crystal`; epic 1420 — `gold-crystal-spin` 👑 `conic-crystal-gold spin 3s`, `diamond-quartz` 💎 `#e8f8ff→#fff + quartz gloss + spin 3s`, `rgb-crystal` 🌈 `crystal base + rgb-shift 2s + quartz`. Покупка: `getCoins()>=price` guard → `addCoins(-price)` atomic + `POST /magnum/api/shop/cosmetic/buy` Neon, инвентарь `magnum_cosmetics[]` + `magnum_shop_inventory` LS + `storage` sync. Экипировка: `equip` → один `equipped=true` per `slot` (frame/banner/title), `EquippedFrame` glow `box-shadow 0 0 24 crystal #38bdf8` для crystal + `backdrop-filter blur 6px`. Крафт: `FORGE` 3xcommon→1 uncommon fee 42, `3xuncommon→rare fee 142`, разбор `rare→+100 dust` / `epic→+420 dust` (dust `magnum-forge-dust`). Cross: `verified=1` → `-42` скидка на 1 покупку/нед + crystal glow в дуэли/эко.
- **Цена:** `common 42` · `uncommon 142` · `rare 420` · `epic 1420` (строго по спеке), крафт `42/142`, разбор `+100/+420 dust`. Старт 1000 = 23xcommon или 2xrare+1 uncommon. Скидка 42 за Эко-Лес Легенда 42.
- **GSAP:** `gsap.context` `ShopGrid` stagger `y:20→0 0.08 ease power2.out`, `SkinCard` hover `scale 1.02 + glow shadow 0 0 16 crystal #38bdf8` + `crystal shimmer 0.4s`, purchase `confetti 80 canvas crystal #a8e8ff` + `scale 0→1 spring back.out(1.7)`, `rarity badge` epic `crystal spin 3s linear infinite` + `conic-crystal`, `ForgeReveal` `scale 0→1 + rotate 5→0 0.5 + crystal pulse`, `prefers-reduced-motion` → `animation:none` + `transform:none`.

---

## Интеграция

- Файлы: `src/pages/games/DuelVolcano.tsx` + `src/lib/duelVolcano.ts` + `src/lib/ws.ts` + `server.ts:websocket lobby 2-4 volcano x11 eruption + ghost`, `src/pages/EcoLesPage.tsx` + `src/lib/ecoLes.ts` + `src/components/EcoBadge.tsx` + `src/components/ShareCard.tsx`, `src/lib/cosmetics.ts` (CATALOG 12 crystal + FORGE) + `src/pages/ShopCrystalPage.tsx` + `src/components/SkinCard.tsx` + `src/components/EquippedFrame.tsx` + `src/components/ForgeModal.tsx`
- LS: `magnum-duel-history/leaderboard/volcano`, `magnum-eco-challenge/history/freeze-used/share-claimed`, `magnum_cosmetics/inventory/equipped/forge-dust`, `magnum-frame-verified/frame-date`, `magnum-coins`, `magnum-arena-season`
- Neon: `magnum_cosmetics`, `magnum_shop_inventory`, `magnum_frames`, `magnum_leaderboard(game=duel42)`, `magnum_eco_challenge`, `magnum_ideas` (ids 130-132)
- Кросс: победа дуэли volcano→`StreakCalendar` + `RecapQuest` + `PresaveRatingPage`, эко-Лес Легенда→скидка 42 в forge + verified glow, покупка epic→`EquippedFrame` crystal glow в дуэли + эко бейдж
- Rate: `ws 30/сек` throttle + `ideas:post 10/мин` + `magnum-eco 1/сутки` + `forge 20/мин`

> Следующий тик 17:10 — ротация crystal + проверка голосов `magnum_ideas` (ids 130-132) + season reset 7дн.
