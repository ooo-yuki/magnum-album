# Perf 2026-09-01 16:58 UTC — stale /srv cleanup + health verify (no new heavy chunk)

**Ветка:** `master` · **HEAD:** 06155d7 → `perf: 16:58 — stale /srv cleanup` · **dist:** 35.2 MB (36954057 B) · **main:** 435 KB (444670 B, gz 129 KB, ratio 0.29)

## Задача (cron каждые 20 мин)
`du -sh` + `ls -lh dist/*.js | sort -hr` → тяжёлый чанк >50KB → lazy/vendor split/800px webp/preconnect/sitemap → 1 оптимизация → `curl magnum/ 200` + `api/ideas 200` + `magnum-bun active` → `tsc 0` + `Bun.build` + `cp -r dist/* /srv/magnum/` → отчёт + `git push`.

## Before / After

| Метрика | Before | After | Δ |
|---------|--------|-------|---|
| **main JS** | `main-mfm6pfjk.js` 435 KB (444670 B, gz 129 KB) | 435 KB (same hash, rebuild idempotent) | 0 — уже оптимизирован |
| **heavy >50KB** | `chunk-xnfzxrfj.js` 288 KB (RecapsPage lazy), `chunk-8y5w0cqf.js` 159 KB (Gallery lazy), `chunk-3z3pvc2z.js` 71 KB (gsap vendor) | все уже `lazy`/`vendor split` — нового eager >50KB нет | — |
| **dist total** | 36M (36954057 B, 83 файла, 1× main) | 36M | 0 |
| **/srv/magnum/** | 38M (91 файл, 5× `main-*.js` stale: 6ek99pgz/ct9mg5bf/mygdvwxb/p6bff8nc) | **37M** (87 файлов, 1× main) | **−1M (−4 stale)** |
| **outputs** | 83 dist / 91 srv | 83 / 87 | −4 stale |
| **build** | 1.45s | ~1.8s (79 ok, 71 minify ok, splitting ok) | — |
| **tsc** | 0 | **0** | OK |
| **sitemap** | 32 urls, synced false (lastmod 2026-09-01) | 32 | OK |

## Bundle-анализ (`ls -lh dist/*.js | sort -k5 -hr | head -10` → /tmp/opt.log)

```
-rw-r--r-- 1 root root 435K main-mfm6pfjk.js   ← entry (HomePage eager + Layout, 30 pages lazy)
-rw-r--r-- 1 root root 288K chunk-xnfzxrfj.js   ← RecapsPage (lazy, 3378L, 148 recaps)
-rw-r--r-- 1 root root 159K chunk-8y5w0cqf.js   ← GalleryPage (lazy, 441KB src, 8.8MB gallery-42)
-rw-r--r-- 1 root root  71K chunk-3z3pvc2z.js   ← gsap vendor (split)
-rw-r--r-- 1 root root  44K chunk-nrabzte5.js
-rw-r--r-- 1 root root  44K chunk-1z1he7b7.js
-rw-r--r-- 1 root root  42K chunk-rrskwxjv.js   ← Timeline (~576L, lazy below-fold HomePage)
-rw-r--r-- 1 root root  34K chunk-j71g62ee.js
-rw-r--r-- 1 root root  32K chunk-s2ap0947.js
-rw-r--r-- 1 root root  31K chunk-6tvyh48d.js
```

**Вывод:** нового тяжёлого eager чанка >50KB нет — все >120KB уже вынесены в `lazy`. `main 1037K → 509K → 460K → 435K` — история оптимизации завершена (App.tsx: 14 pages lazy + 16 games lazy + HomePage 4 below-fold lazy).

## Оптимизация этого тика (1): `stale /srv/magnum cleanup` + verify perf-активов

**Проблема:** `/srv/magnum/` копил stale `main-*.js` (каждый билд — новый hash, `cp -r dist/*` без очистки): 5× mains → 38M.

**Решение:** удалены 4 stale `main-*.js` в `/srv/magnum/` (оставлен только активный `main-mfm6pfjk.js` + `main-tbgb1qnt.css` 208KB). `dist/` уже чист (1× main).

**Verify всех perf-активов (п.3 — не трогал, проверил):**

| Актив | Проверка | Результат |
|-------|----------|-----------|
| `sitemap.xml` | `public/sitemap.xml` + `dist/sitemap.xml` | **32 urls** ✅ (`lastmod 2026-09-01`, `ROUTES` 32) |
| `robots.txt` | `public/robots.txt` | `Allow: /` + `Sitemap: https://5opka.ru/magnum/sitemap.xml` ✅ |
| `OG` | `index.html` | **10** тегов (`music.album`, 1280×720, `music:musician`) ✅ |
| `json-ld` | `index.html` | `MusicAlbum` 5 треков ✅ |
| `preconnect` | `index.html` | **6** preconnect (fonts.googleapis/gstatic, yandex, spotify, thefence) + 3 dns-prefetch + 2 preload image ✅ |
| `lazy images` | `grep loading="lazy" src/` | **13** (`AiBot` avatar/screens, `PressWall`, `Gallery` + `800.webp` 27K/34K/154K/16K) ✅ |
| `800px webp` | `public/images/` | `tusa-meduza-800.webp` 27K vs 68K jpg, `vpn-800.webp` 16K vs 67K, `postcard-800.webp` 154K vs 237K, `ai-bot-avatar.webp` 9.7K vs 90K ✅ |
| `prefers-reduced-motion` | `grep -r prefers-reduced-motion src/` | **392** хитов (global.css + CTA/Footer + gsap gates) ✅ |
| `gsap.context cleanup` | `grep -r gsap.context src/` | **52** usages, все с `ctx.revert()` + `matchMedia(reduce)` gate ✅ |
| `vendor split` | `dist/chunk-3z3pvc2z.js` 71K + `vendor-y7d64bp4.js` 1.1K | gsap изолирован ✅ |
| `HomePage below-fold` | `src/pages/HomePage.tsx` | `Timeline/PressWall/News2026/CTA` lazy + `Suspense minHeight` ✅ |

## Проверки (п.4)

| Проверка | Команда | Результат |
|----------|---------|-----------|
| `magnum/` | `curl -s -o /dev/null -w "%{http_code} %{time_total}s" http://localhost:30646/magnum/` | **200** 0.146s |
| `api/ideas` | `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/magnum/api/ideas` | **200** 0.59s |
| `magnum-bun` | `systemctl is-active magnum-bun` | **active** (since 16:54, 13.1M mem) |
| `tsc` | `bun x tsc --noEmit` | **0** |
| `dist stale` | `ls dist/main-*.js` | 1 файл (чисто) |
| `/srv stale` | `ls /srv/magnum/main-*.js` | 1 файл (почищено) |
| `build` | `bun run build.ts` | 79 outputs, splitting ok |

**Restart не требуется** — все 200, active.

## Деплой
`Bun.build` (splitting, minify, target browser, naming `[hash]`) → `cp -r dist/* /srv/magnum/` → stale cleanup. `index.html` 10 OG + json-ld уже в dist.

## Следующий шаг
`main 435K` — предел для текущего `HomePage` (eager Hero/Marquee/Stats/Singles/About/NavGrid 6 компонентов). Дальше — только `content-visibility: auto` уже есть, `modulepreload` для lazy chunks или `image 800.webp` для новых галерей. Следующий тик — проверить `main gzip 129K < 150K` бюджет, `prefers-reduced-motion` E2E.
