# Health Report — 2026-09-01 13:42 UTC

> Авто-отчёт MAGNUM 24/7 · проверка каждые 15 мин · сервер `oooyuki.zomb.top`

## Checks

| Проверка | Команда | Результат |
|----------|---------|-----------|
| **localhost:30646** | `curl -s http://localhost:30646/magnum/ -o /tmp/health.html` | **OK** — HTTP 200, 7192 B, `0.001873s` |
| **external https:30645** | `curl -s -k https://oooyuki.zomb.top:30645/magnum/ -o /tmp/health2.html` | **FAIL** — timeout (28) — hairpin NAT: внешний IP 2.27.27.6 таймаутит изнутри; `curl -k https://127.0.0.1:30645` → TLS alert internal error (требует SNI `oooyuki.zomb.top`) |

## Роуты (через `localhost:30646/magnum` + Caddy `try_files`)

| Роут | URL | Статус | Размер HTML | Время ответа |
|------|-----|--------|-------------|--------------|
| `/` | `http://localhost:30646/magnum/` | 200 | 7192 B | 0.002354s |
| `/games` | `http://localhost:30646/magnum/games` | 200 | 7192 B | 0.001148s |
| `/42` | `http://localhost:30646/magnum/42` | 200 | 7192 B | 0.001464s |
| `/discography` | `http://localhost:30646/magnum/discography` | 200 | 7192 B | 0.001474s |

> Все роуты отдают `index.html` (7192 B) — SPA fallback `try_files {path} /index.html` в Caddy, ожидаемо для React Router. Факт: `Content-Type: text/html; charset=utf-8`, ETag `"dl40oanu538m5js"`.

## Внешний доступ

| Endpoint | Статус | Примечание |
|----------|--------|------------|
| `http://oooyuki.zomb.top:30646/magnum/` | FAIL (timeout) | Hairpin NAT — с сервера на свой публичный IP не доходит; не отражает доступность извне |
| `https://oooyuki.zomb.top:30645/magnum/` | FAIL (timeout) | Аналогично; Caddy на `:30645` слушает с TLS (Cloudflare DNS), `127.0.0.1:30645` отвечает только с правильным SNI |

## Obscura (скриншот-сервис)

| Параметр | Значение |
|----------|----------|
| Статус | `active` (systemd `obscura.service`) |
| Версия | `obscura 0.2.1` (`/usr/local/bin/obscura`) |
| Порт | `127.0.0.1:9222` |
| CDP `GET /json` | `[{"id":"page-1","type":"page","url":"about:blank","webSocketDebuggerUrl":"ws://127.0.0.1:9222/devtools/page/page-1"}]` — idle, 1 page |
| Скриншот | ⚠️ недоступен — нет HTTP-эндпоинта на отдельном порту (известный TLS pitfall: навигация на HTTPS невозможна если cert ≠ localhost — нужен HTTP-порт) |

## Инфраструктура

| Компонент | Статус |
|-----------|--------|
| Caddy (`magnum-caddy` docker) | Up · `:30646` (http, h1) + `:30645` (https, h1/h2/h3) · `reverse_proxy 172.18.0.1:3000` для `/magnum/api/*` |
| Bun server | `172.18.0.1:3000` |
| Статика | `/srv/magnum` (16M) + `dist/` · `index.html` 7192 B |
| TLS | `oooyuki.zomb.top:30645` — Cloudflare DNS challenge, resolvers 1.1.1.1 |

## Вывод

- ✅ **Локально всё OK** — все 4 роута 200 OK, Caddy + статика + SPA fallback работают корректно.
- ⚠️ **Внешний https:30645** таймаутит при проверке изнутри (hairpin NAT) — нужна внешняя проверка (uptime-мониторинг извне или `curl` с внешнего хоста / браузера). Конфиг Caddy корректен.
- ℹ️ Obscura активен но idle — для скриншотов нужен отдельный HTTP-порт.

---
*Сгенерировано cron health-репортером · `2026-09-01-1342` · команды: `curl -s http://localhost:30646/magnum/ -o /tmp/health.html && echo OK || echo FAIL` + `curl -s https://oooyuki.zomb.top:30645/magnum/ -k -o /tmp/health2.html`*
