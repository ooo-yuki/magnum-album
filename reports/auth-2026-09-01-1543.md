# auth-guardian — 2026-09-01 15:43 UTC · ветка master · 127.0.0.1:3000

> Проверка авторизации MAGNUM 12/11 — все эндпоинты по спеке крона.

## 1) Таблица — эндпоинты без токена (ожидаем 401)

| endpoint | без токена ожидаем | факт | статус |
|----------|-------------------|------|--------|
| `GET /magnum/api/auth/me` | 401 `{"error":"unauthorized"}` | 401 `{"error":"unauthorized"}` | ✅ |
| `GET /magnum/api/coins` | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `GET /magnum/api/shop/inventory` | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `WS /magnum/api/ws` (Upgrade: websocket + Sec-WebSocket-Key/Version 13) | 401 `{"error":"unauthorized — войди, братуха"}` | 401 `{"error":"unauthorized — войди, братуха"}` | ✅ |

Детали сырых ответов (`/tmp/auth.log` + прямой прогон 15:43 UTC):

```
---auth me no cookie---
{"error":"unauthorized"} 401
{"error":"unauthorized"} 401
{"error":"unauthorized"} 401
HTTP/1.1 401 Unauthorized
Content-Type: application/json;charset=utf-8
{"error":"unauthorized — войди, братуха"}
AuthStatus has login
    if (url.pathname === "/magnum/api/ws" || url.pathname === "/magnum/api/ws/") {
      const token = extractToken(req);
      if (!token) return Response.json({ error: "unauthorized — войди, братуха" }, { status: 401 });
```

Вывод: **4/4 проверки из спеки — ✅.** Анонимы не проходят. `curl -s http://127.0.0.1:3000/magnum/api/auth/me` без cookie/header/token отдаёт `401`; аналогично `/coins`, `/shop/inventory`; WS handshake без токена — `401`, не `101 Switching Protocols`.

Прямые проверки 15:43 UTC:
- `curl -s http://127.0.0.1:3000/magnum/api/auth/me -w " %{http_code}"` → `{"error":"unauthorized"} 401` ✅
- `curl -s http://127.0.0.1:3000/magnum/api/coins -w " %{http_code}"` → `{"error":"unauthorized"} 401` ✅
- `curl -s http://127.0.0.1:3000/magnum/api/shop/inventory -w " %{http_code}"` → `{"error":"unauthorized"} 401` ✅
- `curl -i -H "Connection: Upgrade" -H "Upgrade: websocket" -H "Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==" -H "Sec-WebSocket-Version: 13" http://127.0.0.1:3000/magnum/api/ws` → `HTTP/1.1 401 Unauthorized` + `{"error":"unauthorized — войди, братуха"}` ✅
- `curl -s http://127.0.0.1:3000/magnum/api/health` → `200 {"ok":true}` (health открыт намеренно, без auth)

`bun --check server.ts` парсинг: `ok` (ошибка `EADDRINUSE :3000` — порт занят рабочим `magnum-bun.service`, ожидаемо). `systemctl status magnum-bun` — active running, `*:3000 LISTEN`.

## 2) Проверка фронта — кнопка входа

| проверка | команда | факт | статус |
|----------|---------|------|--------|
| `grep -q "Вход" src/components/AuthStatus.tsx` | спека крона | exit 0 — найдено `src/components/AuthStatus.tsx:152` `Войти` и `:158` `Вход` | ✅ |
| `grep -q "extractToken" server.ts` | спека | exit 0 — `server.ts:57` `function extractToken` | ✅ |
| `grep -A2 'pathname === "/magnum/api/ws"' server.ts` | спека | `server.ts:2176` — гейт с `extractToken` + `getUserByToken` + 401 | ✅ |

Файл: `src/components/AuthStatus.tsx:152,158`

```tsx
<button type="button" className={styles.login} onClick={() => setShowModal("login")}>Войти</button>
<button type="button" className={styles.register} onClick={() => setShowModal("register")}>Регистрация</button>
<h3 className={styles.modalTitle}>{showModal === "register" ? "Регистрация 42" : "Вход"}</h3>
```

Кнопка входа **есть** — модалка `Вход/Регистрация` с `fetch("/magnum/api/auth/login", {credentials:"include"})` и GSAP-анимацией (scale 0.96→1, overlay fade). Подключена в шапке. При `me==null` рендерит `authBtns` с двумя кнопками, при `me!=null` — `@username ✓` + logout `×`.

Сервер: `server.ts:57` `extractToken` читает `Authorization: Bearer …` → `?token=` → `magnum_token` cookie. `server.ts:2176-2185` — WS гейт:

```ts
if (url.pathname === "/magnum/api/ws" || url.pathname === "/magnum/api/ws/") {
  const token = extractToken(req);
  if (!token) return Response.json({ error: "unauthorized — войди, братуха" }, { status: 401 });
  let user = null; try { user = await getUserByToken(token); } catch {}
  if (!user) return Response.json({ error: "unauthorized — войди, братуха" }, { status: 401 });
  const ok = server.upgrade(req, { data: { id: String(user.id), username: user.username, roomId: null } });
  if (ok) return undefined as unknown as Response;
  return Response.json({ error: "Upgrade failed" }, { status: 426 });
}
```

## 3) Найденный баг — файл:строка и план фикса

### Баг #1 (средний, остался с прошлого прогона) — `server.ts:1966` `handleAi` открыт без auth и без rate-limit

**Файл:** `server.ts:1966` `async function handleAi(req: Request): Promise<Response>`

```ts
async function handleAi(req: Request): Promise<Response> {
  if (req.method !== "POST") return Response.json({ error: "POST only" }, { status: 405 });
  const apiKey = process.env.XIAOMI_API_KEY;
  if (!apiKey) return Response.json({ error: "XIAOMI_API_KEY not configured" }, { status: 500 });
  let body = await req.json() as { text?: string; history?: ... };
  // сразу fetch к ${MIMO_BASE}/chat/completions — без extractToken / getUserByToken / checkRateLimit
```

**Проблема:** `POST /magnum/api/ai` — единственный POST-эндпоинт без `extractToken` и без `checkRateLimit`. Любой аноним может слать `text` до 2000 + `image` dataUrl + `history` и жечь `XIAOMI_API_KEY` / MIMO-квоту (model `mimo` `max_tokens: 400`). В отличие от `handleEcoSubmit`/`handlePresaveClick` где анонимность осознанная (публичные счётчики), здесь — платный прокси к LLM. Нет логирования `user_id`/`ip` в `magnum_transactions`. При флуде — 502/429 от апстрима, но биллинг уже списан.

Сопоставление с другими хендлерами: `handleCoinsAdd:229`, `handleShopBuy:499`, `handleMiningClick:931` — все начинают с `const token=extractToken(req); if(!token) return 401`. `handleAi` — единственный `handle*` POST без этой строки (проверено `grep -n "^async function handle" server.ts` — 42 хендлера, все POST кроме `handleAi`, `handleIdeasVote` anon-fallback, `handleEcoSubmit` anon-fallback имеют гейт).

**План фикса (без ломания UX — пресейв-бот нужен гостям):**

1. Добавить в `server.ts:1966` сразу после `if (req.method !== "POST")` ветку:
   ```ts
   const ip = getClientIp(req);
   if (!checkRateLimit(`ai:${ip}`, 8, 60_000)) return Response.json({ error: "rate limited" }, { status: 429 });
   const token = extractToken(req);
   let authed: { id:number; username:string }|null = null;
   if (token) try { authed = await getUserByToken(token); } catch {}
   // опционально: if (!authed) return Response.json({ error: "unauthorized — войди, братуха" }, { status: 401 });
   // мягкий вариант: анонимам — строже лимит 3/мин и без image, авторизованным — 8/мин с image
   if (!authed && imageDataUrl) return Response.json({ error: "image requires auth" }, { status: 401 });
   ```
2. Логировать `INSERT INTO magnum_ai_calls (user_id, ip, text_len, created_at) VALUES (...)` аналогично `magnum_presave_clicks`.
3. Добавить в `server.ts` рядом с `MIMO_BASE` константу `AI_RATE_LIMIT_ANON = 3`, `AI_RATE_LIMIT_AUTH = 8`.
4. Проверка: `curl -s -X POST http://127.0.0.1:3000/magnum/api/ai -H "Content-Type: application/json" -d '{"text":"hi"}'` — без токена должен отдать `401` (строгий) или `429` на 4-й запрос в минуту (мягкий). С `Authorization: Bearer <valid>` — `200 {text:...}`.
5. Тест: `bun test` — добавить `tests/ai-auth.test.ts` — `expect(fetch("/magnum/api/ai", {method:"POST"}) без токена).toBe(401|429)`.

Приоритет: **средний** — не блокирует релиз (биллинг MIMO пока копеечный), но перед промо/прессой закрыть обязательно. Текущий auth-guardian помечает 🔴 только если `/auth/me`, `/coins`, `/shop/inventory` или `WS` отдают не-401 — сейчас все ✅, поэтому блок `КРИТИЧНО` не ставится.

### Дополнительные наблюдения (не баги auth, информативно)

- `server.ts:295` `handleIdeasVote` anon-fallback — аноним может накручивать `votes` без `magnum_idea_votes` дедупа (только `rateLimit 12/мин по IP`). Осознанно — публичное голосование, но стоит добавить капчу/soft-auth при росте.
- `server.ts:781` `handleEcoSubmit` / `server.ts:838` `handleEcoRatingSubmit` — anon fallback с `player` полем, но с `checkRateLimit` — ок.
- `server.ts:1432` `handlePresaveClick` — уже пофикшен в прогоне 15:29 UTC (был `url.includes(""")` синтаксис-баг, теперь `url.includes('"')` — `ok`).

## 4) Итог

| категория | результат |
|-----------|-----------|
| Auth эндпоинты без токена (3 REST + 1 WS) | **4/4 ✅** — все `401` |
| Кнопка входа на фронте | **✅** `AuthStatus.tsx:152` `Войти` + `:158` `Вход` |
| WS гейт `extractToken` + `getUserByToken` | **✅** `server.ts:57` + `server.ts:2176` |
| Критичных 🔴 (открытый доступ к приватным данным) | **0** |
| Найденных багов с планом фикса | 1 — `server.ts:1966` `handleAi` без auth/rate-limit (средний) |

Следующий прогон: `2026-09-01 15:53 UTC` (каждые 10 минут, cron `auth-guardian 12/11`).

---
*Сгенерировано кроном `auth-guardian 12/11` · `reports/auth-2026-09-01-1543.md` · `git commit auth: 15:43 — check`*
