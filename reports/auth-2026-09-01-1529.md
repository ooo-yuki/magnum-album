# auth-guardian — 2026-09-01 15:29 UTC · ветка master · 127.0.0.1:3000

> Проверка авторизации MAGNUM 12/11 — все эндпоинты по спеке крона.

## 1) Таблица — эндпоинты без токена (ожидаем 401)

| endpoint | без токена ожидаем | факт | статус |
|----------|-------------------|------|--------|
| `GET /magnum/api/auth/me` | 401 `{"error":"unauthorized"}` | 401 `{"error":"unauthorized"}` | ✅ |
| `GET /magnum/api/coins` | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `GET /magnum/api/shop/inventory` | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `WS /magnum/api/ws` (Upgrade: websocket + Sec-WebSocket-Key/Version 13) | 401 `{"error":"unauthorized — войди, братуха"}` | 401 `{"error":"unauthorized — войди, братуха"}` | ✅ |

Детали сырых ответов (`/tmp/auth.log`):

```
---auth me no cookie---
{"error":"unauthorized"} 401
{"error":"unauthorized"} 401
{"error":"unauthorized"} 401
HTTP/1.1 401 Unauthorized
Content-Type: application/json;charset=utf-8
{"error":"unauthorized — войди, братуха"}
AuthStatus has login
    if (url.pathname === "/magnum/api/ws" || url.pathname === "/magnum/api/ws/") {
      const token = extractToken(req);
      if (!token) return Response.json({ error: "unauthorized — войди, братуха" }, { status: 401 });
```

Вывод: 4/4 проверки из спеки — ✅. Анонимы не проходят. `curl -s http://127.0.0.1:3000/magnum/api/auth/me` без cookie/header/token отдаёт `401`; аналогично `/coins`, `/shop/inventory`; WS handshake без токена — `401`, не `101`.

> Примечание крона: на старте прогона (15:26 UTC) сервер был в crash-loop — `curl` возвращал `000 Connection refused`, `systemctl status magnum-bun` = `activating (auto-restart)`, `journalctl` показал причину (см. баг #0 ниже). После фикса `systemctl restart` — `active (running)`, `*:3000 LISTEN`, `GET /magnum/api/health 200`. Текущий прогон фиксирует состояние **после** фикса.

## 2) Проверка фронта — кнопка входа

| проверка | команда | факт | статус |
|----------|---------|------|--------|
| `grep -q "Вход" src/components/AuthStatus.tsx` | спека крона | exit 0 — найдено `src/components/AuthStatus.tsx:152,158` | ✅ |
| `grep -q "extractToken" server.ts` | спека | exit 0 — `server.ts:57` `function extractToken` | ✅ |
| `grep -A2 'pathname === "/magnum/api/ws"' server.ts` | спека | `server.ts:2123` — гейт с `extractToken` + `getUserByToken` + 401 | ✅ |

Файл: `src/components/AuthStatus.tsx:152,158`

```tsx
<button type="button" className={styles.login} onClick={() => setShowModal("login")}>Войти</button>
<button type="button" className={styles.register} onClick={() => setShowModal("register")}>Регистрация</button>
<h3 className={styles.modalTitle}>{showModal === "register" ? "Регистрация 42" : "Вход"}</h3>
```

Кнопка входа **есть** — модалка `Вход/Регистрация` с `fetch("/magnum/api/auth/login", {credentials:"include"})` и GSAP-анимацией. Подключена в шапке.

Сервер: `server.ts:57` `extractToken` читает `Authorization: Bearer `, `?token=`, `magnum_token` cookie. `server.ts:2123-2129` — WS гейт:

```ts
if (url.pathname === "/magnum/api/ws" || url.pathname === "/magnum/api/ws/") {
  const token = extractToken(req);
  if (!token) return Response.json({ error: "unauthorized — войди, братуха" }, { status: 401 });
  let user = null; try { user = await getUserByToken(token); } catch {}
  if (!user) return Response.json({ error: "unauthorized — войди, братуха" }, { status: 401 });
  const ok = server.upgrade(req, { data: { id: String(user.id), username: user.username, roomId: null } });
```

## 3) Найденный баг — файл:строка и план фикса

### Баг #0 (P0 — КРИТИЧНО, уже пофикшен в этом прогоне) — `server.ts:1433` синтаксис ломает весь сервер

**Файл:** `server.ts:1433` `handlePresaveClick`

```ts
// до фикса:
if (url.length > 300 || url.includes("<") || url.includes(""")) return Response.json({ error: "invalid url" }, { status: 400 });
```

**Проблема:** строка `url.includes(""")` — неэкранированная `"` внутри `"`-строки. `bun --check` / `Bun.serve` падает с:
```
error: Expected ")" but found "")) return Response.json({ error: ""
error: Unterminated string literal
  at /root/magnum-album/server.ts:1433:109
  Bun v1.4.0
```
`magnum-bun.service` уходит в `activating (auto-restart)` → `Connection refused` → **все** `/magnum/api/*` отдают `000`, а не `401` (авторизация формально «проходит» отсутствием сервера). Крон в 15:26 зафиксировал `000` на `/auth/me`, `/coins`, `/shop/inventory`.

**Фикс (применён в этом прогоне, коммит ниже):**

```ts
// после фикса server.ts:1433:
if (url.length > 300 || url.includes("<") || url.includes('"')) return Response.json({ error: "invalid url" }, { status: 400 });
```

`bun --check server.ts` → `ok`, `systemctl restart magnum-bun` → `active (running)`, `ss -tlnp` → `*:3000 LISTEN`, `curl /magnum/api/health` → `200`. Проверено в этом же прогоне.

### Баг #1 (средний, остался) — `server.ts:1908` `handleAi` открыт без auth и rate-limit

**Файл:** `server.ts:1908`

```ts
async function handleAi(req: Request): Promise<Response> {
  if (req.method !== "POST") return Response.json({ error: "POST only" }, { status: 405 });
  const apiKey = process.env.XIAOMI_API_KEY;
  // ... сразу парсит body и fetch к MIMO_BASE без проверки токена
```

Без `extractToken`/`getUserByToken`, без `checkRateLimit` — любой аноним жжёт `XIAOMI_API_KEY` (400 токенов × `temperature:0.9`). Отличается от публичных `GET /magnum/api/ideas`, `GET /magnum/api/coins/top` которые by design публичны.

**План фикса:**

```ts
async function handleAi(req: Request): Promise<Response> {
  if (req.method !== "POST") return Response.json({ error: "POST only" }, { status: 405 });
  const token = extractToken(req);
  if (!token) return Response.json({ error: "unauthorized" }, { status: 401 });
  const user = await getUserByToken(token);
  if (!user) return Response.json({ error: "unauthorized" }, { status: 401 });
  const ip = getClientIp(req);
  if (!checkRateLimit(`ai:${user.id}:${ip}`, 10, 60_000)) return Response.json({ error: "rate limited" }, { status: 429 });
  // ... далее существующая логика
}
```

Опция мягче — оставить анонимам, но добавить `checkRateLimit(`ai:${ip}`, 5, 60_000)`.

**Дополнительно:** `server.ts:57` `extractToken` берёт токен из `?token=` в URL — утечка в логи/Referer. Рекомендация: убрать `url.searchParams.get("token")`, оставить `Authorization + magnum_token cookie`.

## 4) Итог

- 4/4 спеки — ✅ (401 без токена, WS 401)
- Фронт — ✅ (`Войти`/`Вход` + `Регистрация` + модалка `AuthStatus.tsx:152`)
- `extractToken` + `getUserByToken` гейт на WS — ✅ (`server.ts:2123`)
- P0 баг синтаксиса `server.ts:1433` — **найден и пофикшен** в этом прогоне, сервер поднят
- Остался средний баг — открытый `/magnum/api/ai` без auth (см. выше)

---
*auth-guardian 12/11 · `curl` без cookie + `grep -q "Вход" AuthStatus.tsx` + `grep -q extractToken server.ts` · `/tmp/auth.log` приложен выше · сервер после фикса: `active (running)`*
