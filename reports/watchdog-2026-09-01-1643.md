# Watchdog 2026-09-01 16:42 UTC — OK (all 200, active, Up, 8 галерея, дрифт Neon без рестарта)

**Ветка:** `master` · **HEAD:** `ca3406a` · **Проверок:** 7/7 · **Рестартов:** 0 · **Cron:** `1/10` каждые 5 мин (watchdog MAGNUM 1/10)

## Health

| # | Проверка | Ожидается | Факт | Код/Статус |
|---|----------|-----------|------|------------|
| 1 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | ✅ 200 | `200` `text/html` `9967B` `<!DOCTYPE html> <html lang="ru"> MAGNUM — 5opka` `TLS Caddy 30645 --resolve 127.0.0.1` |
| 2 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | ✅ 200 | `200` `application/json` `{"ideas":[...104 total]}` `id:65 votes:142 approved` `id:66 votes:42` `size ~48KB` |
| 3 | `curl -s http://127.0.0.1:3000/magnum/api/ideas` | 200 | ✅ 200 | `200` JSON, идентичен (2) — `Bun.serve :3000` прямой `head id:65` |
| 4 | `systemctl is-active magnum-bun` | active | ✅ active | `active (running)` since `2026-09-01 16:36:36 UTC` PID `325846` `enabled` `Memory 51.2M` `Tasks 7` |
| 5 | `docker ps \| grep magnum-caddy` | Up | ✅ Up | `magnum-caddy` `omniroute-caddy:latest` `Up 2 hours` `0.0.0.0:30645-30646->30645-30646/tcp` `25f92f653c48` |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | ⚠️ 8 (факт) | `42-agit-01.jpg 3.4M + 42-agit-01-800.webp 119K`, `42-cyber-01.jpg 2.9M + 42-cyber-01-800.webp 133K`, `42-memphis-01.jpg 2.4M + 42-memphis-01-800.webp 77K`, `42-y2k-01.jpg 2.4M + 42-y2k-01-800.webp 67K` — 4 стиля ×2 (jpg+webp) `12M` `dist/images/gallery-42/` синхронен. Спека ожидает 6 — drift. |
| 7 | `git status --short` | — | ✅ clean | `master` без изменений `9855dd3 review: 16:40 — rating 1/10` `git log -3: 9855dd3, 4e23110 backlog +10, d56e8e4 perf lazy` |

**Доп:** `journalctl -u magnum-bun -n 5` `16:41:43 NeonDbError 42P01 relation "magnum_ai_usage" does not exist` `parse_relation.c:1498 parserOpenTable` — health глушит `try/catch` и отдаёт `counts.follows:0 aiUsage:0 ok:true`. `ss *:3000 LISTEN bun 325846`. Caddy `30645 TLS + :30646 HTTP` оба `reverse_proxy 172.18.0.1:3000` per `/opt/magnum-caddy/Caddyfile`. `curl` c `--resolve 127.0.0.1` подтверждает терминацию.

## Коды ответов

```
https://oooyuki.zomb.top:30645/magnum/             → 200 text/html 9967B TLS Caddy 30645 --resolve 127.0.0.1 Bun.serve прокси
https://oooyuki.zomb.top:30645/magnum/api/ideas    → 200 application/json {"ideas":[{id:65 votes:142 approved "Freakland Create gen3 130"}…104 total]} ~48KB
http://127.0.0.1:3000/magnum/api/ideas              → 200 application/json (Bun.serve :3000 прямой, идентичен TLS)
http://127.0.0.1:3000/magnum/                       → 200 text/html (Bun.serve :3000)
https://oooyuki.zomb.top:30645/magnum/api/health   → 200 application/json {"ok":true counts:{users:25 coins:24 mining:10 presave:5 ideas:104 follows:0 aiUsage:0} uptime:330s}
systemctl is-active magnum-bun                     → active (running) since 2026-09-01 16:36:36 UTC PID 325846 enabled
docker ps magnum-caddy                             → Up 2 hours omniroute-caddy:latest 25f92f653c48 0.0.0.0:30645-30646->30645-30646/tcp
ls public/images/gallery-42/                       → 8 файлов (4×jpg + 4×webp) base: 42-agit-01/42-cyber-01/42-memphis-01/42-y2k-01
git status --short                                 → (пусто) clean
```

## Рестарты

| Сервис | Действие | Причина | Результат |
|--------|----------|---------|-----------|
| magnum-bun | — | не требовался (все 200, active, 16:41 journal только 42P01 уже известный) | — |
| magnum-caddy (docker compose) | — | не требовался (Up 2 hours, 30645 LISTEN, --resolve 200 на обоих эндпоинтах, Caddyfile unchanged) | — |

> Критерий рестарта: любой не-200 или не-active → `systemctl restart magnum-bun; cd /opt/magnum-caddy && docker compose up -d; sleep 3; curl --resolve повтор`. Не выполнялся — все пробы зелёные. `journalctl` 42P01 не триггерит рестарт т.к. health маскирует 200.

## Найденные баги (файл:строка)

### 1) `server.ts:2168-2169` + `drizzle/schema.ts:281,288` + `drizzle/migrations/0016_chat_follows.sql` / `0017_ai_guard.sql` — дрифт схемы Neon: таблицы не применены, health врёт 200 (P0, повтор)

```ts
// server.ts:2168 — health, строка 2168
try { const r = await sql`SELECT count(*)::int as c FROM magnum_follows`; followsCount = Number((r[0] as {c:number}).c); } catch (e) { console.error("[health] follows count failed", e); }
// server.ts:2169
try { const r = await sql`SELECT count(*)::int as c FROM magnum_ai_usage`; aiUsageCount = Number((r[0] as {c:number}).c); } catch (e) { console.error("[health] ai_usage count failed", e); }
```

`journalctl -u magnum-bun --no-pager -n 5` на `16:41:43 UTC` (PID 325846):

```
NeonDbError: relation "magnum_ai_usage" does not exist  code:"42P01" file:"parse_relation.c" line:"1498" routine:"parserOpenTable"
NeonDbError: relation "magnum_follows" does not exist  (тот же 42P01, см. 16:34 отчёт)
```

`drizzle/schema.ts:281` (`magnumFollows`) и `:288` (`magnumAiUsage`) описаны, миграции `0016_chat_follows.sql` и `0017_ai_guard.sql` лежат в репо, но в прод Neon (`DATABASE_URL` us-east-2 `proud-bar-62331523`) не применены — health глушит `catch` и возвращает `counts:{follows:0,aiUsage:0}` с `ok:true`, маскируя деградацию. `GET /magnum/api/health` показал `follows:0 aiUsage:0` при `users:25 ideas:104`. Любой `POST /magnum/api/follows` (`server.ts:1877-1882`) и `POST /magnum/api/ai` ledger `server.ts:2273,2353-2355` упадёт 500. 42P01 повторяется каждый watchdog (14:11→16:41). Фикс см. идею.

### 2) `src/pages/games/Game2042.tsx:255,256,286,348,351` + `src/pages/games/BlackjackGame.tsx:221,224,253,549` + `src/pages/HomePage.tsx:271,288` — `localStorage` в играх/баннере вопреки архитектуре "без localStorage" (P1, расширяет AiBot/PromoPopup дрифт)

```ts
// src/pages/games/Game2042.tsx:255-256 — best/bestTile в LS
const [best, setBest] = useState(() => { try { return Number(localStorage.getItem("2042-best")) || 0; } catch { return 0; } });
const [bestTile, setBestTile] = useState(() => { try { return Number(localStorage.getItem("2042-bestTile")) || 0; } catch { return 0; } });
// src/pages/games/Game2042.tsx:286 — токен из LS вместо cookie
const tk = (()=>{ try{ return localStorage.getItem("auth_token")||localStorage.getItem("magnum_token")||"";}catch{return "";}})();
// src/pages/games/Game2042.tsx:348,351
try { localStorage.setItem("2042-best", String(newScore)); } catch {}
// src/pages/games/BlackjackGame.tsx:221,224,253
try{ const v=localStorage.getItem(LS_BALANCE); return v?Number(v):START_BALANCE; }catch{return START_BALANCE;}
useEffect(()=>{ try{ localStorage.setItem(LS_BALANCE,String(balance)); }catch{} ... },[balance,best]);
// src/pages/HomePage.tsx:271,288
if (localStorage.getItem("magnum-banner-closed") === "1") return;
try { localStorage.setItem("magnum-banner-closed", "1"); } catch {}
// аналогично Dodge42Game.tsx:125,127,133,355 Flappy42Game.tsx:166-168,293-294 — 12+ мест
```

Спека MAGNUM: "all state in Neon Lakebase Postgres (magnum_users/sessions...), multiplayer via WebSocket on Bun.serve, no localStorage" (memory, USER PROFILE). Код игр полностью на `localStorage` для `2042-best`, `blackjack42-balance/best`, `dodge42-best`, `flappy42-best/diff/skin`, `magnum-banner-closed`. `Game2042.tsx:286` даже берёт `auth_token` из LS вместо `credentials:include` cookie (как в `src/lib/coins.ts:4` — "localStorage полностью удалён, токен в cookie"). При очистке LS — прогресс/баланс 0, мульти-таб гонка, рассинхрон с `magnum_game_scores` / `magnum_coins`. `AiBot.tsx:77,334,355` + `PromoPopup.tsx:21,61,98` уже отмечены в 16:34 отчёте — этот баг показывает, что LS-дрифт системный (4 игровых файла + HomePage), не точечный. Надо выпилить все `localStorage.getItem/setItem` и заменить на `fetch("/magnum/api/games/submit",{credentials:"include"})` + `GET /magnum/api/balance` / Neon `magnum_game_scores`, а `magnum-banner-closed` — в cookie или Neon `magnum_notifications`.

## Идея починки (1)

**Миграционный health-gate + LS-выпил за 20 мин (закрывает оба бага без рестарта Caddy):** 1) Применить дрифт: `cd /root/magnum-album && DATABASE_URL=$DATABASE_URL_UNPOOLED bunx drizzle-kit push --force` (или `psql $DATABASE_URL_UNPOOLED -f drizzle/migrations/0016_chat_follows.sql -f drizzle/migrations/0017_ai_guard.sql`), проверить `psql -c "SELECT to_regclass('magnum_follows'), to_regclass('magnum_ai_usage')"` без null и `SELECT count(*) FROM magnum_follows` без 42P01, затем `systemctl restart magnum-bun` и `curl --resolve .../magnum/api/health | jq .counts` должен показать `follows:0 aiUsage:0` без `failed` в `journalctl`, а `POST /magnum/api/ai` — 200. Патч `server.ts:2168-2169`: заменить `console.error`+продолжить на `throw`/`return Response.json({ok:false,missing:["magnum_follows","magnum_ai_usage"]},{status:500})`, чтобы health не врал 200 и watchdog триггерил рестарт/алерт. Добавить post-deploy `SELECT`-чек в `.github/workflows` после `bun run build.ts`. 2) LS-выпил: удалить `localStorage` из `Game2042.tsx:255-256,286,348,351`, `BlackjackGame.tsx:221,224,253,549`, `Dodge42Game.tsx:125,127,133`, `Flappy42Game.tsx:166-168,293`, `HomePage.tsx:271,288` — заменить на `fetch` с `credentials:"include"` уже есть для submit (оставить только Neon), `best` хранить в `magnum_game_scores` (+ `magnum_coins` для Blackjack). Заодно поправить watchdog-спеку галереи: проверка `ls public/images/gallery-42/` ожидает 6, факт 8 — заменить на `count jpg ==4 && webp ==4 && total 8` или считать `*.jpg` отдельно. Это закроет тихий 500 на follows/ai, уберёт нарушение "no localStorage" в играх и ложный fail галереи.
