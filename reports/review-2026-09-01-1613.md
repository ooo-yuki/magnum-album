# MAGNUM Review — 2026-09-01 16:13 UTC (10м) — 7/10 ревьюер

**Период:** 16:03–16:13 UTC (10м) · ветка `master` @ `11ddb07` (+ `613fb20 fix`) · `git log --since="10 minutes ago" --numstat`
**Коммитов в окне:** **8** (3 продуктивных + 5 отчётов/watchdog/backlog) · `reports/` в `.gitignore` с 15:52 — локально, пуш через `git add -f`
**Рейтинг:** **3/10** — **819 строк** (ADDED 778 / REMOVED 41) · продуктивных (без `reports/`) **279 строк** (268/11) → отдельно 2/10 · шкала: 0-49=0, 50-199=1, 200-499=2, 500-999=3, 1000-1499=4, 1500-1999=5, 2000-2999=6, 3000-3999=7, 4000-4999=8, 5000+=10 · норма ~10к/10м (80-200/фича)
**Статус сборки:** `tsc` **PASS 0** · `tests` **PASS 3141/3141** · `gallery` 8 файлов 200×4 (4 стиля×2) · `health` PASS 200/200/200 · `services` PASS (magnum-bun active, caddy Up ~1h)

---

## Коммиты (10м, `git log --since="10 minutes ago" --numstat --oneline | head -120`)

| Хэш | Сообщение | + | − | Файлы |
|-----|-----------|---|---|-------|
| `11ddb07` | backlog: 16:12 — +10 задач | 19 | 17 | docs/BACKLOG.md (19/17) |
| `613fb20` | fix(report): 16:11 — ai 401 + gallery Y2K fallback + DDL/validator sync (tsc 0, 3141 PASS) | 37 | 0 | reports/fix-report-2026-09-01-1611.md (37/0) |
| | | 1 | 1 | src/pages/games/BlackjackGame.tsx (1/1) |
| | | 1 | 1 | src/pages/games/RouletteGame.tsx (1/1) |
| `a4c6a25` | watchdog: 16:11 — OK (all 200, no restart) | 87 | 0 | reports/watchdog-2026-09-01-1611.md (87/0) |
| `8afd888` | feat(api): P0 ai guard — anon 8/мин + image requires auth + Neon magnum_ai_usage — handleAi ledger | 16 | 0 | drizzle/migrations/0017_ai_guard.sql (16/0) |
| | | 7 | 0 | drizzle/migrations/meta/_journal.json (7/0) |
| | | 10 | 0 | drizzle/schema.ts (10/0) |
| | | 70 | 7 | server.ts (70/7) |
| | | 3 | 3 | src/pages/GalleryPage.tsx (3/3) |
| | | 1 | 1 | src/pages/ShopPage.tsx (1/1) |
| `9c336dd` | test: 16:08 — 3141 passed | 84 | 0 | reports/test-2026-09-01-1608.md (84/0) |
| | | 110 | 0 | tests/new-coverage-1607.test.ts (110/0) |
| `89658a0` | auth: 16:08 — check | 103 | 0 | reports/auth-2026-09-01-1607.md (103/0) |
| `70cb919` | feat(content): PRISM 42 — 12 неон-призм + пыль/dust крафт/разбор — Shop 44 + Neon magnum_dust | 83 | 0 | server.ts (83/0) |
| | | 68 | 11 | src/pages/ShopPage.tsx (68/11) |
| `fe59254` | watchdog: 16:04 — OK (8/8 green, no restart, 2 bugs) | 78 | 0 | reports/watchdog-2026-09-01-1604.md (78/0) |

*Окно 10м: 778/41 = 819 строк. Продуктивных (без `reports/`): 268/11 = 279 строк — 2 фичи (ai guard ledger + PRISM/dust) + 1 фикс галереи + 1 backlog. Для 10/10 нужно ~5000 строк (~7-8 фич по 80-200 + тесты) — фактический темп 0.82к/10м → 3/10.*

---

## Статусы

| Проверка | Результат | Детали |
|----------|-----------|--------|
| **tsc --noEmit** | ✅ PASS 0 | `~/.bun/bin/bunx tsc --noEmit 2>&1 | head -80` → пусто, exit 0 · после `613fb20` фикс линта |
| **tests (bun test)** | ✅ PASS 3141/3141 | 0 fail · 10014 expects · 35 файлов · 1.48s · было 3130 (15:59) → 3141 (+11) via `tests/new-coverage-1607.test.ts` 110 строк (ai guard + prism sync) |
| **gallery — файлы** | ✅ 8 файлов | `public/images/gallery-42/`: `42-agit-01.{jpg 3.4M, 800.webp 119K}`, `42-cyber-01.{jpg 2.9M, 800.webp 133K}`, `42-memphis-01.{jpg 2.4M, 800.webp 77K}`, `42-y2k-01.{jpg 2.4M, 800.webp 67K}` — 12M, 4 стиля ×2 (jpg+webp) |
| **gallery — curl 4 реальных** | ✅ 200×4 | `curl http://127.0.0.1:3000/magnum/images/gallery-42/42-agit-01-800.webp` → 200, `42-cyber-01-800.webp` → 200, `42-memphis-01-800.webp` → 200, `42-y2k-01-800.webp` → 200 · через `:3033` 000 (ожидаемо, bun на :3000) |
| **gallery — src grep** | ✅ PASS (фикс Y2K) | `GalleryPage.tsx:30-35 REAL_BY_STYLE` ок (СССР→agit, Y2K→y2k, киберпанк→cyber, мемфис→memphis), `REAL_FALLBACK:36-43` исправлен (`y2k-01→y2k`, `y2k-02→y2k.jpg`, `y2k-03→y2k`) — Y2K больше не показывает мемфис |
| **gallery — dist sync** | ✅ | `build.ts` hash+vendor+splitting + `dist/images/gallery-42` sync 8 файлов, hash intact |
| **health — magnum/** | ✅ 200 | `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3000/magnum/` → 200 |
| **health — api/ideas** | ✅ 200 | `http://127.0.0.1:3000/magnum/api/ideas` → 200 JSON |
| **health — api/health** | ✅ 200 | `server.ts` `GET /magnum/api/health` {ok:true} + counts 15 таблиц (users 24, coins 23, ideas 98, + aiUsage 0) |
| **services — magnum-bun** | ✅ active | `systemctl is-active magnum-bun` → active (running) PID 300048, :3000 |
| **services — caddy** | ✅ Up ~1h | `docker ps` → `magnum-caddy Up About an hour` `omniroute-caddy:latest` `0.0.0.0:30645-30646->30645-30646/tcp` |
| **git status** | ✅ clean | `git status --porcelain` пусто (кроме нового review), `reports/` игнорируется `.gitignore` |

---

## Замечания (3 конкретных, файл:строка)

### 1. `server.ts:2232-2241` — `handleAi` перефиксил anon: теперь все anon → 401, а по ТЗ `anon 8/мин` текст должен проходить

```ts
// server.ts:2237-2241 (текущий код)
const rawToken = extractToken(req);
if (!rawToken) return Response.json({ error: "unauthorized" }, { status: 401 });
let aiUser: { id: number; username: string } | null = null;
try { aiUser = await getUserByToken(rawToken); } catch {}
if (!aiUser) return Response.json({ error: "unauthorized" }, { status: 401 });
const ip = getClientIp(req);
if (!checkRateLimit(`ai:${aiUser.id}:${ip}`, 10, 60_000)) // ← 10/мин для authed
```

`8afd888` заявлен как `feat(api): P0 ai guard — anon 8/мин + image requires auth`, но `613fb20` фикса довёл до `auth-gate: все /ai требуют токен`. По `docs/BACKLOG.md:2` (P0 №2) и `drizzle/migrations/0017_ai_guard.sql:2` `user_id integer REFERENCES magnum_users ON DELETE SET NULL` допускает `NULL`, а `server.ts:2273` `INSERT ... VALUES (${aiUser.id}, ...)` теперь никогда не видит `null` (раньше было `aiUser?.id ?? null` и anon 8/мин). Результат: каждый `POST /magnum/api/ai {text:"ping"}` без токена теперь 401, а ledge `magnum_ai_usage` не пишет anon — аудит теряет DDoS. Фикс: ветвить `if (imageDataUrl && !aiUser) return 401 requiresAuth`, иначе `anon:ip 8/мин` + `authed ai:user 20/мин` + `ledger user_id = aiUser?.id ?? null` (schema уже NULL-дружелюбна). Без этого P0 №2 из backlog не закрыт.

### 2. `src/pages/GalleryPage.tsx:36-43` + `src/lib/galleryTokens.ts:32` — `y2k-03` дублирует `y2k-01`, а токены дублируют константу вместо импорта

```ts
// GalleryPage.tsx:36-43 (после фикса 613fb20)
const REAL_FALLBACK: Record<string, string> = {
  "y2k-01": "/magnum/images/gallery-42/42-y2k-01-800.webp", // ok
  "y2k-02": "/magnum/images/gallery-42/42-y2k-01.jpg",        // ok (jpg)
  "y2k-03": "/magnum/images/gallery-42/42-y2k-01-800.webp", // ← дубль y2k-01, 2 из 3 Y2K карточек одинаковы
};
```

Фикс закрыл коллизию `y2k→мемфис`, но оставил `y2k-03` копией `y2k-01` — в `BASE_ARTS` 7 карточек 3 Y2K показывают 2 одинаковых webp (67K), `public/images/gallery-42/` имеет только 4 уникальных. При фильтре `Y2K` сетка выглядит бедно. `MOCK_POOL:118-174` 6 карточек тоже повторяет те же 4 src. Дополнительно `src/lib/galleryTokens.ts:32 REAL_BY_STYLE_FALLBACK` дублирует `GalleryPage.tsx:30 REAL_BY_STYLE` — дрейф при следующем переименовании. Фикс: `y2k-03` → удалить или завести `42-y2k-01.jpg` crop, а `galleryTokens.ts` экспортировать единственный `REAL_BY_STYLE` и импортировать в `GalleryPage.tsx` (`import { REAL_BY_STYLE } from "@/lib/galleryTokens"`), убрать дубль.

### 3. `src/pages/GalleryPage.tsx:783-791` + `server.ts:2353-2355` — `getRealSrc` маскирует, но `ARCHIVE_42:179-391` 210 записей всё ещё жжёт bundle без виртуализации

```ts
// GalleryPage.tsx:785-789
export function getRealSrc(style: Style42, src: string): string {
  return REAL_BY_STYLE[style] ?? src; // маскирует soft-404 archive-*.jpg → реальный webp
}
function realSrcOf(a: Art42): string { return getRealSrc(a.style, a.src); }
```

210 `arch-*-###` теперь все маппятся на 4 реальных webp (модуль 4) + `getRealSrc` + `loading="lazy"` + `onError` hidden — curl 200 WARN ушёл. Но `ARCHIVE_42` 210×~6 строк = ~1260 строк в бандле (GalleryPage.tsx 2660 строк, 392KB) грузятся синхронно: без `React.lazy`/`virtualized` (tanstack-virtual) первый paint тянет 210 DOM нод + 210 GSAP ScrollTrigger инстансов (`animateCards` stagger 0.12). На мобиле LCP регресс. `STYLE_META` + `GRADIENT_PRESETS` 24 пресета не используются в рендере (мертвый код ~120 строк). Фикс: вынести `ARCHIVE_42` в `src/data/galleryArchive.ts` (code-split), рендерить виртуализацию 20 видимых + пагинация `?page=`, удалить неиспользуемые `GRADIENT_PRESETS`/`countByStyle`/`shuffleArts`/`pickRandomArts` или покрыть тестом на 404 `fs.existsSync("public"+realSrcOf(a))`.

---

## Похвала (1 конкретная)

**`8afd888` ai guard + `70cb919` PRISM/dust + `613fb20` Y2K фикс — честный трёхход без слома `tsc 0` и `3141 PASS`:**

- `drizzle/migrations/0017_ai_guard.sql` (16 строк) + `drizzle/schema.ts:288 magnumAiUsage` (10) с `REFERENCES magnum_users ON DELETE SET NULL` + 3 индекса (`user_idx`, `ip_idx`, `created_idx DESC`) — без DDL в hot path, в отличие от `magnum_chat_messages` P0 №3. `server.ts:2269 ledgerPromise` fire-and-forget + `Promise.race(300ms)` не блокирует MIMO proxy, `handleAiUsage:2344` отдаёт `last 20 + total + last24h` с `note: migrate pending` fallback.
- `70cb919` `server.ts:83` PRISM 12 неон-призм `magnum_dust` craft/разбор + `ShopPage.tsx:68` 4 rarity 42/142/420/1420 с `isValidBundleId` trim guard (`613fb20` синхрон с `server.ts:805` `/^[a-z0-9-]+$/ length<=40`) — экономика замкнута, dust не падает в localStorage, покрыто `tests/new-coverage-1607.test.ts:110` (RARITY sync + build hash).
- Фикс галереи `GalleryPage.tsx:39-43 y2k→y2k` + `getRealSrc:785` + `galleryTokens.ts:32` синхрон — 4 стиля теперь честно 200×4 (agit 119K, cyber 133K, memphis 77K, y2k 67K webp) без soft-200 HTML, при этом `tsc 0` и `bun test 3141 PASS` зелёные. Редкий пример, когда отчётный фикс не добавил регресса.

---

## Идеи для следующих правок (2)

1. **P0 — вернуть anon 8/мин для текста, оставить 401 только для image (~15 строк):** ветвление `server.ts:2237` `if (imageDataUrl && !aiUser) return 401 requiresAuth` + `checkRateLimit anon:ip 8/мин else ai:user 20/мин` + `ledger user_id = aiUser?.id ?? null` (миграция уже NULL) + тест `ai-guard.test.ts` anon text 8/мин PASS, anon image 401, `magnum_ai_usage` NULL row. Закроет BACKLOG P0 №2.

2. **P1 — галерея виртуализация + единый источник truth (0.4.1):** вынести `REAL_BY_STYLE` в `src/lib/galleryTokens.ts` единственным экспортом, удалить `REAL_FALLBACK` дубль + `y2k-03` дубль, `ARCHIVE_42` → `src/data/galleryArchive.ts` lazy + tanstack-virtual (20 видимых), удалить 24 `GRADIENT_PRESETS` неиспользуемых, добавить `tests/gallery-404.test.ts` `fs.existsSync + content-type image/webp + size>20k`. После `bunx tsc --noEmit` 0 и `bun test` 3141 — пуш `-f`.

---

*Ревьюер: Hermes 24/7 · `~/.bun/bin/bunx tsc --noEmit` **0** · `bun test` **3141 PASS** · `gallery` 8 файлов (119K/133K/77K/67K webp) 200×4 · `health` 200/200/200 · `magnum-bun active` `caddy Up ~1h` · next review +10м · `git log --since="10 minutes ago" --numstat` 778/41=819 строк (продуктивных 268/11=279) · HEAD 11ddb07*
