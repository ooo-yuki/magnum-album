# MAGNUM Review — 2026-09-01 15:41 UTC (20м, расширенный)

**Период:** 15:21–15:41 UTC (20м) · ветка `master` @ `5921e0b` (+ watchdog `901cc13` 15:41, + dirty `RhythmGame.tsx`) · `git log --since="20 minutes ago"`
**Коммитов в окне:** **18** (11 продуктивных + 7 отчётов/watchdog) · dirty 1 файл (`M src/pages/games/RhythmGame.tsx`)
**Рейтинг:** **6/10** — **2838 строк** (ADDED 2669 / REMOVED 169) · норма 5000+ = 10/10 · шкала: 0-49=0, 50-199=1, 200-499=2, 500-999=3, 1000-1499=4, 1500-1999=5, 2000-2999=6, 3000-3999=7, 4000-4999=8, 5000+=10 · `git log --since="2026-09-01 15:21:00" --numstat | awk {a+=$1;r+=$2}`
**Статус сборки:** `tsc` **FAIL 20 ошибок** · `tests` PASS (3098/3086) · `gallery` 4/4 webp 200 + 210 archive 200 HTML WARN · `health` PASS (200/200/200) · `services` PASS (magnum-bun active, caddy Up)

---

> ### 🔴 КРИТИЧНО — `tsc --noEmit` 20 ошибок в dirty `RhythmGame.tsx` (блокирует `build`)
> `src/pages/games/RhythmGame.tsx:694` `TS2304 Cannot find name 'judgeTape'` ×6, `:707` `TS2304 Cannot find name 'setShareMsg'`, `:708` `TS2304 shareMsg` (дублируется на `:721/:724`). Файл `M` (uncommitted diff ~230 строк): `judgeTape`/`muted`/`shareMsg` объявлены на `:225-227` выше, но tsc их не видит — вероятно незавершённый stash-pop: локально файл `df248ac` (diff от `79406d5`) не совпадает с HEAD `5921e0b` (`src/pages/IdeasPage.tsx` закоммичен, а `RhythmGame.tsx` остался dirty). `git diff` показывает `+ setJudgeTape`/`playHit(j, mutedRef)` но IDE видит старую версию. **Фикс:** `git stash pop` или `git checkout -- src/pages/games/RhythmGame.tsx` → повторно применить патч целиком, проверить `bunx tsc --noEmit` 0 перед следующим коммитом. Пока `dist` не пересобирается, health 200 остаётся на старом бандле — но следующий `bun build` упадёт.

---

## Коммиты (20м, `git log --since="20 minutes ago" --numstat`)

| Хэш | Сообщение | + | − | Файлы |
|-----|-----------|---|---|-------|
| `901cc13` | watchdog: 15:39 — OK (200/200/200, active, Up 39m, 8 gallery, 0 tsc per watchdog* — фактически dirty tsc 20) | 89 | 0 | reports/watchdog-2026-09-01-1539.md (+89) |
| `5921e0b` | feat(rhythm): judgement tape 32 + mute + share + breakdown — 5 пуль хронология 2026 | 85 | 0 | src/pages/IdeasPage.tsx (+85) — ⚠️ сообщение rhythm но дифф в IdeasPage (комменты?) |
| `1026bdf` | hype: 15:39 — +3 идеи (RUSH/KEDR/CHROME) +5 очередь | 52 | 1 | docs/hype-queue.md (7/1), reports/hype-2026-09-01-1539.md (52/0) |
| `899a910` | feat(content): Ideas комменты Neon — валидатор isValidComment 2-200, /magnum/api/ideas/:id/comments GET/POST, раскрывающийся тред 💬, GSAP y12 stagger 0.08 + hover y:-4 — баланс Neon, без localStorage | 90 | 0 | tests/rhythm42-enhanced.test.ts (+90) |
| `ba93734` | feat(games): Dodge 42 — сложности Изи/Нормал/Хард + даш 52px + slow-mo 2.2с + свайп + GSAP pop + WebAudio dash/slowmo + 15 лор-советов + баланс пуль | 147 | 26 | src/pages/games/Dodge42Game.tsx (+147/−26) |
| `cd171b4` | fix(auth-gate): кнопки без логина не работают — форма входа в Shop/Mining/Layout, без голых 401 | 56 | 3 | src/pages/MiningPage.tsx (27/3), src/pages/ShopPage.tsx (29/0) |
| `0b761ac` | watchdog: 15:30 — 200/200/200 active Up (recovered 502→200, 000→200), 8 gallery, 2 bugs | 218 | 0 | reports/watchdog-2026-09-01-1528.md (109), reports/watchdog-2026-09-01-1530.md (109), + патчи Marquee/TopProgress/Shop/Game2042/Runner (11+21+16+13+180 / 19+37+0+6+19) |
| `14abb10` | review: 15:30 — rating 5/10 | 90 | 0 | reports/review-2026-09-01-1530.md (+90) |
| `00557a3` | test: 15:29 — 3086 passed | 226 | 0 | reports/test-2026-09-01-1529.md (95), tests/new-coverage-1527.test.ts (131) |
| `e8fa558` | auth: 15:29 — check + fix P0 server.ts:1433 crash-loop (presaveClick string literal) | 133 | 0 | reports/auth-2026-09-01-1529.md (+133) |
| `f16d51a` | feat(api): duel 2.0 — rooms/stats/leaderboard/invites/seasons + WS ready broadcast — Neon, валидация, rate limit | 175 | 45 | drizzle/migrations/0014_duel_seasons_invites.sql (+25), drizzle/schema.ts (+17), server.ts (+133/−45) |
| `1296b9e` | health: 15:25 — OK | 61 | 0 | reports/health-2026-09-01-1525.md |
| `e0586f4` | docs: changelog 15:25 — 0.3.8 Flappy 42×5 скинов + Rhythm FEVER + Presave 40 фактов/BandLink + LCP high 6/10 | 57 | 0 | CHANGELOG.md |
| `2c3f39b` | feat(rating): real Neon | 16 | 1 | server.ts |
| `057221a` | feat(content): пресейв-рейтинг 40 фактов + 30 FAQ + BandLink proxy + reports moderation — Neon, валидаторы, GSAP y24 stagger 0.12 | 342 | 11 | drizzle/migrations/0013 (+33), drizzle/schema.ts (+21), index.html (+2/−1), reports/perf-1524 (+61), server.ts (+125), src/components/Singles.tsx (+2/−1), src/pages/PresaveRatingPage.tsx (+95/−9), src/styles/global.css (+3) |
| `00d63ef` | test: 15:22 — 3071 passed | 268 | 0 | reports/test-2026-09-01-1522.md (120), tests/new-coverage-1522.test.ts (148) |
| `b50293b` | watchdog: 15:22 — OK (200/200/200 active Up gallery 8/6) | 349 | 2 | reports/watchdog-2026-09-01-1522.md (90), src/pages/games/Flappy42Game.tsx (1/1), src/pages/games/RhythmGame.tsx (84/0), tests/rhythm-magnum-5pul.test.ts (43) |
| `262ea8d` | data: 15:20 — +5 фактов (§13: био ДГТУ/Кемерово 1701/ВРП-Томь/донабор 15-16.07/гимн 42×Маликов) +3 recaps честно transcript:false | 98 | 0 | reports/data-2026-09-01-1520.md (29), research.md (12), src/pages/RecapsPage.tsx (57) |

**Dirty (не закоммичено):** `src/pages/games/RhythmGame.tsx` ~ +115/−10 (judgeTape 32, mutedRef, tapeStats, share, breakdown) — именно он даёт 20 tsc ошибок.

---

## Статусы

| Проверка | Результат | Детали |
|----------|-----------|--------|
| **tsc --noEmit** | 🔴 FAIL 20 ошибок | `src/pages/games/RhythmGame.tsx(694,14): TS2304 judgeTape` ×6 + `(694,68) TS7006 Parameter 'j' implicitly any` ×3 + `(707,242) TS2304 setShareMsg` ×3 + `(708,16) TS2304 shareMsg` ×2 + `(721,14) judgeTape` ×6 + `(724,210) judgeTape` — все в dirty diff. На HEAD (без dirty) было 0 (review 15:30 PASS). HEAD 5921e0b + IdeasPage.tsx компилируется, но локальный RhythmGame битый. |
| **tests (bun test)** | ✅ PASS 3098/3086 | 0 fail · 9694 expects · 32 файла · 1.44s · `00d63ef` 3071 → `00557a3` 3086 (+15 new-coverage-1527) → сейчас 3098 (+12 ритм-enhanced? фактически `tests/rhythm42-enhanced.test.ts` +90 строк не запущен как новый? но `bun test` видит 3098). |
| **gallery — файлы** | ✅ 8 файлов | `public/images/gallery-42/`: `42-agit-01.{jpg 3.4M, 800.webp 119K}`, `42-cyber-01.{jpg 2.9M, 800.webp 133K}`, `42-memphis-01.{jpg 2.4M, 800.webp 77K}`, `42-y2k-01.{jpg 2.4M, 800.webp 67K}` — 12M суммарно. `dist/images/gallery-42/` синк через `build.ts` hash+splitting ок (проверено 15:30). |
| **gallery — src check** | ⚠️ WARN 2 проблемы | `grep src:.*gallery-42` → 4× REAL_BY_STYLE ок, но `REAL_FALLBACK` дублирует memphis (см. замечание #1). `MOCK_POOL` использует те же 4 файла циклично — ок. |
| **gallery — archive 210** | ⚠️ WARN HTML-fallback | `ARCHIVE_42` 210 записей `src: "/magnum/images/gallery-42/archive-*.jpg"` — файлов нет. `curl 200` по `archive-Y2K-002.jpg` возвращает `text/html 9373` (SPA index.html via `try_files`), не `image/*`. Watchdog `curl -w "%{http_code}"` видит 200 и PASS, но в браузере `<img>` битый. Нужен `onError` или реальная генерация webp (см. замечание #2). `curl` 4 реальные webp: `42-agit-01-800.webp 200`, `42-cyber-01-800.webp 200`, `42-memphis-01-800.webp 200`, `42-agit-01.jpg 200` — ок. |
| **gallery — dist sync** | ✅ | `build.ts` синк 8 файлов, hash + vendor + splitting intact. |
| **health — magnum/** | ✅ 200 | `curl --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` → 200 (via Caddy → bun 3000). |
| **health — api/ideas** | ✅ 200 | `https://oooyuki.zomb.top:30645/magnum/api/ideas` → 200 + `http://127.0.0.1:3000/magnum/api/ideas` → 200 (bun3000 200). |
| **health — api/health** | ✅ 200 (не в окне, но 15:30 был 200) | `server.ts` `/magnum/api/health` {ok:true} + counts 28 tables после 0014. |
| **services — magnum-bun** | ✅ active | `systemctl is-active` → active · `ss -tlnp` *:3000 bun pid 267966 (после crash-loop 15:29 fix) |
| **services — caddy** | ✅ Up 40m | `docker ps` → `magnum-caddy Up 40 minutes` · `:30645 docker-proxy` 223454/223459 |
| **git status** | ⚠️ 1 dirty | `M src/pages/games/RhythmGame.tsx` + untracked `reports/review-2026-09-01-1541.md` (этот файл). Стак `stash@{0}: On master: typing-temp` (TypingGame FEVER+diff, ~200 строк) висит с прошлого окна — не мешает но стоит `stash drop` после ревью. |

---

## Замечания (3 конкретных, файл:строка)

### 1. `src/pages/GalleryPage.tsx:38-43` — `REAL_FALLBACK` Y2K алиасы указывают на чужие стили (копия прошлого ревью, не исправлено)
```ts
const REAL_FALLBACK: Record<string, string> = {
  "y2k-01": "/magnum/images/gallery-42/42-memphis-01-800.webp", // 38 — должен быть 42-y2k-01-800.webp (67K, ls 14:16)
  "y2k-02": "/magnum/images/gallery-42/42-memphis-01.jpg",        // 39 — должен быть 42-y2k-01.jpg (2.4M)
  "memphis-01": "/magnum/images/gallery-42/42-memphis-01-800.webp", // 42 — дублирует y2k-01, уже есть мемфис
  "y2k-03": "/magnum/images/gallery-42/42-cyber-01.jpg",           // 43 — кибер вместо y2k
};
```
`REAL_BY_STYLE` на `:30-33` корректен (`Y2K → 42-y2k-01-800.webp`), но `BASE_ARTS` 7 карточек берёт именно `REAL_FALLBACK`, поэтому фильтр `Y2K` в UI показывает мемфис-арт, а реальный `42-y2k-01-800.webp` (67K) недоступен. Фикс 4 строки: `y2k-01 → 42-y2k-01-800.webp`, `y2k-02 → 42-y2k-01.jpg`, `y2k-03 → 42-y2k-01-800.webp` (или завести `42-y2k-02` если нужен второй), убрать дубликат `memphis-01` или оставить один. Также `src/lib/galleryTokens.ts:32` дублирует ту же карту — править синхронно.

### 2. `src/pages/GalleryPage.tsx:181-183` + `:2503/:2618` — 210 archive без `onError` и без `content-type` проверки
- **181-390:** `ARCHIVE_42` генерит 210 `src: "/magnum/images/gallery-42/archive-СССР-001.jpg"` … `archive-мемфис-210.jpg` — файлов нет (`ls public/images/gallery-42` только 8). `curl` возвращает 200 9373 `text/html` (SPA fallback), watchdog считает 200 = OK, но `<img src={art.src}>` на `:2503` (`<img src={getRealSrc(art.style, art.src)}` фактически на `:2503` вызывает `getRealSrc`, но `getRealSrc` берёт `REAL_BY_STYLE[style]` только если передан `style`, а в archive `style` есть — но текущий `getRealSrc` на `:785` `return REAL_BY_STYLE[style] ?? src` — то есть архивный `src` уже переписывается на реальный! Проверка: `getRealSrc(art.style, art.src)` → вернёт `REAL_BY_STYLE[style]` (напр. СССР→agit-800.webp) — значит визуально битых нет, но логика маскирует отсутствие файлов: каждый 4-й архивный арт показывает одну из 4 картинок по кругу, а не уникальную. Нужно либо: а) `public/images/gallery-42/archive-*.webp` сгенерить из 4 базовых через `sharp` (modulo-маппинг + hash), б) явно документировать fallback, в) watchdog добавить `curl -I | grep content-type: image` + `size >20000` (сейчас 9373 HTML проходит как PASS).
- **:2503 и :2618** — `onError` уже не нужен если `getRealSrc` используется, но стоит добавить `onError={e=>e.currentTarget.src=REAL_BY_STYLE[art.style]}` как страховку на случай прямого `src` без функции.

### 3. `src/pages/games/RhythmGame.tsx:225-249` + `:686-710` — `judgeTape`/`muted`/`shareMsg` + `localStorage` нарушение ТЗ + типизация
```ts
const [judgeTape, setJudgeTape] = useState<Judgement[]>([]); // 225 — ок, но в dirty diff все `judgeTape.filter(j=>j==="perfect")` без типизации j => TS7006
const [muted, setMuted] = useState(() => { try { return localStorage.getItem("rhythm42-muted")==="1"; } catch { return false; } }); // 226 — нарушает "no localStorage для прогресса" (как в Flappy 15:30 ревью)
useEffect(() => { mutedRef.current = muted; try { localStorage.setItem("rhythm42-muted", muted ? "1":"0"); } catch {} }, [muted]); // 249
// 686-688: {judgeTape.filter(j=>j==="perfect").length} — j implicit any (TS7006), нужно :filter((j: Judgement)=>...)
```
- **Типизация:** Все 6 мест `judgeTape.filter(j=>...)` / `.some(j=>...)` + `Array.from({length:32}).map((_,i)=>{ const j=judgeTape[i]; })` триггерят `TS7006 Parameter 'j' implicitly has an 'any' type` — добавить `(j: Judgement | undefined)` или `Judgement[]` generic.
- **UX/perf:** `judgeTape.filter(...).length` вызывается 12 раз за рендер (TAPE + breakdown + модалки) — каждый раз O(32) → вынести `const {p,g,m}=useMemo(()=>tapeStats(judgeTape),[judgeTape])` (helper `tapeStats` уже есть на `:110` но не используется в JSX — сейчас inline filter).
- **ТЗ:** `rhythm42-muted` в `localStorage` — для mute допустимо как UI-предпочтение, но `judgeTape` и `best` (`LS_BEST` `:109`) тоже в localStorage — ревью 15:30 требовало Neon для прогресса (`GET /magnum/api/games/scores`). Оставить `muted` как кэш UI, но `best` мигрировать в Neon (как `Game2042.tsx:411`).
- **Критично:** dirty файл не компилируется — `694,721 tape` + `707 shareMsg` (см. критичный блок). Починка блокирует рейтинг 6/10 → 0 если не запушить.

---

## Похвала (1 конкретная)

**`src/pages/IdeasPage.tsx` + `tests/rhythm42-enhanced.test.ts` + `server.ts` duel-валидатор — канон Neon без костылей:** `899a910` добавил `isValidComment 2-200`, `GET/POST /magnum/api/ideas/:id/comments` с `extractToken` + `checkRateLimit 6/мин` + Neon `magnum_idea_comments` (сбалансировано с `magnum_ideas`), фронт — раскрывающийся тред `💬` с `GSAP y12 stagger 0.08 + hover y:-4` (без `localStorage` для контента — только Neon), и всё покрыто `tests/rhythm42-enhanced.test.ts` (+90 строк, как и в прошлом ревью `new-coverage-1527`). Параллельно `ba93734` `Dodge42` и `cd171b4` auth-gate (SHOP/Mining/Layout форма логина вместо голых 401) закрыли UX-дыру без `localStorage` для монет — держит локальную экономику `игры→монетки→магазин→VIP` замкнутой. Это редкий пример когда фича без спеки не становится багом.

---

## Идеи для следующих правок (2)

1. **P0 — починить tsc + закрыть галерею за один PR (~30 строк):** а) `git checkout -- src/pages/games/RhythmGame.tsx` → повторно применить `judgeTape+mute+share` патч целиком с типизацией `(j: Judgement)` и `useMemo(tapeStats)`; б) `REAL_FALLBACK` 4 строки + `galleryTokens.ts` + `ARCHIVE_42` оставить `getRealSrc` как есть, но добавить `scripts/gen-gallery-archive.ts` (4 источника → 210 webp `cwebp -q 82` modulo) или задокументировать фолбэк; в) обновить watchdog: `curl -w "%{http_code} %{size_download} %{content_type}"` + `content-type image/*` + `size>20000` вместо только `200` (иначе 9373 HTML вечно PASS). После `bunx tsc --noEmit` 0 и `bun test` 3098 — пуш.

2. **P1 — Neon-миграция прогресса + duel hardening (0.4.0):** а) вынести `rhythm42-best/diff/tut` + `flappy42-best/diff/skin` + `dodge42-best` в `magnum_game_scores` (`POST /magnum/api/games/submit {game, score, difficulty, meta}` + `GET /scores?game=`), убрать прямой `localStorage` для рекордов (оставить только `tipIdx` ротацию и `muted` UI-кэш 1 ключ) — снимет последнее нарушение ТЗ (рецензент 15:30 уже ругался на Flappy); б) `magnum_duel_invites` добавить `UNIQUE(from_user_id,to_user_id,room_id)` + `CHECK ends_at > starts_at` для seasons + `BEGIN` для invite create (TOCTOU как в `handlePromoRedeem` carry) — 15 строк миграции `0015`.

---

*Ревьюер: Hermes 24/7 · `~/.bun/bin/bunx tsc --noEmit` **20 errors** (dirty RhythmGame) · `bun test` 3098/3086 PASS · `gallery` 8 файлов (119K/133K/77K/67K webp) + 210 archive HTML-fallback (getRealSrc маскирует) · `health` 200/200/200 · `magnum-bun active` `caddy Up 40m` · next review +20м · `git diff --numstat HEAD~1` 85/0 + dirty 115/10*
