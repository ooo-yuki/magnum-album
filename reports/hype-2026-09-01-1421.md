# HYPE 2026-09-01 14:21 — MAGNUM 3/10

> Хайп-генератор · бэклог 4 фичи · 3 новые идеи · Neon OK · очередь +5

## POST /magnum/api/ideas

Авторизован как `hypebot42` (id 20). 3 идеи успешно созданы в Neon `magnum_ideas`:

- id 82 `SHOP VAULT 42 — 12 скинов каталог` → 201
- id 83 `FRAME VERIFIED GOLD — рамка за пресейв БРАТ-БОТ` → 201
- id 84 `DUEL ROYALE 42 — WS лобби 2-4 + ставки + сезон` → 201

`curl -s http://localhost:3000/magnum/api/ideas | jq .ideas[-3:]` подтверждает. Всего идей в БД: 66.

---

## Новые идеи (3)

| # | идея | приоритет | монетки | спека (3 строки) |
|---|------|-----------|---------|-------------------|
| 1 | **SHOP VAULT 42 — 12 скинов каталог** | P0 🛒 | 42 common / 142 uncommon / 420 rare / 1420 epic | Полный каталог 12 скинов: 3 common 42 (42-white, neophyte-gray, fence-minimal) · 3 uncommon 142 (clay-73-brown, tusa-teal, kemerovo-coal) · 3 rare 420 (meduza-holo, vpn-neon, nova-80-purple) · 3 epic 1420 (gold-42-conic, diamond-fence-black, rgb-legend-42). Покупка `POST /magnum/api/shop/cosmetic/buy` за `magnum-coins`, хранение `magnum_cosmetics` + `magnum_shop_inventory`, экипировка `magnum-cosmetics-equipped` LS + `storage` sync. |
| 2 | **FRAME VERIFIED GOLD — рамка за пресейв БРАТ-БОТ** | P0 🖼️ | бесплатно (требует `verified=1`) | Эксклюзивная рамка профиля за проверку пресейва через `POST /magnum/api/ai` → `mimo-v2.5` vision. Эвристика: содержит «засчитан»/«легенда» без «не вижу» → `INSERT magnum_frames(user_id, verified)`. LS `magnum-frame-verified=1` + `frame-date` ISO, CSS `conic-gradient gold spin 3s` + `box-shadow 0 0 16 gold` + `EquippedFrame` glow. |
| 3 | **DUEL ROYALE 42 — WS лобби 2-4 + ставки + сезон** | P1 ⚔️ | wager 0/42/142/420, win `+wager*2` +42 ELO, lose −12, draw возврат | WebSocket лобби 2-4 игрока на `Bun.serve ws /magnum/api/ws`: `lobby:create` → код 4 символа `ABCD` → `join` → `ready` → сервер стартует `10с` дуэль. Анти-чит CPS>20 → `suspect:true` + бан раунда, `cps=clicks/10`, `>150 clicks` флаг. `ws:online` счётчик + heartbeat 25с + реконнект + fallback hot-seat. Сезон 7дн `magnum_leaderboard(game=duel42)` топ-3 `arena-crown-42` `conic-gold`. |

### Детализация

#### 1. SHOP VAULT 42 — 12 скинов каталог
- **Описание:** Закрывает backlog #3 полностью — 12 скинов за монетки. Каталог `COSMETICS_CATALOG` 12: common 42 — `42-white` 🤍 `linear-gradient #fff,#e5e5e5`, `neophyte-gray` 🩶, `fence-minimal` 🏗️; uncommon 142 — `clay-73-brown` 🧱 `#8b4513→#d2691e`, `tusa-teal` 🪼 `#14b8a6→#06b6d4`, `kemerovo-coal` ⛏️ `#1f2937→#4b5563`; rare 420 — `meduza-holo` 🪼 `conic-gradient`, `vpn-neon` 📡 `neon #0ff`, `nova-80-purple` 💜 `#7c3aed→#ec4899`; epic 1420 — `gold-42-conic` 👑 `conic-gold spin`, `diamond-fence-black` 🏴 `#000→#333 + gloss`, `rgb-legend-42` 🌈 `rgb-shift`.
  Покупка: `getCoins()>=price` guard → `addCoins(-price)` atomic + `POST /magnum/api/shop/cosmetic/buy` Neon, инвентарь `magnum_cosmetics[]` + `magnum_shop_inventory`. Экипировка: `equip` → один `equipped=true` per `slot` (frame/banner/title), `EquippedFrame` glow `box-shadow 0 0 24 gold`. Пустое состояние: «Собери vault, братуха!» + CTA в казино.
- **Цена:** `common 42` · `uncommon 142` · `rare 420` · `epic 1420` (строго по спеке). Старт-баланс 1000 хватает на 3 rare или 23 common. Скидка 42 за эко-бейдж Легенда 42 (кросс).
- **GSAP:** `gsap.context` `ShopGrid` stagger `y:20→0 0.08 ease power2.out`, `SkinCard` hover `scale 1.02 + glow shadow`, purchase `confetti 80 canvas` + `scale 0→1 spring`, `rarity badge` epic `shimmer spin 3s linear infinite`, `prefers-reduced-motion` → `animation:none` + `transform:none`.

#### 2. FRAME VERIFIED GOLD — рамка за пресейв БРАТ-БОТ vision
- **Описание:** Закрывает backlog #4 — эксклюзивная рамка профиля за проверку пресейва. Флоу: юзер жмёт «Проверить пресейв» → `input[type=file]` скрин Bandlink пресейва `https://music.thefence.me/psmagnum` → `POST /magnum/api/ai` `{image, prompt: "это пресейв MAGNUM?"}` → `mimo-v2.5` vision. Эвристика ответа: `text.includes("засчитан")||includes("легенда") && !includes("не вижу")` → `verified=true` else `false`. Успех: `INSERT magnum_frames(user_id, verified)` + `POST /magnum/api/frame/verify` + LS `magnum-frame-verified=1` + `magnum-frame-date=ISO`. Рамка рендерится в `ProfileBadge` + `Leaderboard` аватар: `conic-gradient(from 0deg,#ffd700,#ff8c00,#ffd700) spin 3s` + `border 3px gold` + `box-shadow 0 0 16 rgba(255,215,0,.7)` + `filter glow`. Без verified — серая рамка + CTA «пройди проверку».
- **Цена:** Бесплатно, требует `verified=1` (нельзя купить). Повторная проверка 1×/сутки guard `magnum-frame-last-check:date` + rateLimit 8/мин IP. Кросс: verified → `+42 coins` + `+10%` скидка в `SHOP VAULT`, бейдж в `PresaveRatingPage` топ-20.
- **GSAP:** `FrameReveal` `scale 0→1 0.6 spring ease back.out(1.7)` + `rotation 180→0 0.8`, `gold glow` pulse `box-shadow 0 0 16→32 2s infinite alternate`, `spin` `rotate 0→360 3s linear infinite` на conic, stagger аватарок в рейтинге `y:12→0 0.06`, `prefers-reduced-motion` → `opacity 0→1 0.2` без spin.

#### 3. DUEL ROYALE 42 — WS лобби 2-4 + ставки + сезон
- **Описание:** Апгрейд backlog #1 — полноценная WS арена 2-4 игрока вместо hot-seat. Хост «Создать арену» → `ws.send({type:"lobby:create", wager})` → сервер `room:ABCD` + `ws.subscribe('room:ABCD')` + `roomPublic()` → до 3 братух `join ABCD` → `ready` → авто-старт `10с` дуэль. Ставка `0/42/142/420` из `magnum-coins` (`getCoins()>=wager` guard, `addCoins(-wager)` atomic перед WS). Игра: полноэкранный кликер `10с`, счётчик, прогресс `clicks/150` burst `+1` + `navigator.vibrate(20)`, GSAP `scale 1→1.15` на клик. Результат: сервер `broadcast scores` → `winner ? +wager*2 +42 ELO : -12`, draw возврат. Топ-3 сезона 7дн `arena-crown-42` `conic-gold + crown 👑` + `magnum_leaderboard(game=duel42)` Neon + `magnum-duel-history` LS 20 записей.
- **Цена:** `0` френдли или `42/142/420`; победитель `+wager*2` + `+42 ELO`, лузер `−12 ELO`, draw возврат ставок. Топ-3 сезона `+1420` + лимитка `arena-crown-42` + `conic-gold` рамка на неделю. Anti-economy: `wager 420` топ-дроп, требует фарм `magunum-coins` через казино/квиз.
- **GSAP:** `gsap.context` `LobbyGrid` stagger `y:24→0 0.12 power2.out`, `DuelStage` `shake x:±6 0.08s` на каждый клик + `scale 1→1.15 0.1`, `Transit 3-2-1` `scale 2→1 + opacity 1→0`, конфетти 160 частиц canvas `particle(x,y,vx,vy,life)` + `requestAnimationFrame`, `Leaderboard` `stagger y:10 0.05`, `prefers-reduced-motion` → `animation:none` + `transform:none`.

---

## Интеграция

- Файлы: `src/lib/cosmetics.ts` (CATALOG 12) + `src/pages/ShopVaultPage.tsx` + `src/components/SkinCard.tsx` + `src/components/EquippedFrame.tsx`, `src/lib/frameVerify.ts` + `src/components/FrameGold.tsx` + `server.ts POST /magnum/api/frame/verify`, `src/pages/games/DuelRoyale.tsx` + `src/lib/ws.ts` + `server.ts:websocket lobby 2-4`
- LS: `magnum_cosmetics/inventory/equipped`, `magnum-frame-verified/frame-date`, `magnum-duel-history/leaderboard`, `magnum-coins`, `magnum-arena-season`
- Neon: `magnum_cosmetics`, `magnum_shop_inventory`, `magnum_frames`, `magnum_leaderboard(game=duel42)`, `magnum_ideas` (ids 82-84)
- Кросс: покупка скина → `EquippedFrame` glow в дуэли + эко, verified frame → скидка 42 в shop + бейдж в рейтинге, победа дуэли → `StreakCalendar` + `RecapQuest` + `PresaveRatingPage`

> Следующий тик 14:36 — ротация shop vault + проверка голосов `magnum_ideas` (ids 82-84) + season reset.

