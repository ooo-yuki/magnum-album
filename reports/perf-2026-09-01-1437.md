# Perf — 2026-09-01 14:37 UTC

> Оптимизатор MAGNUM 10/10 · cron каждые 20 мин · `perf: 14:37 — stale dist cleanup + bundle health`

## Задача

Сайт не должен падать: `du -sh` + `ls -lh dist/*.js | sort -hr` → тяжёлый чанк >50KB → `lazy`/`vendor split`/`800px webp`/`preconnect`/`sitemap` → 1 оптимизация → `curl magnum/ 200` + `api/ideas 200` + `magnum-bun active` → `tsc 0` + `Bun.build` + `cp -r dist/* /srv/magnum/` → отчёт + `git push`.

## Before / After

| Метрика | Before | After | Δ |
|---------|--------|-------|---|
| **dist total** | **47 MB** (48 425 363 B, 18× `main-*.js` stale) | **38 MB** (38 551 015 B, 1× `main-*.js`) | **−9 MB (−19%)** |
| **/srv/magnum/** | 47 MB (дубль dist, 18 stale) | **38 MB** | −9 MB |
| **main JS** (`dist/main-*.js`) | `main-hhq1p997.js` **526 KB** (538 433 B), gzip 148 KB (151 095 B) | `main-hhq1p997.js` **526 KB** (тот же, build idempotent) | — (стабилен после lazy 9 pages: 1037→510→526 KB) |
| **main CSS** | `main-93jc2k31.css` **206 KB**, gzip 35 KB | `main-93jc2k31.css` **206 KB** | — |
| **chunks JS** | 193 `chunk-*.js` | 193 | — |
| **chunks CSS** | 29 `chunk-*.css` | 29 | — |
| **heavy chunks >50KB** | `chunk-w1a9cx82.js` 257 KB, `chunk-09r6t25v.js` 256 KB (Recaps), `chunk-2hj5pnc7`/`m20th44f` 253 KB, `chunk-2y8mmf1t` 250 KB, `chunk-0d9gx3v3` 160 KB, `chunk-mxam54ne` 158 KB — все lazy pages/games, не в main | те же, vendor split уже: `vendor-kzn5m822.js` 1.1 KB + `chunk-7kmngp5b` 69 KB (gsap) + `chunk-89k7mbyj` 52 KB (@gsap/react) | — |
| **stale files** | 17 `main-*.js` + 4 `main-*.css` мусора (9 MB) | **0** | −17 js, −4 css |
| **tsc** | 0 | 0 | OK |
| **build time** | — | ~1.96s (Bun.build ESM, splitting, minify, 203/250 ok, 58 outputs) | — |

## Что оптимизировал: `stale dist cleanup + bundle health` (1 тяжёлый чанк >50KB → lazy уже, теперь гигиена dist)

**Проблема:** `dist/` копит `main-*.js` каждый билд (новый hash, старые не удалялись). К 14:35 накопилось **18 stale `main-*.js`** (от `main-dngxgjck.js` 510K до `main-hhq1p997.js` 526K) + 4 stale `main-*.css` = **9 MB мусора**. `du 47 MB` vs реальный активный bundle ~38 MB. Та же картина в `/srv/magnum/` (деплой `cp -r dist/*` копировал мусор).

**Решение:**
- Выбран активный `main-hhq1p997.js` + `main-93jc2k31.css` (последний `ls -t`, `Bun.build` idempotent — hash не менялся, gzip 151 KB / 35 KB).
- Удалены 17 stale `main-*.js` + 4 stale `main-*.css` в `dist/` и синхронно в `/srv/magnum/` → `47 MB → 38 MB (−19%)`.
- Заложен паттерн: после `Bun.build` чистить stale (как в `perf: 14:11`).

**Heavy chunk анализ (>50KB):**
- `main-hhq1p997.js` 526 KB — уже −51% от 1037 KB благодаря `lazy 9 heavy pages` (Gallery 441KB, Recaps 277KB, Discography 36KB, Shop 29KB + Eco/Mining/etc вынесены из main в `chunk-*` 250–257 KB). Дальше резать — только новыми lazy (например, `chunk-w1a9cx82.js` 257 KB — это текущий лидер, но уже lazy).
- `vendor split` уже: `vendor-kzn5m822.js` 1.1 KB (react/gsap/router) + `chunk-7kmngp5b.js` 69 KB (gsap) + `chunk-89k7mbyj.js` 52 KB — в `src/vendor.ts` (`import "react"`, `gsap`, `ScrollTrigger`, `@gsap/react`).

**Доп. проверка perf-активов (п.3, не трогал — все OK):**
- `sitemap.xml` **32 urls** (`public/` + `dist/` synced, `lastmod 2026-09-01`, 3.7 KB) ✅
- `robots.txt` (`Allow: /` + `Sitemap: https://5opka.ru/magnum/sitemap.xml`) ✅
- `OG` 10 тегов + `json-ld MusicAlbum` ✅
- `preconnect` 6 (fonts + music.yandex/spotify/fence) + `dns-prefetch` 3 + `modulepreload` vendor + `prefetch` fence ✅
- `lazy images` 12 (`loading="lazy"` + `decoding="async"` + `width/height`) + `800px webp` для `gallery-42` ✅
- `prefers-reduced-motion` (`global.css` `animation:none` + `gsap.context` ветки) ✅
- `gsap.context` cleanup — 405 вхождений, `ctx.revert()` ✅

## Проверки (п.4)

| Проверка | Команда | Результат |
|----------|---------|-----------|
| `magnum/` | `curl -s -o /dev/null -w "%{http_code}" http://localhost:30646/magnum/` | **200** (0.003s) |
| `api/ideas` | `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/magnum/api/ideas` | **200** |
| `api/ideas` (30646) | `curl http://localhost:30646/magnum/api/ideas` | **200** |
| `magnum-bun` | `systemctl is-active magnum-bun` | **active** |
| `tsc` | `bun x tsc --noEmit` | **0** |
| `main fetch` | `curl http://localhost:30646/magnum/main-hhq1p997.js` | 538 433 B, gzip 151 KB |
| Нет restart | bun active, health 200 | не требуется |

## Билд (п.5)

```
tsc 0 → Bun.build ESM splitting minify target browser → dist/main-hhq1p997.js 526 KB + vendor-kzn5m822.js 1.1 KB + 193 chunks JS + 29 chunks CSS + sitemap 32 urls, minify 203/250 ok → stale cleanup 17 js + 4 css → cp -r dist/* /srv/magnum/ → health 200
```

- `build.ts` вывод: `58 outputs, Summary: 2 entries, 55 chunks, 1 vendor`, `sitemap: 32 urls synced`, `Deploi: cp -r dist/* /srv/magnum`
- `ls -lh dist/*.js | sort -k5 -hr | head -10` → `main-hhq1p997.js` 526 KB на вершине, далее `chunk-w1a9cx82.js` 257 KB, `chunk-09r6t25v.js` 256 KB (Recaps), `chunk-2hj5pnc7.js` 253 KB и т.д. — все lazy, не в main

## Логи

- `/tmp/opt.log` — `du -sh dist/` + `ls -lh dist/*.js | sort -k5 -hr | head -10` appended (before 47M + after 38M)

## Следующий шаг

- Держать stale cleanup после каждого `Bun.build` (крон 20 мин). Следующий тяжёлый чанк для lazy — `chunk-w1a9cx82.js` 257 KB: определить источник (игры/страницы), вынести в `React.lazy` если eager.
