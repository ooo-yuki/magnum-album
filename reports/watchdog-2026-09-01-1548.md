# Watchdog MAGNUM — 2026-09-01 15:48 UTC

> Авто-проверка каждые 5 минут. Хост: vds-crbanana-3924 · ветка: master · commit: e3b3325

## Health

| # | Проверка | Ожидаемо | Факт | Статус |
|---|----------|----------|------|--------|
| 1 | `curl --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | **200** `9439 B / 0.04s` `<!DOCTYPE html> MAGNUM — 5opka` | ✅ |
| 2 | `curl https://oooyuki.zomb.top:30645/magnum/api/ideas` (TLS через Caddy) | 200 JSON `{"ideas":[...]}` | **200** `95 ideas`, valid JSON, первая `id:65 gen3 — 130 pereskazov` | ✅ |
| 3 | `curl http://127.0.0.1:3000/magnum/api/ideas` (прямо Bun) | 200 JSON | **200** `95 ideas`, `0.23s` | ✅ |
| 4 | `systemctl is-active magnum-bun` | active | **active** `running since 15:43:34 UTC (4m35s) PID 277619 bun run server.ts Mem 24.8M` | ✅ |
| 5 | `docker ps \| grep magnum-caddy` | Up | **Up 47m** `magnum-caddy` `0.0.0.0:30645-30646->30645-30646/tcp` `omniroute-caddy:latest` | ✅ |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов (спека watchdog 1/10) | **8 файлов** `42-agit-01.{jpg, -800.webp} 42-cyber-01.{jpg, -800.webp} 42-memphis-01.{jpg, -800.webp} 42-y2k-01.{jpg, -800.webp}` `12M total` `dist/images/gallery-42/` — тоже 8, синхрон OK | ⚠️ — файлов 8 вместо 6 (4×jpg + 4×webp), это норма после 1550-теста; спека устарела |
| 7 | `git status --short` | clean | **dirty** `M src/pages/games/RunnerGame.tsx` `?? public/images/5opka-runner-800.webp` `?? tests/new-coverage-1550.test.ts` | ⚠️ |
| 8 | Caddy `/srv/magnum` + `try_files` SPA fallback | index.html отдаётся | `dist/index.html` существует, `/magnum/` → 200, `/magnum/api/*` → reverse_proxy 172.18.0.1:3000 | ✅ |

**Итог:** 6/8 — **PASS** (6 ✅, 2 ⚠️ без рестарта). Сервисы живы, TLS и API отвечают 200.

## Коды и latency

- TLS `GET /magnum/` → `200` `0.040s` `size 9439`
- TLS `GET /magnum/api/ideas` → `200` `0.237s` `Content-Type: application/json` `95 ideas`
- Local `GET http://127.0.0.1:3000/magnum/api/ideas` → `200` `0.231s` `95 ideas` — паритет с TLS, Caddy reverse_proxy OK
- `systemctl status magnum-bun` → `Active: active (running)` `Main PID 277619`
- `docker ps magnum-caddy` → `Up 47 minutes` `Ports 30645-30646`
- `tsc --noEmit` → `0` ошибок
- `git log --oneline -3` → `e3b3325 feat(games): Timeline2026` `1c4584f review: 15:44 — 3/10` `b3744a0 docs: changelog 15:43`

## Рестарты

- Критерий рестарта: любой `http_code != 200` или `systemctl != active` или `docker != Up`.
- Факт: все три http 200, systemctl active, docker Up — **рестарт не требуется**.
- Действий: `systemctl restart magnum-bun` — **не выполнялся**, `docker compose up -d` — **не выполнялся**.
- Повторная проверка после (не)рестарта — не нужна, первичные пробы успешны.

## Найденные баги (2)

### BUG-1 — `public/images/5opka-runner-800.webp` не попал в `dist/`

- **Файл:строка:** `build.ts:1578` (`await $\`cp -r ./public/images ./dist/images\``) + `src/pages/games/RunnerGame.tsx:225` (`img.src = "/magnum/images/5opka-runner-800.webp"`) + артефакт `dist/images/5opka-runner-800.webp` отсутствует
- **Суть:** `public/images/5opka-runner-800.webp` (34K, создан 15:47) существует в `public/`, есть `public/images/5opka-runner.png` (1.4M fallback). В `dist/images/` только `5opka-runner.png` (проверено `ls -lh dist/images/5opka-runner*` — только png, `dist/images/gallery-42` синхрон, а `dist/images/5opka-runner-800.webp` отсутствует). Последний `cp -r` был в build 15:13, до появления webp. Caddy отдаёт статику из `/srv/magnum` (= копия `dist`), значит `GET /magnum/images/5opka-runner-800.webp` через TLS отдаст 404, сработает `onerror → fallback png` в RunnerGame, но LCP слетает с 34KB на 1.4MB (97.6% регресс). Новый тест `tests/new-coverage-1550.test.ts` не покрывает этот ассет. `git status` показывает `?? public/images/5opka-runner-800.webp` — файл не закоммичен/не скопирован в dist.
- **Риск:** 404 в галерее спрайта, лишние 1.4MB на каждом хите, `gallery-404.test.ts` может начать падать если проверят runner.
- **Быстрый фикс:** `cp public/images/5opka-runner-800.webp dist/images/5opka-runner-800.webp && rsync -a dist/ /srv/magnum/` (или пересобрать `bun run build.ts`), затем `git add public/images/5opka-runner-800.webp`.

### BUG-2 — `src/pages/GalleryPage.tsx:39,43` — неверный `REAL_FALLBACK` для Y2K

- **Файл:строка:** `src/pages/GalleryPage.tsx:39` ` "y2k-01": "/magnum/images/gallery-42/42-memphis-01-800.webp"` и `src/pages/GalleryPage.tsx:43` ` "y2k-03": "/magnum/images/gallery-42/42-cyber-01.jpg"` (контекст `REAL_FALLBACK:36-43`, `REAL_BY_STYLE:30-34` корректен)
- **Суть:** `REAL_BY_STYLE["Y2K"]` правильно → `42-y2k-01-800.webp`, но `REAL_FALLBACK` для `y2k-01`/`y2k-02` указывает на `42-memphis-01-*`, а `y2k-03` — на `cyber`. `BASE_ARTS` использует `REAL_FALLBACK["y2k-*"]`, поэтому карточки Y2K рендерят мемфис/кибер арты, ломая канон 4 стилей. Копипаст-ошибка, не ловится `gallery-real.test.ts` (проверяет только `REAL_BY_STYLE`, не `FALLBACK`). Дополнительно `ARCHIVE_42:181-201` генерирует 16 несуществующих `archive-*.jpg` — спас только `getRealSrc()` (строка 785), без него — массовый 404.
- **Риск:** визуальный баг галереи, неверный фильтр Y2K, путаница для игроков 42.
- **Быстрый фикс:** `REAL_FALLBACK["y2k-01"] = "/magnum/images/gallery-42/42-y2k-01-800.webp"`; `["y2k-02"] = "/magnum/images/gallery-42/42-y2k-01.jpg"`; `["y2k-03"] = "/magnum/images/gallery-42/42-y2k-01-800.webp"` (или удалить y2k-03 если лишний), прогнать `vitest gallery-real`.

## Идея починки (1)

**Инкрементальный `syncPublicAssets` + вотчер вместо полного `cp -r` + честный health `/magnum/api/health`.**

Сейчас `build.ts:syncPublicAssets()` копирует всю `public/images` только при `bun run build.ts`. Любой арт, добавленный после билда (как `5opka-runner-800.webp` в 15:47), остаётся только в `public/` и пропадает из `dist/` и `/srv/magnum` до следующей полной сборки — отсюда BUG-1. Предложение: (а) вынести sync в отдельный `scripts/sync-images.ts` c `rsync -a --checksum public/images/ dist/images/` + `rsync -a dist/images/ /srv/magnum/images/` и дергать его post-build и по `chokidar` вотчеру в `server.ts` dev-режиме; (б) добавить в `server.ts` маршрут `GET /magnum/api/health` который проверяет: `Bun.file(dist/index.html).exists()`, `Bun.file(dist/images/5opka-runner-800.webp).size() >5KB`, `count dist/images/gallery-42 == 8`, `Neon SELECT 1`, и отдаёт `{status:"ok", images:8, runner:true, db:true}` — watchdog будет бить в него вместо трёх curl, быстрее и честно ловит рассинхрон public/dist. (в) обновить спека watchdog с `6 файлов` на `8 файлов (4 webp + 4 jpg)` и добавить проверку `dist/images/5opka-runner-800.webp` в `gallery-404.test.ts`. Это закрывает оба бага одним механизмом и убирает ручной `cp` после каждого нового арта.

---
*Watchdog 1/10 · сгенерировано `bun` cron · следующий запуск +5 мин · `reports/watchdog-2026-09-01-1548.md`*
