# Watchdog 1/10 — 2026-09-01 14:59 UTC

## Health

| # | Проверка | Ожидаемо | Факт | Статус |
|---|----------|----------|------|--------|
| 1 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | 200 | ✅ |
| 2 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | 200 JSON (`ideas:[{id:65 votes:142}]`) | ✅ |
| 3 | `curl -s http://127.0.0.1:3000/magnum/api/ideas` | 200 | 200 (идентичный JSON) | ✅ |
| 4 | `systemctl is-active magnum-bun` | active | active (running since 14:57:51, PID 220086, 13.0M, up 9s) | ✅ |
| 5 | `docker ps \| grep magnum-caddy` | Up | Up (431e292c2c6a omniroute-caddy, Up 2 hours, 0.0.0.0:30645-30646) | ✅ |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | 8 файлов (4× jpg + 4× 800.webp — agit/cyber/memphis/y2k, 12 MB) | ⚠️ +2 |
| 7 | `git status --short` | clean | clean (no changes) | ✅ |
| 8 | `bun tsc --noEmit` | 0 errors | 0 errors | ✅ |
| 9 | `bun test` | pass | 3017 pass / 0 fail (25 files, 9228 expects, 1.91s) | ✅ |
| 10 | `curl -s http://127.0.0.1:3000/magnum/api/health` | ok:true | ok:true (`users:23 coins:22 ideas:75 ... uptime:9.3s`) — до рестарта был `ok:false db error code 42P01` | ✅ (после рестарта) |

**Итоговый статус: OK — все критичные сервисы UP, рестарты отработали, повторная проверка 200.**

## Коды ответов

- Внешний HTTPS `/magnum/` → `200` (HTML `MAGNUM — 5opka | Новый альбом 2026`, `dist/index.html` 8446 B)
- Внешний HTTPS `/magnum/api/ideas` → `200` (JSON, top idea #65 Freakland gen3 votes:142)
- Локальный HTTP `:3000/magnum/api/ideas` → `200` (идентичный JSON, связность Bun↔Caddy OK)
- Локальный `/magnum/api/health` → `200 ok:true` (14 counts), но в 14:57:47 был `500 ok:false db error` (см. рестарты)

## Рестарты

| Действие | Время | Причина | Результат |
|----------|-------|---------|-----------|
| `systemctl restart magnum-bun` | 14:57:51 UTC (systemd log) | health `500 {ok:false, error:"db error"}` + `code:42P01 parserOpenTable` в 14:57:47 — один из 14 `SELECT count(*)` в `handleHealth()` упал (отсутствующая таблица) | ✅ `active (running)` PID 220086, `/magnum/api/health` теперь `ok:true` counts 14 таблиц, uptime сброшен 9s |
| `docker compose up -d` (/opt/magnum-caddy) | — | `magnum-caddy` уже `Up 2 hours` (StartedAt 2026-09-01T13:26:13Z), `Caddyfile reverse_proxy 172.18.0.1:3000` + `:30645 https h1/h2/h3` здоров — рестарт не требовался | ⏭️ пропущен (Up) |
| Повторная проверка | 14:58:00 UTC | после рестарта | `200/200/200` + `health ok:true` — подтверждено |

> Авто-рестарт сработал между 14:57:47 (db error) и 14:57:51 (Started) — watchdog следующего цикла застал уже восстановленный сервис. Следующий цикл должен считать это нормальным восстановлением, но залогировать причину `42P01`.

## Галерея детально

```
42-agit-01-800.webp  119K
42-agit-01.jpg       3.4M
42-cyber-01-800.webp 133K
42-cyber-01.jpg      2.9M
42-memphis-01-800.webp 77K
42-memphis-01.jpg    2.4M
42-y2k-01-800.webp   67K
42-y2k-01.jpg        2.4M
```
Итого 8 файлов — чек-лист ожидает 6, но фактически 4 стиля × 2 формата (jpg + 800.webp) = 8 — корректно. 404 нет; `REAL_BY_STYLE`/`REAL_FALLBACK` указывают на существующие файлы. Предыдущий WARN про 6→8 остаётся открытым (чек-лист устарел).

## Найденные баги (файл:строка)

### BUG-1 — Перепутанные маппинги `REAL_FALLBACK` Y2K → memphis/cyber (визуальный баг)

- **Файл:** `src/pages/GalleryPage.tsx:39-43`
  ```ts
  const REAL_FALLBACK: Record<string, string> = {
    "y2k-01": "/magnum/images/gallery-42/42-memphis-01-800.webp", // ← должен быть y2k
    "y2k-02": "/magnum/images/gallery-42/42-memphis-01.jpg",      // ← должен быть y2k
    "y2k-03": "/magnum/images/gallery-42/42-cyber-01.jpg",        // ← id y2k, файл cyber
  };
  ```
- **Почему баг:** `REAL_BY_STYLE["Y2K"]` корректно `42-y2k-01-800.webp`, но `REAL_FALLBACK` для тех же Y2K-карточек (`BASE_ARTS` id y2k-01/02/03 — Bling-бабл/Флипфон/Неон-бейдж) отдаёт memphis/cyber файлы. Фильтр «Y2K» показывает мемфис-арты; ревьюер `gallery` (проверка style→src) словит несоответствие. Файлы `42-y2k-01.*` существуют — надо просто заменить 3 строки.
- **Строки:** `src/pages/GalleryPage.tsx:39`, `:40`, `:43` (и `BASE_ARTS` ниже использует их).

### BUG-2 — `handleHealth()` падает целиком из-за одной отсутствующей таблицы (`42P01`)

- **Файл:** `server.ts:1518-1535` (`handleHealth` Promise.all 14 SELECT) + `server.ts:1555-1557` catch
  ```ts
  const [users, coins, ... duels] = await Promise.all([
    sql`SELECT count(*)::int as c FROM magnum_duel_history`, // ← если таблица не смигрирована → throw 42P01
  ]);
  // catch → return { ok:false, error:"db error", status:500 }
  ```
- **Лог 14:57:47:** `code: "42P01" routine: "parserOpenTable" file: "parse_relation.c" line:"1498" table: undefined` — один `SELECT` на несуществующую таблицу валит весь health в 500, хотя `magnum_users/coins/ideas` живы. Сейчас после рестарта `duel_history` уже существует (health 14:58 вернул `duels:0`), но паттерн хрупкий: любая новая таблица без миграции = health 500 → watchdog рестарт → ложная тревога.
- **Риск:** P1 — health должен быть resilient; ревьюер health проверяет `ok:true`.

## Идея починки (1)

**P1 — За один проход ≤60 строк: галерея + health-resilience**

1. **Галерея:** `src/pages/GalleryPage.tsx:39-43` заменить на:
   ```ts
   "y2k-01": "/magnum/images/gallery-42/42-y2k-01-800.webp",
   "y2k-02": "/magnum/images/gallery-42/42-y2k-01.jpg",
   "y2k-03": "/magnum/images/gallery-42/42-y2k-01.jpg", // или добавить 42-y2k-02/03 если нужен разный арт
   ```
   Обновить чек-пункт 6 с «6 файлов» → «8 файлов (4× jpg + 4× 800.webp)» чтобы снять ложный WARN. Добавить тест `gallery-real.test.ts: style→filename includes(styleKey)` — ловит перепутанные маппинги.

2. **Health-resilience:** `server.ts:1518-1535` обернуть каждый `sql` в `catch` → `0`:
   ```ts
   const safe = (q: Promise<any>) => q.catch(()=>[{c:0}]);
   const [users, ..., duels] = await Promise.all([
     safe(sql`SELECT count(*)::int as c FROM magnum_users`),
     ...
     safe(sql`SELECT count(*)::int as c FROM magnum_duel_history`),
   ]);
   ```
   И в `catch` логировать `e.code/table` отдельно, возвращать `counts` с частичными нулями + `warnings:[missingTable]`, а не `500`. Health останется `ok:true` при живой БД, рестарты только при реальном DB down.

---

*Watchdog cron 1/10 • 2026-09-01 14:59 UTC • /root/magnum-album • auto-push*
