# Watchdog MAGNUM — 2026-09-01 15:22 UTC

> Авто-проверка каждые 5 мин · задание 1/10 · `/root/magnum-album`

## Health

| # | Проверка | Ожидается | Факт | Статус |
|---|----------|-----------|------|--------|
| 1 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | `200` · 9238 B · title MAGNUM — 5opka | ✅ |
| 2 | `curl -sk ... https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | `200` · 39195 B · ideas[] | ✅ |
| 3 | `curl -s http://127.0.0.1:3000/magnum/api/ideas` | 200 | `200` · 39195 B · identical to #2 | ✅ |
| 4 | `systemctl is-active magnum-bun` | active | `active` · since 15:12:41 UTC · PID 240034 · 64.6M · bun run server.ts | ✅ |
| 5 | `docker ps \| grep magnum-caddy` | Up | `Up 22 minutes` · omniroute-caddy:latest · 0.0.0.0:30645-30646->30645-30646 | ✅ |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов (спека) | `8` файлов · `42-agit-01-800.webp,42-agit-01.jpg,42-cyber-01-800.webp,42-cyber-01.jpg,42-memphis-01-800.webp,42-memphis-01.jpg,42-y2k-01-800.webp,42-y2k-01.jpg` · 12M total | ⚠️ (8 вместо 6 — 4 стиля × jpg+webp) |
| 7 | `git status --short` | clean | `M src/pages/games/RhythmGame.tsx` + untracked `reports/data-2026-09-01-1520.md` | ⚠️ |
| 8 | Caddy /srv sync | dist ≡ /srv/magnum | `diff srv vs public = 0` · /srv/magnum/images/gallery-42 synced (8 файлов) | ✅ |

**Итог:** 6/8 ✅, 2/8 ⚠️ (не критично, рестарт не требуется).

## Коды ответов

- `GET /magnum/` via Caddy TLS (30645): **200** (curl -sk --resolve …)
- `GET /magnum/api/ideas` via Caddy TLS: **200** (JSON, 39195 B)
- `GET /magnum/api/ideas` direct Bun :3000: **200** (JSON, 39195 B)
- Все три — 200, тела идентичны (diff 0 B).

## Рестарты

| Сервис | Действие | Причина | Результат повторной проверки |
|--------|----------|---------|------------------------------|
| magnum-bun | не перезапускался | is-active = active, /api 200 | — |
| magnum-caddy (docker compose up -d) | не перезапускался | Up 20 minutes, /magnum 200 | — |

> Правило: рестарт только если код ≠200 или не active/Up. Все условия выполнены — рестарт пропущен.

## Найденные баги (файл:строка)

### BUG-1 — Перепутан REAL_FALLBACK для Y2K (копипаста мемфиса)

- **Файл:** `src/pages/GalleryPage.tsx:39` (и дубль `src/lib/galleryTokens.ts:42`)
- **Код:**
  ```ts
  // src/pages/GalleryPage.tsx:36-43
  const REAL_FALLBACK: Record<string, string> = {
    "ussr-01": "/magnum/images/gallery-42/42-agit-01-800.webp",
    "ussr-02": "/magnum/images/gallery-42/42-agit-01.jpg",
    "y2k-01": "/magnum/images/gallery-42/42-memphis-01-800.webp", // ← BUG: должен быть 42-y2k-01-800.webp
    "y2k-02": "/magnum/images/gallery-42/42-memphis-01.jpg",        // ← BUG: дубль мемфиса
    "cyber-01": "/magnum/images/gallery-42/42-cyber-01-800.webp",
    "memphis-01": "/magnum/images/gallery-42/42-memphis-01-800.webp",
    "y2k-03": "/magnum/images/gallery-42/42-cyber-01.jpg",
  };
  ```
- **Также:** `src/lib/galleryTokens.ts:41-44` — `REAL_BY_STYLE_FALLBACK["Y2K"]` указывает на `42-memphis-01-800.webp` вместо `42-y2k-01-800.webp` (тот же копипаст).
- **Эффект:** фильтр Y2K показывает мемфис-арт, проверка 404/health пропустит, но ревьюер галереи (только реальные файлы) засчитает mismatch. На диске `42-y2k-01-800.webp` (67K) и `42-y2k-01.jpg` (2.4M) существуют, но никогда не отдаются.
- **Фикс:** `y2k-01 → 42-y2k-01-800.webp`, `y2k-02 → 42-y2k-01.jpg`, в galleryTokens `Y2K → 42-y2k-01-800.webp`.

### BUG-2 — 350 архивных src → 404 (файлов нет на диске, fallback не применяется)

- **Файл:** `src/pages/GalleryPage.tsx:181-391` (`ARCHIVE_42` 210 шт) + `637-779` (`ARCHIVE_WAVE_2` 140 шт)
- **Код:**
  ```ts
  // пример 181
  { id: "arch-СССР-001", src: "/magnum/images/gallery-42/archive-СССР-001.jpg", ... },
  // пример 639
  { id: "wave2-СССР-211", src: "/magnum/images/gallery-42/wave2-СССР-211.jpg", ... },
  ```
- **Факт:** `ls public/images/gallery-42/` = 8 файлов, `ls /srv/magnum/images/gallery-42/` = 8 файлов, ни одного `archive-*.jpg` / `wave2-*.jpg` нет. Хелпер `getRealSrc(style, src): string` (строка 785) уже существует и чинит это, но в рендере `<img src={art.src}>` используется сырой `art.src` без подмены и без `onError` → 350 × 404 при открытии архива/генерации.
- **Эффект:** галерея рендерит градиент+emoji, но Network 404, Lighthouse ругается, ревьюер `health/tsc/tests/services` может пометить как soft-404.
- **Фикс:** в рендере `src={realSrcOf(art)}` (или `getRealSrc(art.style, art.src)`) + `<img onError={(e)=> e.currentTarget.src=getRealSrc(art.style, art.src)}>`.

## Идея починки

**Скрипт проверки галереи до деплоя + onError fallback (1 задача, 30 мин):**

1. `scripts/check-gallery.sh` — парсит все `"/magnum/images/gallery-42/[^"]+"` из `src/pages/GalleryPage.tsx` и `src/lib/galleryTokens.ts`, сверяет с `ls public/images/gallery-42/`, падает если есть отсутствующие файлы (кроме archive/wave2 — для них проверяет что рендер идёт через `getRealSrc`). Запускать в `bun run build.ts` перед `cp -r dist/* /srv/magnum` и в CI.
2. Патч рендера: заменить `<img src={a.src}>` на `<img src={realSrcOf(a)} onError={...} loading="lazy" decoding="async">` — 2 строки.
3. Поправить BUG-1 (3 строки) и добавить тест `tests/gallery-real-files.test.ts` — `expect(FULL_ARCHIVE.every(a=> existsSync(public+realSrcOf(a))))` не нужен, достаточно что realSrc указывает на 1 из 8 существующих.

> Альтернатива: сгенерить 350 реальных webp батчем (промпты → `public/images/gallery-42/archive-*.jpg`), но это 2ч + 700M, а fallback даёт зелёный health за 30 мин.

## Git

- Ветка: `master` · remote `origin https://github.com/ooo-yuki/magnum-album.git`
- До: `b433f0c hype: 15:17 — +3 идеи` / `76ae4bd review: 15:18 — rating 6/10`
- Status: `M src/pages/games/RhythmGame.tsx` (84 строки: RHYTHM_TIPS + LS_BEST + playFeverBurst + tip rotation — локально, не коммитим в watchdog)
- После: `git add reports/watchdog-*.md && git commit -m "watchdog: $(date +%H:%M) — OK" && git push`

---
*watchdog 1/10 · 2026-09-01 15:22 UTC · magnum-bun active · caddy Up · /magnum 200 · /api 200 · gallery 8/6 ⚠️*
