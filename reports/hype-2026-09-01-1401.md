# HYPE 2026-09-01 14:01 — MAGNUM 3/10

> Хайп-генератор · бэклог 4 фичи · 3 новые идеи · Neon OK

## POST /magnum/api/ideas

Авторизован как `hypebot42` (id 20). 3 идеи успешно созданы в Neon `magnum_ideas`:

- id 73 `DUEL LOBBY 2-4 — WebSocket арена 2-4 + код ABCD` → 201
- id 74 `ЭКО-РЕЙТИНГ 42 v2 — босс Кузбасса 7дн` → 201
- id 75 `LIMITED DROP 42 — 6 скинов/72ч тираж 42` → 201

`curl -s http://localhost:3000/magnum/api/ideas | jq .ideas[0:3]` подтверждает.

---

## Новые идеи (3)

| # | идея | приоритет | монетки | спека (3 строки) |
|---|------|-----------|---------|-------------------|
| 1 | **DUEL LOBBY 2-4 — WebSocket арена** | P0 🔥 | wager 0/42/142/420 + награда `+wager*2` / `+42 рейтинга` | Лобби 2-4 игрока на `Bun.serve ws /magnum/api/ws`: `lobby:create` → код 4 символа → `join` → `ready` → сервер стартует `10с` дуэль. Анти-чит CPS>20 → `suspect:true` + бан раунда. `ws:online` счётчик + heartbeat 25с + реконнект + fallback hot-seat. |
| 2 | **ЭКО-РЕЙТИНГ 42 v2 — босс Кузбасса** | P1 🌱 | 42 (Нормис 0-3) / 142 (Братуха 4-6) / 420 (Легенда 7-8) — 1×/сутки | 8Q квиз про Томь/Кузбасс/42 + факты MAGNUM (ТУСА 8K/200K, VPN 28.04, CLAY РЗТ73, SUPER PUPER NOVA РЗТ80/XXL86). Экран `n/8` + `А знаешь ли ты…`, результат `conic-gradient` бейдж + `addCoins(reward)` + шаринг `Я — Легенда 42!` + `magnum-eco-history` график 10 попыток. |
| 3 | **LIMITED DROP 42 — FOMO-магазин лимиток** | P1 💎 | 142 (uncommon) / 420 (rare) / 1420 (epic) | Дроп 6 скинов/72ч, тираж 42: `meduza-gold-42` epic 1420 `conic-gold`, `tusa-8k-holo` rare 420. `LEFT 12/42` бар + `FomoTimer 72:00:00` GSAP pulse. Солд-аут → аукцион `+42` ставка. `magnum-limited:{dropId,left,endsAt}` LS + `magnum-inventory/equipped` + `EquippedFrame` glow. |

### Детализация

#### 1. DUEL LOBBY 2-4 — WebSocket арена
- **Описание:** Лобби вместо hot-seat. Хост «Создать арену» → `room:ABCD` + `ws.subscribe('room:ABCD')` → до 3 братух `join`. Ставка `0/42/142/420` из `magnum-coins` (`getCoins()>=wager` guard, `addCoins(-wager)` atomic). Таймер `10с`, прогресс `clicks/150` burst `+1` + `navigator.vibrate(20)`, GSAP `scale 1→1.15`. Между раундами `Передай братухе! 3-2-1`.
- **Цена:** `0` френдли или `42/142/420`; победитель `+wager*2` + `+42 ELO`, лузер `−12`, draw возврат. Топ-3 сезона `arena-crown-42` `conic-gold`.
- **GSAP:** `gsap.context` stagger `y:24→0 0.12`, `shake x:±6 0.08s`, конфетти 160 частиц canvas, `prefers-reduced-motion` → `animation:none`.

#### 2. ЭКО-РЕЙТИНГ 42 v2 — босс Кузбасса
- **Описание:** Апгрейд `EcoQuizPage` → босс-неделя 7дн: каждый день 1 задание (сортировка, берег Томи, воздух Кузбасса) + факт MAGNUM. Пропуск → стрик горит, заморозка `🧊 420` 1×/неделю (`magnum-eco-freeze-used`). Финал вс — босс-квиз 8Q → `streak*42+420` если `7/7`. Пропаганда: «Томь — артерия Кемерово, 42 братухи чистят берега».
- **Цена/награда:** `42/142/420` по уровням + `1420` за босс `8/8` + бейдж `Эко-Легенда 42`.
- **GSAP:** stagger `y:18 0.1`, `scale 0→1 spring` бейджа, `progress width n/8*100%` tween `0.4s power2.out`.

#### 3. LIMITED DROP 42 — FOMO-магазин лимиток
- **Описание:** `6/72ч` тираж `42`. Каталог #1: `meduza-gold-42` 🪼 epic 1420, `tusa-8k-holo` 🎬 rare 420, `vpn-neon-42` 📡 rare 420, `clay-73-brown` 🧱 uncommon 142, `nova-80-purple` 💜 rare 420, `fence-42-black` 🏴 epic 1420. `LEFT 12/42` + `72:00:00` таймер. Солд-аут → аукцион `+42`. `magnum-limited:{dropId,items,endsAt}` LS, сервер `magnum_limited_drops` v2. Первый `42→41` → `+142` + `OG 42`.
- **Цена:** `142/420/1420` по редкости `common 42 / uncommon 142 / rare 420 / epic 1420`.
- **GSAP:** `FomoTimer` pulse `scale 1→1.04 1s`, `LimitedGrid` stagger `y:20 0.08`, `SoldOut` shimmer `spin 3s`.

---

## Интеграция

- Файлы: `src/pages/games/DuelArena.tsx` + `src/lib/ws.ts` + `server.ts:websocket`, `src/pages/EcoChallengePage.tsx` + `src/lib/ecoChallenge.ts`, `src/pages/LimitedShopPage.tsx` + `src/lib/limitedShop.ts`
- LS: `magnum-duel-history/leaderboard`, `magnum-eco-quiz/history/challenge`, `magnum-limited/inventory/equipped`, `magnum-coins`
- Кросс: победа дуэли → `StreakCalendar` + `RecapQuest`, эко-бейдж → скидка 42 в шопе, лимитка → OG 1080×1080

> Следующий тик 14:15 — ротация дропа + проверка голосов `magnum_ideas`.
