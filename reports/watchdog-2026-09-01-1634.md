# Watchdog 2026-09-01 16:34 UTC — OK (all 200, active, drift в Neon без рестарта)

**Ветка:** `master` · **HEAD:** `32c3a6a` · **Проверок:** 7/7 · **Рестартов:** 0 · **Cron:** `1/10` каждые 5 мин (watchdog MAGNUM 1/10)

## Health

| # | Проверка | Ожидается | Факт | Код/Статус |
|---|----------|-----------|------|------------|
| 1 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | ✅ 200 | `200` `text/html` `<!DOCTYPE html> MAGNUM — 5opka` |
| 2 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | ✅ 200 | `200` `application/json` `{"ideas":[...101 total]}` ~48KB |
| 3 | `curl -s http://127.0.0.1:3000/magnum/api/ideas` | 200 | ✅ 200 | `200` JSON, идентичен (2) — Bun.serve :3000 |
| 4 | `systemctl is-active magnum-bun` | active | ✅ active | `active (running)` since `2026-09-01 16:33:21 UTC` PID `321407` `enabled` uptime `~70s` `Memory 27.3M` |
| 5 | `docker ps \| grep magnum-caddy` | Up | ✅ Up | `magnum-caddy` `omniroute-caddy:latest` `Up 2 hours` `0.0.0.0:30645-30646->30645-30646/tcp` `25f92f653c48` |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | ⚠️ 8 (факт) | `42-agit-01.jpg 3.4M + 42-agit-01-800.webp 119K`, `42-cyber-01.jpg 2.9M + 42-cyber-01-800.webp 133K`, `42-memphis-01.jpg 2.4M + 42-memphis-01-800.webp 77K`, `42-y2k-01.jpg 2.4M + 42-y2k-01-800.webp 67K` — 4 стиля ×2 (jpg+webp), всего 12M. Ожидалось 6, фактически 8 — расхождение в спеке (webp — производные). `dist/images/gallery-42/` синхронен. |
| 7 | `git status --short` | — | ✅ clean | `master` без изменений (после `32c3a6a feat(games): BACKLOG P0#1-3...`), `git log -5` clean |

**Доп:** `journalctl -u magnum-bun` показывает `NeonDbError 42P01` для `magnum_follows` и `magnum_ai_usage` (см. баги), но health отвечает 200 т.к. ошибки глушатся try/catch. `ss *:3000 LISTEN` bun `321407`. Caddy `30645 TLS --resolve 127.0.0.1` подтверждает терминацию.

## Коды ответов

```
https://oooyuki.zomb.top:30645/magnum/             → 200 text/html (TLS Caddy 30645, --resolve 127.0.0.1, Bun.serve прокси)
https://oooyuki.zomb.top:30645/magnum/api/ideas    → 200 application/json {ideas:[{id:65,votes:142,approved},{id:55..123} 101 total]} id:65 Freakland gen3 130, 65-66 approved, остальные pending/new
http://127.0.0.1:3000/magnum/api/ideas              → 200 application/json (Bun.serve :3000 прямой, идентичен TLS, ~48KB)
systemctl is-active magnum-bun                     → active (running) since 2026-09-01 16:33:21 UTC
docker ps magnum-caddy                             → Up 2 hours (omniroute-caddy:latest, 30645-30646)
ls public/images/gallery-42/                       → 8 файлов (4×jpg + 4×webp)
git status --short                                 → (пусто) clean
```

## Рестарты

| Сервис | Действие | Причина | Результат |
|--------|----------|---------|-----------|
| magnum-bun | — | не требовался (все 200, active) | — |
| magnum-caddy (docker compose) | — | не требовался (Up 2 hours, 30645 LISTEN, --resolve 200) | — |

> Критерий рестарта: любой не-200 или не-active → `systemctl restart magnum-bun; cd /opt/magnum-caddy && docker compose up -d; sleep 3; curl-повтор`. Повторная проверка не выполнялась — все пробы зелёные.

## Найденные баги (файл:строка)

### 1) `server.ts:2168-2169` + `drizzle/migrations/0016_chat_follows.sql` / `0017_ai_guard.sql` — дрифт схемы Neon: таблицы не применены, health врёт 200 (P0)

```ts
// server.ts:2168
try { const r = await sql`SELECT count(*)::int as c FROM magnum_follows`; followsCount = Number((r[0] as {c:number}).c); } catch (e) { console.error("[health] follows count failed", e); }
// server.ts:2169
try { const r = await sql`SELECT count(*)::int as c FROM magnum_ai_usage`; aiUsageCount = Number((r[0] as {c:number}).c); } catch (e) { console.error("[health] ai_usage count failed", e); }
```

`journalctl -u magnum-bun -n 50` (16:33:35):

```
NeonDbError: relation "magnum_follows" does not exist  code: "42P01"  file: "parse_relation.c" line: "1498" routine: "parserOpenTable"
NeonDbError: relation "magnum_ai_usage" does not exist code: "42P01"
[health] follows count failed ...
[health] ai_usage count failed ...
```

`drizzle/schema.ts:281` (`magnumFollows`) и `:288` (`magnumAiUsage`) описаны, миграции `0016_chat_follows.sql` и `0017_ai_guard.sql` лежат в репо, но в прод Neon (`DATABASE_URL` us-east-2 proud-bar-62331523) не применены — health глушит ошибку и возвращает `counts:{follows:0,aiUsage:0}` с `ok:true`, маскируя деградацию. Любой `POST /magnum/api/follows` / `POST /magnum/api/ai` упадёт 500. Фикс: `DATABASE_URL_UNPOOLED` → `drizzle-kit push` / `bunx drizzle-kit migrate` или ручной `psql < 0016+0017`, добавить post-deploy проверку `SELECT to_regclass('magnum_follows')`.

### 2) `src/components/AiBot.tsx:77,334,355` + `src/components/PromoPopup.tsx:21,61,98` — `localStorage` вопреки архитектуре "без localStorage" (P1)

```ts
// src/components/AiBot.tsx:77
if(!balance){ try{ const v = localStorage.getItem("magnum_coins"); if(v) balance = Number(v)||0; }catch{} }
// src/components/AiBot.tsx:334
try { localStorage.setItem("magnum-frame-verified", "1"); } catch {}
// src/components/AiBot.tsx:355
try { localStorage.setItem("magnum-frame-verified","1"); } catch {}
// src/components/PromoPopup.tsx:21
if (localStorage.getItem(seenKey)) return;
// src/components/PromoPopup.tsx:61 + :98 аналогично setItem
```

Спека MAGNUM: "all state in Neon Lakebase Postgres (magnum_users/sessions...), custom auth, multiplayer via WebSocket on Bun.serve, no localStorage" (memory). Код всё ещё читает/пишет `magnum_coins` и `magnum-frame-verified` из localStorage как fallback, создавая рассинхрон с Neon `magnum_coins`/`magnum_frames` и `LS magnum_shop_inventory`. При очистке LS — баланс 0, при мульти-табе — гонка. Надо удалить fallback и оставить только `fetch("/magnum/api/coins",{credentials:"include"})` и `POST /magnum/api/frame/verify` (уже есть на :355 рядом). PromoPopup по той же причине должен хранить `seen` в Neon `magnum_notifications` или cookie, не в LS.

## Идея починки (1)

**Миграционный health-gate + LS-выпил за 15 мин:** 1) `cd /root/magnum-album && DATABASE_URL=$DATABASE_URL_UNPOOLED bunx drizzle-kit push --force` (применяет 0016/0017, проверяет `SELECT count(*) FROM magnum_follows` / `magnum_ai_usage` без 42P01, затем `systemctl restart magnum-bun` и `curl --resolve ... /magnum/api/health` должен показать `counts.follows`/`aiUsage` без `failed` в journal). Добавить в `server.ts:2167-2169` явный `throw` если таблицы отсутствуют, чтобы health отдавал 500 а не 200 с нулями, и cron-алерт в watchdog. 2) Патч `AiBot.tsx`: удалить строки 77,334,355 `localStorage` — заменить на `credentials:include` fetch уже есть (строка 5-6), для frame — оставить только `fetch /magnum/api/frame/verify` (строка 355 уже дублирует). 3) Обновить спеку галереи: `ls -lh public/images/gallery-42/` ожидает 6, факт 8 — поправить проверку на `8 (4 jpg + 4 webp)` или считать `*.jpg` отдельно. Это закроет дрифт БД, уберёт тихий фейл health и нарушение "no localStorage", без рестарта Caddy.

