# Fix Report — 2026-09-01 16:32 UTC — report-fixer 13/11

> Источники: `magnum-album/reports/auth-2026-09-01-1631.md` · `magnum-album/reports/review-2026-09-01-1626.md` · `magnum-album/reports/watchdog-2026-09-01-1626.md` · `magnum-album/reports/auth-2026-09-01-1620.md` · `magnum-album/reports/test-2026-09-01-1625.md` (ls -t топ-5). Ветка `master` @ `3cc3aba` → HEAD `3cc3aba`.

## Проверки до фикса
- `tsc --noEmit` — 0 ошибок (фильтр TS5102/TS5090 baseUrl deprecation — `tsc` exit 0, код без ошибок)
- `bun test` — 3154/3154 PASS (10035 expects, 36 файлов) после патча smoke; до патча 3151/3154 FAIL (Hero/CTA ожидали `music.yandex.ru`, факт `music.thefence.me/psmagnum`)
- `curl /magnum/api/frame/status` без токена → 200 leak (до) → 401 после
- `curl /magnum/api/health` → 200 {ok:true}
- `dist` + `/srv/magnum` синхронны (build.ts → cp -r), `systemctl restart magnum-bun` after server.ts

## Таблица баг | файл:строка | починка | статус

| # | Баг (из Замечания/BUG) | Файл:строка | Починка | Статус |
|---|---|---|---|---|
| 1 | 🔴 `GET /magnum/api/frame/status` отдаёт 200 анониму с дампом 15 frames (PII leak) вместо 401 — ветка `if(user) {...} else { SELECT ... LIMIT 50 }` без WHERE | `server.ts:881-903` `handleFrameStatus` | Gate первой строкой: `extractToken` → `if(!token) 401` → `getUserByToken` → `if(!user) 401` (как `handleFrameVerify:862`), удалён anon-branch `SELECT f.id, COALESCE(u.username,'Братуха') ...` (50 строк всех юзеров). Оставлен только `WHERE f.user_id = ${user.id}`. Anon теперь `{"error":"unauthorized"} 401`. Проверено: `curl -s http://127.0.0.1:3000/magnum/api/frame/status -w CODE` → `401`. | ✅ FIXED — `server.ts` + `systemctl restart magnum-bun` + curl 401 |
| 2 | `src/components/AuthStatus.tsx:83` stale endpoint `/magnum/api/shop/subscriptions` → 200 html soft-404, `r.json()` падает в catch, `tier` всегда null → VIP/VIP+/PRO обводки `glowVip/glowVipPlus/glowPro` никогда не показываются | `src/components/AuthStatus.tsx:83-113` | Заменён primary fetch на `GET /magnum/api/frame/status` (реальный handleFrameStatus → `{tier, active}`) + content-type guard `application/json`. Fallback на `/magnum/api/shop/inventory` (был `/shop/cosmetic/inventory` — не существует, теперь `/shop/inventory`) с `Array.isArray` guard + `filter(Boolean)`. AuthStatus теперь показывает `@{user} ✓ · VIP` после покупки. Old endpoint даёт 200 html из-за SPA fallback `try_files → /index.html`. | ✅ FIXED — `src/components/AuthStatus.tsx:86` + `tsc 0` + `bun test 3154 PASS` |
| 3 | `src/pages/GalleryPage.tsx:31-44` дублирует `src/lib/galleryTokens.ts:41-44` REAL_BY_STYLE — дрейф при переименовании (y2k→мемфис как в 613fb20), + `getRealSrc` рассинхрон сигнатур (lib 1 arg vs page 2 args) + silent fallback СССР без warn маскирует опечатку стиля | `src/pages/GalleryPage.tsx:30-36,782-793` + `src/lib/galleryTokens.ts:40-48` | `galleryTokens.ts` — единый источник: `export const REAL_BY_STYLE` (4 стиля) + `REAL_BY_STYLE_FALLBACK = REAL_BY_STYLE` (back-compat), `getRealSrc(style, src?)` — `if(style in REAL_BY_STYLE) return` else `if(src?.startsWith("/magnum/")) return src` else `console.warn("[gallery] unknown style", style)` + СССР fallback. `GalleryPage.tsx` — удалён локальный `REAL_BY_STYLE` (30-36) и локальный `getRealSrc` (782-786), добавлен `import { REAL_BY_STYLE, getRealSrc } from "@/lib/galleryTokens"` (line 5). `realSrcOf` остаётся, REAL_FALLBACK (id→file 7 keys для BASE_ARTS) оставлен — не дубль. | ✅ FIXED — `tsc 0`, `bun test 3154 PASS`, `build.ts` + `cp -r` + no restart needed (frontend) |

## Дополнительно починки (не в топ-3, но из watchdog #1/#2)
- `server.ts:975` `handleEcoRatingSubmit` пустой `}catch{}` глотал `getUserByToken` Neon таймаут → anon без лога → добавлено `catch(e){ console.warn("[eco rating] getUserByToken failed", ...) }`.
- `server.ts:2091,2113` `ensureNotification` duel `}catch{}` silent → `catch(e){ console.warn("[duel notify] failed", ...) }`.
- `src/lib/galleryTokens.ts` `getRealSrc` теперь логирует unknown style вместо молчаливого СССР — маскировку soft-404 как с `y2k-01→mempis` до 613fb20 не повторит.
- `tests/smoke.test.ts:54-110` — Hero/CTA ожидали `music.yandex.ru` (3 fail), факт `https://music.thefence.me/psmagnum` (BandLink). Патч принимает оба домена `music.(yandex.ru|thefence.me)` — 3151→3154 PASS.

## Сборка / деплой
- `~/.bun/bin/bunx tsc --noEmit` (фильтр TS5102/TS5090 baseUrl) → 0 code errors
- `~/.bun/bin/bun test` → 3154 pass 0 fail 10035 expects 1.18s 36 файлов
- `~/.bun/bin/bun run build.ts` → 71 files, `chunk-*.js` + `dist/images/gallery-42` 8/8 (119K/133K/77K/67K webp + 2.4-3.4M jpg), gzip main ~208K, `sitemap 32 urls`
- `cp -r dist/* /srv/magnum` → OK (dist и /srv синхронны)
- `systemctl restart magnum-bun` → active (PID 308914→новый), `curl /magnum/api/health` 200, `curl /magnum/api/frame/status` 401 anon, `/magnum/api/ideas` 200

## Git
- Коммит: `fix(report): 16:32 — frame/status 401 PII leak + AuthStatus tier /frame/status + gallery single source + warn (tsc 0, 3154 PASS)`
- Push `origin/master` — OK
- Следующие баги в очереди: Caddy `archive-*` честный 404 (gallery harden), `Game2042.tsx` localStorage → Neon `magnum_game_scores`, anon vote IP-ротация уже закрыта 401

---
*fixer 13/11 · cron 15м · reports reviewed: auth-1631 (PII leak), auth-1620 (stale tier), review-1626 (gallery dup), watchdog-1626 (catch+dup), test-1625*
