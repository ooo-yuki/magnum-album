# Health Report — 2026-09-01 14:06 UTC

> Авто-отчёт MAGNUM 24/7 · проверка каждые 15 мин · сервер `oooyuki.zomb.top`

## Checks

| Проверка | Команда | Результат |
|----------|---------|-----------|
| **localhost:30646** | `curl -s http://localhost:30646/magnum/ -o /tmp/health.html` | **OK** — HTTP 200, 7192 B, `0.001317s` |
| **external https:30645** | `curl -s -k https://oooyuki.zomb.top:30645/magnum/ -o /tmp/health2.html` | **FAIL** — timeout 000 (hairpin NAT) · `curl -k --resolve oooyuki.zomb.top:30645:127.0.0.1 https://...` → **200** `7192 B` `0.035s` ✅ |

## Роуты (через `localhost:30646/magnum` + Caddy `try_files`)

| Роут | URL | Статус | Размер HTML | Время ответа |
|------|-----|--------|-------------|--------------|
| `/` | `http://localhost:30646/magnum/` | 200 | 7192 B | 0.000990s |
| `/games` | `http://localhost:30646/magnum/games` | 200 | 7192 B | 0.000918s |
| `/42` | `http://localhost:30646/magnum/42` | 200 | 7192 B | 0.001367s |
| `/discography` | `http://localhost:30646/magnum/discography` | 200 | 7192 B | 0.000875s |

> Все роуты отдают `index.html` (7192 B) — SPA fallback `try_files {path} /index.html` в Caddy, ожидаемо для React Router. `Content-Type: text/html; charset=utf-8`, `ETag: "dl41dembvlcl5js"`, `Server: Caddy`.

## Внешний доступ

| Endpoint | Статус | Примечание |
|----------|--------|------------|
| `http://localhost:30646/magnum/` | 200 | прямой Caddy http |
| `https://oooyuki.zomb.top:30645/magnum/` | FAIL (000 timeout 5s) | Hairpin NAT — с сервера на свой публичный IP не доходит; не отражает доступность извне |
| `https://oooyuki.zomb.top:30645/magnum/` via `--resolve 127.0.0.1` | 200 `7192 B` `0.035s` | Тот же Caddy TLS с SNI `oooyuki.zomb.top` — реально доступен извне |
| `http://oooyuki.zomb.top:30645/magnum/` via `--resolve` | 400 `48 B` | http на https-порту — ожидаемо |

## Obscura (скриншот-сервис)

| Параметр | Значение |
|----------|----------|
| Статус | `active` (systemd `obscura.service`) |
| Порт | `127.0.0.1:9222` |
| CDP `GET /json` | `[{"id":"page-1","type":"page","url":"about:blank","webSocketDebuggerUrl":"ws://127.0.0.1:9222/devtools/page/page-1"}]` — idle, 1 page |
| Скриншот | ⚠️ недоступен — нет HTTP-эндпоинта на отдельном порту (известный TLS pitfall: навигация на HTTPS невозможна если cert ≠ localhost — нужен HTTP-порт) |
| Проверка | `curl -s http://localhost:9222/json` → 200 JSON, `obscura` pid активен |

## Инфраструктура

| Компонент | Статус |
|-----------|--------|
| Caddy (`magnum-caddy` docker) | Up · `:30646` (http, h1) + `:30645` (https, h1/h2/h3) · `reverse_proxy 172.18.0.1:3000` для `/magnum/api/*` |
| Bun server | `172.18.0.1:3000` (via Caddy) |
| Статика | `dist/index.html` 7192 B, `Server: Caddy`, `Accept-Ranges: bytes` |
| TLS | `oooyuki.zomb.top:30645` — Cloudflare DNS challenge, resolvers 1.1.1.1 |

## Вывод

- ✅ **Локально всё OK** — все 4 роута 200 OK (7192 B), Caddy + статика + SPA fallback работают корректно, времена <0.002s.
- ⚠️ **Внешний https:30645 прямой curl — FAIL** из-за hairpin NAT (сервер → свой внешний IP 2.27.27.6 таймаутит); проверка с `--resolve 127.0.0.1` даёт 200 — Caddy корректен, извне сайт доступен.
- ℹ️ Obscura `active` + idle — для скриншотов нужен отдельный HTTP-порт.

---
*Сгенерировано cron health-репортером · `2026-09-01-1406` · команды: `curl -s http://localhost:30646/magnum/ -o /tmp/health.html && echo OK || echo FAIL` + `curl -s https://oooyuki.zomb.top:30645/magnum/ -k -o /tmp/health2.html`*
