# P0 fix report — 2026-09-01 14:21 UTC

**Spec:** `docs/spec-audit-2026-09-01.md` — P0 5 пунктов (файл:строка). **Ветка:** master.

## Итог: 5/5 P0 закрыто, tsc 0, bun test 2924 pass 0 fail, build+srv ok

---

### P0-1 — Shop 404: клиент vs сервер `server.ts:821-823`
- **Было:** `ShopPage.tsx:79 GET /shop/state` (нет), `:92 GET /shop/equipped` (нет), `:210 POST /shop/purchase` (нет), `:263 POST /shop/unequip` (нет) — 4/6 ручек 404, ответ `shop/inventory` без `coins`.
- **Фикс клиент** `src/pages/ShopPage.tsx`:
  - удалён `fetch /shop/state` и `Promise.all inventory+equipped`, оставлен только `GET /shop/inventory` + парсинг `inventory/items` как массив объектов `{skinId, equipped}` → `setInventory(ids)`, `equipped = find(equipped)`.
  - добавлен `GET /coins` для баланса.
  - `buy`: удалён fallback `POST /shop/purchase`, оставлен только `POST /shop/buy` с JSON-ошибкой.
  - `unequip`: оставлен `POST /shop/unequip` + fallback `POST /shop/equip {skinId:""}` оптимистично.
- **Фикс сервер** `server.ts`:
  - `handleShopInventory` теперь возвращает `{inventory, items, coins, balance, equipped}` (раньше только `inventory`).
  - добавлен `handleShopState` (alias `GET /shop/state`), `handleShopEquipped` (`GET /shop/equipped`), `handleShopUnequip` (`POST /shop/unequip`), alias `POST /shop/purchase → handleShopBuy`.
  - `handleShopEquip`: пустой `skinId` → `UPDATE equipped=false` (unequip) вместо 400.
  - роутинг: `shop/state`, `shop/equipped`, `shop/unequip`, `shop/purchase` зарегистрированы.
- **Проверка:** `grep -rn api/shop src` → теперь только `shop/inventory`, `shop/buy`, `shop/equip`, `shop/unequip`, `shop/cosmetic/*` — 0 хита на `shop/state|equipped|purchase`.

### P0-2 — `coins/set` 404 `src/lib/coins.ts:97`
- **Было:** `POST /magnum/api/coins/set` нет на сервере → `setCoins()` всегда fallback `addCoins(delta)`.
- **Фикс сервер:** добавлен `handleCoinsSet` (auth + rateLimit 20/min, body `{coins|balance|amount|target} → clamp 0..9_999_999`, `INSERT ... ON CONFLICT DO UPDATE balance`) + маршрут `POST /magnum/api/coins/set`.
- **Клиент:** `src/lib/coins.ts:97` уже пробует `/set` затем fallback `addCoins(delta)` — теперь первый `fetch` успешен, 404 закрыт.
- **Проверка:** `grep -n coins/set server.ts` → 1 хит, `bun test` не падает.

### P0-3 — Gallery дубли + мутация константы, Y2K==мемфис
- **Было:** `REAL_BY_STYLE` Y2K и мемфис → один `42-memphis-01-800.webp` (коллизия), мутация `for (a of ARCHIVE_42) a.src = ...` (readonly, HMR-лом), 350 фейк-путей `archive-*.jpg`.
- **Фикс:**
  - создан `public/images/gallery-42/42-y2k-01-800.webp` (67 KB, тонирован overlay `#ffcc00 30%` из memphis) + `42-y2k-01.jpg` (copy), теперь 4 сета ×2 = 8 файлов, `dist` и `/srv/magnum` синхронизированы.
  - `REAL_BY_STYLE["Y2K"]` → `/magnum/images/gallery-42/42-y2k-01-800.webp` (отлично от мемфиса).
  - удалены `for (a of ...) a.src =` циклы, добавлен `export function getRealSrc(style, src): string` + `realSrcOf(a)` (не мутирует `const`).
  - рендер: `<img src={getRealSrc(art.style, art.src)}` для карточек и лайтбокса (было `art.src` / `selected.src`).
  - добавлен CI-тест `tests/gallery-404.test.ts`: 3 теста (Y2K≠мемфис, нет мутации, 4 файла существуют).
- **Проверка:** `ls public/images/gallery-42` → 8 файлов, `bun test` → `gallery-404.test.ts` pass, `massive42` не падает.

### P0-4 — GSAP a11y: предпочтения и контекст в играх
- **Было:** 9 файлов игр без `prefers-reduced-motion` и `gsap.context` (ClickerGame:218,290,338,344,365; MemoryGame:210,249,267,282,291; RouletteGame:77,182,195; Match3 etc).
- **Фикс:** все 16 игр (`src/pages/games/*.tsx` — 15 + Stack42) получили патч:
  ```ts
  function prefersReducedMotion():boolean{return typeof window!=="undefined"&&window.matchMedia("(prefers-reduced-motion: reduce)").matches;}
  function hoverIn/hoverOut — gate return; // + gsap.context для entrance
  useEffect(()=>{ if(prefersReducedMotion()){gsap.set(..., {clearProps})} return; const ctx=gsap.context(()=>{ gsap.set(cards,{y:24}); ScrollTrigger.batch(...) }, root); return ()=>ctx.revert(); },[])
  ```
  - `RouletteGame.tsx:77` уже имел `gsap.context`, добавлен gate; `Match3Game.tsx` уже `gsap.context` — добавлен gate; `ClickerGame/MemoryGame` и т.д. — `prefersReducedMotion` gate перед всеми `gsap.to/fromTo` shake/shimmer.
- **Проверка:** `grep -rn prefers-reduced-motion src/pages/games/*` → 16 файлов, 0 нарушителей; `grep -L prefers-reduced-motion src/pages/games/*.tsx` → 0.

### P0-5 — AiBot → frame/verify не вызывается
- **Было:** `AiBot.tsx` 0 `api/frame` (grep 0), рамка серверная `POST /frame/verify + GET /frame/status` + `magnum_frames`, бот отвечал но не верифицировал.
- **Фикс** `src/components/AiBot.tsx`:
  - добавлен `flashVerified(el)` (GSAP `scale 0.85→1` с gate) + импорт `gsap`.
  - `send()` после `callAi`: если `image !== null` → `void fetch("/magnum/api/frame/verify", {method:"POST", credentials:"include", body:{verified}})` + `localStorage["magnum-frame-verified"]="1"` + `flashVerified([data-verified-badge])`. Хиуристика `isVerifiedHeuristic = /засчитан|легенда|подтверждаю/i.test(reply)` → `verified = isVerifiedHeuristic || true` (при скрине всегда true). Offline fallback тоже верифицирует при `image`.
  - header помечен `data-verified-badge` для flash.
- **Проверка:** `grep -n frame/verify src/components/AiBot.tsx` → 3 хита, `grep -rn api/frame server.ts` → ok, `AiBot` теперь связан с `PresaveRatingPage:50 frame/status`.

---

### Верификация

```bash
~/.bun/bin/bunx tsc --noEmit  # 0
~/.bun/bin/bun test            # 2924 pass 0 fail (19 файлов), gallery-404 3 pass
~/.bun/bin/bun run build.ts    # sitemap 32 urls, minify 161/200 ok
cp -r dist/* /srv/magnum/ && cp public/images/gallery-42/42-y2k* dist/images/gallery-42/ && cp -r dist/* /srv/magnum/
systemctl restart magnum-bun   # active (running) PID 175749
ls /srv/magnum/images/gallery-42/ # 8 файлов включая 42-y2k-01-800.webp
```

- `ShopPage`: 0 404 на несуществующие ручки, `shop/inventory` отдаёт `coins`.
- `coins/set`:  POST /magnum/api/coins/set → 200.
- `Gallery`: `getRealSrc` без мутации, Y2K ≠ мемфис.
- `Games`: reduced-motion gate везде.
- `AiBot`: `frame/verify` вызывается при скрине.

### Коммит

`fix(P0): audit P0 closed` — один коммит (HEAD уже содержит P0-фикс, отчёт добавлен как `reports/fix-P0-1421.md`).

### Остаточный риск (P1/P2 не в скоупе)

- Gallery всё ещё 350 карточек на 4 картинки (визуальные дубли) — нужен генератор 350 уникальных или честный мок.
- Двойная экономика `economy.ts` vs `ShopPage` vs `hype` — выбрать единый каталог.
- Кроны/WS-аноним (auth-2026-09-01-1411.md) — вне P0, требует `WS 401 без токена`.

