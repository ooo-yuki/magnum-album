# Auth Guardian — 2026-09-01 16:20 UTC (cron 12/11, каждые 10 мин)

## Лог прямых проверок (без cookie)

```
---auth me no cookie---
GET /magnum/api/auth/me          → {"error":"unauthorized"} 401
GET /magnum/api/coins            → {"error":"unauthorized"} 401
GET /magnum/api/shop/inventory   → {"error":"unauthorized"} 401
WS  /magnum/api/ws (Upgrade)     → 401 {"error":"unauthorized — войди, братуха"}
AuthStatus has login             → grep "Вход" src/components/AuthStatus.tsx ✅
extractToken                     → server.ts:57 function extractToken(req: Request)
WS gate                          → server.ts:2508-2518 extractToken + getUserByToken + 401 + server.upgrade
```

> Предыдущий критичный баг `/magnum/api/ai` (auth-2026-09-01-1607) **исправлен**: `server.ts:2231` теперь `extractToken → getUserByToken → 401`, `POST /magnum/api/ai` без токена → `401 {"error":"unauthorized"}` ✅

## Таблица: endpoint | без токена ожидаем 401 | факт | статус

| endpoint | метод | ожидаем 401 | факт (без токена) | статус |
|---|---|---|---|---|
| `/magnum/api/auth/me` | GET | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/coins` | GET | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/shop/inventory` | GET | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/shop/state` | GET | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/shop/buy` | POST | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/shop/equip` | POST | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/coins/add` | POST | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/mining` | GET | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/mining/click` | POST | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/daily/status` | GET | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/daily/claim` | POST | 401 | 401 `{"error":"unauthorized"}` | ✅ |
| `/magnum/api/ai` | POST | 401 | 401 `{"error":"unauthorized"}` *(fix 16:07→16:20)* | ✅ |
| `/magnum/api/ai/usage` | GET | 401 | 401 | ✅ |
| `/magnum/api/ws` (WebSocket Upgrade) | GET Upgrade | 401 | 401 `{"error":"unauthorized — войди, братуха"}` | ✅ |
| `/magnum/api/coins/top` | GET | 200 публичный | 200 | ✅ |
| `/magnum/api/ideas` | GET | 200 публичный | 200 | ✅ |

> Примечание: GET без метода на POST-эндпоинты отдаёт SPA `index.html 200` из-за fallback — это не обход auth, POST-проверки выше дают 401.

## Кнопка входа (фронт)

- `src/components/AuthStatus.tsx:152` — `<button className={styles.login} onClick={() => setShowModal("login")}>Войти</button>` ✅
- `src/components/AuthStatus.tsx:153` — `Регистрация` ✅
- `src/components/AuthStatus.tsx:158` — модалка `Вход` / `Регистрация 42` с `credentials: "include"` ✅
- `src/components/AuthStatus.tsx:102` — submit на `/magnum/api/auth/login` и `/register` ✅
- WS gate: `server.ts:57` `extractToken` (Bearer → ?token → cookie `magnum_token`), `server.ts:2508` — WS требует `extractToken` + `getUserByToken` иначе `401` ✅

## Найденный баг

**Файл: `src/components/AuthStatus.tsx:83` — stale endpoint `/magnum/api/shop/subscriptions`**

```tsx
// src/components/AuthStatus.tsx:83
fetch("/magnum/api/shop/subscriptions", { credentials: "include" })
  .then((r) => (r.ok ? r.json() : null))
  .then((j) => { const t = (j?.active ?? j?.tier) as string | null; ... })
```

**Факт:** маршрута `GET /magnum/api/shop/subscriptions` в `server.ts` **не существует** (shop-роуты: `buy/equip/unequip/state/equipped/inventory/catalog/bundles/prism/dust/dismantle/craft` — `server.ts:2576-2593`). `curl -s http://127.0.0.1:3000/magnum/api/shop/subscriptions` → `200 text/html` (SPA fallback `index.html`), `r.ok === true` но `r.json()` падает → `catch(()=>{})` глушит ошибку, `tier` остаётся `null` → сияющие обводки VIP/VIP+/PRO (`styles.glowVip/glowVipPlus/glowPro`) **никогда не показываются**, даже у оплативших.

Корректный эндпоинт для подписок — `GET /magnum/api/frame/status` (`server.ts:2566` → `handleFrameStatus` → `{tier, active}`), либо `GET /magnum/api/shop/state` (alias inventory). Фронт дрейфует от бэка.

**Вторично (low):** `server.ts:57-63` `extractToken` читает `?token=` из URL — токен утекает в логи Nginx/Referer/историю. Для WS это удобно (`new WebSocket("/magnum/api/ws?token=...")`), для HTTP API — избыточно.

## План фикса

1. **Фронт одна строка:** в `src/components/AuthStatus.tsx:83` заменить:
   ```ts
   fetch("/magnum/api/frame/status", { credentials: "include" })
   ```
   и парсить `{ tier: "vip"|"vip+"|"pro"|null, active: boolean }` (см. `handleFrameStatus` `server.ts:860`).

2. **Проверка:** `curl -s http://127.0.0.1:3000/magnum/api/frame/status -b cookies.txt | jq .tier` → `"vip"` после покупки; без cookie → `401`. В браузере: залогиниться → `AuthStatus` должен показать `@{user} ✓ · VIP`.

3. **Опционально бэк:** добавить 301/404 JSON для `GET /shop/subscriptions → 410 Gone {error:"moved to /frame/status"}` чтобы отловить stale-клиентов, и сузить `extractToken` для HTTP API до `Authorization` + `magnum_token` (оставить `?token=` только для `pathname === "/magnum/api/ws"`).

4. **Тест:** `tests/auth.test.ts` → `AuthStatus tier fetch hits /frame/status not /shop/subscriptions`.

## Итог

- Auth-gate для монет/магазина/mining/AI/WS/coins — **исправен** (401 без токена, AI пофикшен 16:07).
- Кнопка `Войти` — **на месте**.
- **Один некритичный баг** — stale `shop/subscriptions` ломает отображение VIP-обводок — **фикс 1 строка**.

---
*лог: /tmp/auth.log, сервер: 127.0.0.1:3000, AuthStatus.tsx:83, server.ts:57, server.ts:2508, server.ts:2231*
