# Watchdog 1/10 — 2026-09-01 14:51 UTC

## Health

| # | Проверка | Ожидаемо | Факт | Статус |
|---|----------|----------|------|--------|
| 1 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | 200 | ✅ |
| 2 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | 200 JSON (`ideas:[{id:65...}]`) | ✅ |
| 3 | `curl -s http://127.0.0.1:3000/magnum/api/ideas` | 200 | 200 | ✅ |
| 4 | `systemctl is-active magnum-bun` | active | active (running since 14:42:39, PID 205121, 28.8M) | ✅ |
| 5 | `docker ps \| grep magnum-caddy` | Up | Up (431e292c2c6a omniroute-caddy, ~1h, 0.0.0.0:30645-30646) | ✅ |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | 8 файлов (4× jpg + 4× 800.webp — agit/cyber/memphis/y2k, 12 MB) | ⚠️ +2 |
| 7 | `git status --short` | clean | clean (no changes) | ✅ |
| 8 | `bun tsc --noEmit` | 0 errors | 0 errors | ✅ |
| 9 | `bun test` | pass | 3017 pass / 0 fail (25 files, 9226 expects, 1.92s) | ✅ |

**Итоговый статус: OK — все критичные сервисы UP, рестарты не требуются.**

## Коды ответов

- Внешний HTTPS `/magnum/` → `200` (HTML, title MAGNUM — 5opka)
- Внешний HTTPS `/magnum/api/ideas` → `200` (JSON, votes:142 top idea #65 Freakland gen3)
- Локальный HTTP `:3000/magnum/api/ideas` → `200` (идентичный JSON)

## Рестарты

Не выполнялись — все проверки 200/active/Up. Условие «что-то не 200 или не active» не сработало.
- `systemctl restart magnum-bun` — не нужен
- `docker compose up -d` (/opt/magnum-caddy) — не нужен
- Повторная проверка — не нужна (первичные 200)

## Галерея детально

```
42-agit-01-800.webp  119K
42-agit-01.jpg       3.4M
42-cyber-01-800.webp 133K
42-cyber-01.jpg      2.9M
42-memphis-01-800.webp 77K
42-memphis-01.jpg    2.4M
42-y2k-01-800.webp   67K
42-y2k-01.jpg        2.4M
```
Итого 8 файлов против ожидаемых 6 по чек-листу — лишние 2 это 800.webp дубли (по факту это норма: 4 оригинала + 4 превью). 404 не обнаружено; GalleryPage.tsx использует эти 4 сета как REAL_FALLBACK/REAL_BY_STYLE. ARCHIVE_42 (210 фейковых archive-*.jpg) — мок-генерация, файлов на диске нет, но ревьюер health проверяет 404 — покажет битые src для archive.

## Найденные баги (файл:строка)

### BUG-1 — Архитектурное нарушение: `localStorage` вместо Neon/cookie/WebSocket
- **Файлы:**
  - `src/pages/games/BlackjackGame.tsx:221` — `localStorage.getItem(LS_BALANCE)` / `:224` `LS_BEST` / `:253` `setItem` / `:549`
  - `src/pages/games/RouletteGame.tsx:96` — `localStorage.getItem(LS_BALANCE)` / `:99` `LS_HISTORY` / `:103`
  - `src/pages/games/Game2042.tsx:255` — `localStorage.getItem("2042-best")` / `:256` `2042-bestTile` / `:309,:312`
  - `src/components/AiBot.tsx:77,319,340` — `localStorage.getItem("magnum_coins")` / `setItem("magnum-frame-verified")`
  - `src/components/PromoPopup.tsx:21,61,98` — `localStorage.getItem/setItem(seenKey)`
  - `src/pages/HomePage.tsx:269,286` — `magnum-banner-closed`
- **Суть:** ТЗ требует «all state in Neon Lakebase Postgres, custom auth, no localStorage» — баланс, история, рекорды должны жить в `magnum_users/coins/sessions` + WebSocket на `Bun.serve`. Сейчас 45 вхождений `localStorage` дают рассинхрон баланса (игра → монетки → магазин → VIP), обход экономики и потерю данных при очистке браузера. `ShopPage.tsx:595` даже пишет «без localStorage», но код делает наоборот.
- **Риск:** P1 — ломает замкнутую экономику, ревьюер `reviewer` должен ловить как `localStorage` violation.

### BUG-2 — Перепутанные маппинги галереи `REAL_FALLBACK`
- **Файл:** `src/pages/GalleryPage.tsx:39-43`
  ```ts
  "y2k-01": "/magnum/images/gallery-42/42-memphis-01-800.webp", // ← должен быть y2k
  "y2k-02": "/magnum/images/gallery-42/42-memphis-01.jpg",      // ← должен быть y2k
  "y2k-03": "/magnum/images/gallery-42/42-cyber-01.jpg",       // ← id y2k, а файл cyber
  ```
- **Суть:** 3 ключа Y2K указывают на файлы memphis/cyber. Карточки `Bling-бабл 42`, `Флипфон 42`, `Неон-бейдж 42` (BASE_ARTS id y2k-01/02/03) показывают чужие стили — фильтр «Y2K» отдаёт мемфис-арты. Аналогично `MOCK_POOL:134` `Тетрисуй 42` (style Y2K) грузит `42-memphis-01-800.webp`. Ревьюер gallery проверяет соответствие style→src — будет 404-логика/визуальный баг.
- **Плюс ARCHIVE_42:** `src/pages/GalleryPage.tsx:181-391` — 210 записей `archive-*.jpg` без файлов на диске (ls показал только 8 файлов). Любой рендер `ARCHIVE_42`/`pickRandomArts` даст 404, health-check галереи падает.

## Идея починки (1)

**P0 — Галерея + экономика за один проход (≤80 строк):**
1. Пофиксить `GalleryPage.tsx:39-41,43` — заменить на правильные `42-y2k-01-*` (или добавить недостающие `42-y2k-*.jpg` генерацией, сейчас `42-y2k-01.jpg` отсутствует — `ls` показал `42-memphis-*.jpg` вместо y2k jpg, хотя `42-y2k-01-800.webp` есть). Унифицировать: 4 стиля × 2 файла = 8, обновить чек-лист с 6→8. Удалить или лениво подгружать `ARCHIVE_42` (210 моков) — заменить на `MOCK_POOL` из реальных 8 или генерировать `archive-*.jpg` батчем через `sharp`/`bun`.
2. Вынести баланс/историю игр из `localStorage` в `POST /magnum/api/coins` + `GET /magnum/api/me` (Neon `magnum_coins` already exists) — в `Blackjack/Roulette/Game2042` заменить `useState(()=>localStorage)` на `useEffect(fetch /api/me)` + `fetch /api/coins {delta}`. Добавить `drizzle 0010` таблицу `game_scores` если нужно хранить рекорды. `PromoPopup`/`HomePage` баннер — в cookie или `magnum_sessions` meta. После этого `grep -r localStorage src/` должен вернуть 0 (кроме комментов). Тест `gallery-real.test.ts` уже ждёт 4 сета — дополнить проверкой `style→filename` соответствия.

## Доп. наблюдения

- `systemd magnum-bun` рестарт 14:42:39 — 8 мин назад, стабилен. `magnum-caddy` Up ~1h, порты 30645-30646 проброшены.
- `git log` — 9a2c9f4 rhythm FEVER, 3009→3017 тестов, tsc чист.
- Рекомендация watchdog: обновить чек-пункт 6 с «6 файлов» на «8 файлов (4× jpg + 4× webp)» чтобы не триггерить ложный WARN.

---
*Watchdog cron 1/10 • 2026-09-01 14:51 UTC • /root/magnum-album • auto-push*
