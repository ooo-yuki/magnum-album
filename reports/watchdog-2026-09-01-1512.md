# Watchdog 1/10 — 2026-09-01 15:12 UTC

## Health

| # | Проверка | Ожидаемо | Факт | Статус |
|---|----------|----------|------|--------|
| 1 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | 200 (HTML `MAGNUM — 5opka | Новый альбом 2026`) | OK |
| 2 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | 200 JSON (`ideas:[{id:65 votes:142 approved}, 75 total]`) | OK |
| 3 | `curl -s http://127.0.0.1:3000/magnum/api/ideas` | 200 | 200 (идентичный JSON — Bun-Caddy связность OK) | OK |
| 4 | `systemctl is-active magnum-bun` | active | active (running since 15:12:41 UTC PID 240034, Memory 19.6M) | OK |
| 5 | `docker ps | grep magnum-caddy` | Up | Up (25f92f653c48 omniroute-caddy:latest, Up 12 minutes, 0.0.0.0:30645-30646) | OK |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | 8 файлов (4x jpg PNG 1536x1024 + 4x 800.webp, 12 MB: agit/cyber/memphis/y2k) | WARN +2 |
| 7 | `git status --short` | clean | clean (nothing to commit, working tree clean, branch master = origin/master) | OK |
| 8 | `bun tsc --noEmit` | 0 errors | 0 errors | OK |

**Итоговый статус: OK — все критичные сервисы UP, рестарты не требуются.**

## Коды ответов

- Внешний HTTPS `/magnum/` -> `200` (HTML, `<title>MAGNUM — 5opka | Новый альбом 2026</title>`)
- Внешний HTTPS `/magnum/api/ideas` -> `200` (JSON, top id:65 `Freakland Create gen3 — 130 novykh pereskazov` votes:142, status approved)
- Локальный HTTP `:3000/magnum/api/ideas` -> `200` (идентичный JSON, связность подтверждена)
- `systemctl` -> `active`, `docker` -> `Up 12 minutes`, галерея 8/6, git clean

## Рестарты

| Действие | Время | Причина | Результат |
|----------|-------|---------|-----------|
| `systemctl restart magnum-bun` | — | все 200/active — не требовался | пропущен |
| `docker compose up -d` (/opt/magnum-caddy) | — | `magnum-caddy` Up 12 min — здоров | пропущен |
| Повторная проверка | — | первичные 200 — не нужна | пропущена |

> Примечание: `magnum-bun` uptime 25s (restart в 15:12:41) — вероятно рестарт предыдущего цикла watchdog 15:06; текущий цикл застал уже здоровый сервис.

## Галерея детально

```
42-agit-01-800.webp   119K  RIFF WebP 800x533
42-agit-01.jpg        3.4M  PNG 1536x1024
42-cyber-01-800.webp  133K  RIFF WebP 800x800
42-cyber-01.jpg       2.9M  PNG 1536x1024
42-memphis-01-800.webp 77K  RIFF WebP 800x533
42-memphis-01.jpg     2.4M  PNG 1536x1024
42-y2k-01-800.webp    67K  RIFF WebP 800x533
42-y2k-01.jpg         2.4M  PNG 1536x1024
```

Итого 8 файлов — чек-лист ожидает 6, фактически 4 стиля x 2 формата = 8 — корректно. `file` показывает что `.jpg` фактически PNG 1536x1024 (расширение вводит в заблуждение, но `img src` резолвится). `dist/` собран. 404 нет.

## Найденные баги (файл:строка)

### BUG-1 — Рассинхрон `REAL_BY_STYLE` Y2K между `GalleryPage.tsx` и `galleryTokens.ts` — `src/lib/galleryTokens.ts:42` vs `src/pages/GalleryPage.tsx:32`

- **Файл:** `src/lib/galleryTokens.ts:40-44` vs `src/pages/GalleryPage.tsx:30-34`
  ```ts
  // GalleryPage.tsx:32 — канон
  "Y2K": "/magnum/images/gallery-42/42-y2k-01-800.webp",
  // galleryTokens.ts:42 — рассинхрон
  "Y2K": "/magnum/images/gallery-42/42-memphis-01-800.webp", // должен быть 42-y2k-01-800.webp
  ```
  `REAL_BY_STYLE` в GalleryPage указывает Y2K->`42-y2k-01-800.webp` (67K, корректно), а экспорт `REAL_BY_STYLE_FALLBACK` в `galleryTokens.ts` мапит Y2K->`42-memphis-01-800.webp` (77K, мемфис). Любой импорт `getRealSrc` из `galleryTokens.ts` (рефактор P2-2) покажет мемфис-картинку для Y2K-фильтра — визуальный баг, фильтр Y2K отдаёт мемфис.
  Дополнительно `GalleryPage.tsx:39-43` `REAL_FALLBACK` мапит `y2k-01->memphis`, `y2k-02->memphis.jpg`, `y2k-03->cyber.jpg` — но это частично маскируется `getRealSrc(style,src)` (строка 785) который переопределяет по `style`, так что BASE_ARTS показывается верно, а напрямую через `REAL_FALLBACK["y2k-01"]` — нет.
- **Риск:** P2 — визуальная несогласованность, импорты из `galleryTokens.ts` ломают Y2K; ревьюер gallery-404 поймает рассинхрон.
- **Фикс:** `galleryTokens.ts:42` заменить на `42-y2k-01-800.webp`, проверить что оба файла экспортируют одинаковый маппинг, добавить тест `expect(REAL_BY_STYLE_FALLBACK["Y2K"]).toBe(REAL_BY_STYLE["Y2K"])`.

### BUG-2 — `DAILY_KEY` dead code + `restart()` ломает детерминизм daily-доски — `src/pages/games/Game2042.tsx:411` и `463-483`

- **Файл:** `src/pages/games/Game2042.tsx:411` + `463-483`
  ```ts
  // 411
  const DAILY_KEY = "2042-daily"; void DAILY_KEY; // подавляет unused, но ключ нигде не читается/пишется
  // 463
  const startDaily = useCallback(() => {
    setGrid(seededBoard(dailySeed())); // нет getItem/setItem DAILY_KEY, нет проверки лимита
    setDailyMode(true); ... setSubmitted(false);
  }, []);
  // 471
  const restart = useCallback(() => {
    if (dailyMode) { setGrid(seededBoard(dailySeed()+moves+score)); } // +moves+score ломает детерминизм
  }, [dailyMode, moves, score]);
  ```
- **Почему баг:** UI обещает "ЕЖЕДНЕВНЫЙ ЧЕЛЛЕНДЖ — одна попытка в день — топ попадёт в рейтинг MAGNUM" (`:541`), но `DAILY_KEY` нигде не `getItem/setItem` и не уходит в `magnum_daily_claims`/`magnum_game_scores`. Игрок может жать "Ежедневный челлендж" бесконечно, спамить `POST /magnum/api/games/submit` и накрутить рейтинг. `restart()` в daily-режиме делает `dailySeed()+moves+score` — каждый рестарт новая доска, теряется смысл "единой доски дня" (seed должен быть только `dailySeed()`). `void DAILY_KEY` — lint-хак, скрывающий неиспользуемую переменную, повторён из отчёта 15:06 — не пофикшен.
- **Риск:** P1 — экономика/рейтинг: накрутка daily + несоответствие спеке "1 попытка/день".
- **Связь:** `src/pages/games/Game2042.tsx:271` `historyRef` без `useState` (баг 15:06 BUG-2) также остаётся, но выбран DAILY_KEY как более критичный.

## Идея починки (1)

**P1 — Синхронизация галереи + закрытие daily-накрутки за 45 строк**

1. **Галерея:** в `src/lib/galleryTokens.ts:42` заменить `"Y2K": ".../42-memphis-01-800.webp"` -> `".../42-y2k-01-800.webp"`. Вынести `REAL_BY_STYLE` в единый источник (`galleryTokens.ts`) и импортировать в `GalleryPage.tsx` вместо дублирования. Почистить `REAL_FALLBACK:39-43` — `y2k-01->42-y2k-01-800.webp`, `y2k-02->42-y2k-01.jpg`, `y2k-03->42-y2k-01.jpg` или удалить неиспользуемый `y2k-03`. Тест: `tests/gallery-sync.test.ts` — `expect(getRealSrc("Y2K")).toBe("/magnum/images/gallery-42/42-y2k-01-800.webp")` + проверка `fs.existsSync` для всех 4 путей.
2. **Daily:** в `startDaily` читать `localStorage.getItem(DAILY_KEY)` (значение `YYYY-MM-DD`) — если совпадает с сегодня, toast "Уже играл сегодня — жди 04:02 МСК" и early return; при старте `setItem(DAILY_KEY, todayISO)`. Серверно: `POST /magnum/api/games/submit` с `daily:true` добавить guard `magnum_daily_claims` (unique user+date) -> 429 при повторе. Убрать `void DAILY_KEY`, убрать `+moves+score` из `restart` daily-ветки — только `seededBoard(dailySeed())`. Опционально добавить `useState` для `historyRef.length` (undo-счётчик).
3. **Чек-лист:** обновить watchdog чек `gallery-42/ — 6 файлов` -> `8 файлов (4 стиля x 2 формата)` чтобы убрать ложный WARN.

Оценка: ~15 строк gallery + ~25 строк daily + ~10 строк тест, без миграций, без рестарта Caddy.
