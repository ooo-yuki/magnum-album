# Watchdog 2026-09-01 14:02 UTC — ✅ OK (all 200, no restart)

**Ветка:** `master` · **HEAD:** `459c489` · **Проверок:** 8/8 · **Рестартов:** 0

## Health

| # | Проверка | Ожидается | Факт | Код/Статус |
|---|----------|-----------|------|------------|
| 1 | `curl --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | ✅ 200 | `200` `text/html` `<!DOCTYPE html>…MAGNUM — 5opka` |
| 2 | `curl --resolve … https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | ✅ 200 | `200` `application/json` `{"ideas":[{"id":65,"votes":142,…}]}` ideas>0 |
| 3 | `curl http://127.0.0.1:3000/magnum/api/ideas` | 200 | ✅ 200 | `200` JSON, совпадает с (2) — Bun.serve |
| 4 | `systemctl is-active magnum-bun` | active | ✅ active | `active (running)` since 14:02:11 UTC, PID 138375, 18.7M, `enabled` |
| 5 | `docker ps \| grep magnum-caddy` | Up | ✅ Up 36m | `431e292c2c6a omniroute-caddy:latest Up 36 minutes 0.0.0.0:30645-30646->30645-30646/tcp` |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | ✅ 6 | `42-agit-01-800.webp 119K`, `42-agit-01.jpg 3.4M`, `42-cyber-01-800.webp 133K`, `42-cyber-01.jpg 2.9M`, `42-memphis-01-800.webp 77K`, `42-memphis-01.jpg 2.4M` |
| 7 | `git status --short` | — | ⚠️ untracked 1 | `?? tests/gallery-api-games.test.ts` (master, ahead?) |
| 8 | `tsc + bun build + vitest` | 0 errors / green | ✅ | `bun build 0` (dist 27M), `vitest 2862 pass 0 fail 8444 expects` |

**Доп:** `curl …/magnum/images/gallery-42/42-agit-01-800.webp` → `200 image/webp 120902b`, `curl http://127.0.0.1:3000/magnum/` → 200, `ss *:3000 bun pid 138375`, Caddy `handle /magnum/api/* → 172.18.0.1:3000`, `handle_path /magnum/* → try_files {path} /index.html + file_server` (30645 TLS CF + 30646 http).

## Коды ответов

```
https://oooyuki.zomb.top:30645/magnum/             → 200 (TLS CF, Caddy 30645, caddy run --config)
https://oooyuki.zomb.top:30645/magnum/api/ideas    → 200 JSON {ideas:[{id:65,votes:142,approved},…]}
http://127.0.0.1:3000/magnum/api/ideas              → 200 JSON (Bun.serve :3000, идентичен TLS)
http://127.0.0.1:3000/magnum/                        → 200 text/html
/magnum/images/gallery-42/42-agit-01-800.webp       → 200 image/webp 120902 (Caddy file_server)
```

## Рестарты

| Сервис | Действие | Причина | Результат |
|--------|----------|---------|-----------|
| magnum-bun | — | не требовался (active, все 200) | — |
| magnum-caddy (docker compose) | — | не требовался (Up 36m) | — |

> Повторная проверка после рестарта не выполнялась — все пробы зелёные. Если бы не 200/active — `systemctl restart magnum-bun; cd /opt/magnum-caddy && docker compose up -d`.

## Найденные баги (файл:строка)

### 1) `src/pages/GalleryPage.tsx:182–392` — ARCHIVE_42 210× `archive-*.jpg` soft-404 через SPA fallback
`BASE_ARTS` (7) и `MOCK_POOL` (5) уже переведены на реальные файлы (`REAL_BY_STYLE`/`REAL_FALLBACK` → `42-agit-01-800.webp` и т.д.) — `curl` 3/3 `200 image/webp`. Но `ARCHIVE_42` (строка 180, 210 элементов `arch-СССР-001`…`arch-мемфис-210`) хранит `src: "/magnum/images/gallery-42/archive-*.jpg"` — файлов `archive-*` на диске нет (`ls public/images/gallery-42/` — только 6 реальных). Caddy `try_files {path} /index.html` отдаёт для них `200 text/html` (7192 байт `index.html`) вместо 404 — в `<img>` битая картинка, `onError` без заглушки, кириллица в пути (`archive-СССР-…`) риск double-encode. `curl …/archive-СССР-001.jpg -i` покажет `content-type: text/html`.

### 2) `server.ts:267–278` — `handleIdeasVote` без авторизации, спам голосов
`handleIdeasPost` (строка 241) требует `extractToken` + `getUserByToken` → 401, а `handleIdeasVote(req, idStr)` (строка 267) делает `UPDATE magnum_ideas SET votes = votes+1 WHERE id=${id}` без проверки токена. Любой аноним может `POST /magnum/api/ideas/:id/vote` в цикле накрутить `votes` до ∞, обходя экономику `coins→votes`. Таблица `magnum_ideas` без колонки `voted_by`, rate-limit отсутствует. Рядом `SHOP_PRICES`/`coins` требуют auth (строки 316+), идеи — нет.

## Идея починки (1)

**Галерея + API — один патч закрывает обе дыры.** 
1. Галерея: в `src/pages/GalleryPage.tsx:180` заменить ручной `ARCHIVE_42` на циклический маппинг `Array.from({length:210},(_,i)=>{ const s=(["СССР","Y2K","киберпанк","мемфис"] as Style42[])[i%4]; return {id:`arch-${s}-${String(i+1).padStart(3,"0")}`,style:s,src:REAL_BY_STYLE[s],gradient:STYLE_META[s].accent,…}})` — 0 битых путей, все 210 артов показывают реальные 3 webp/jpg. В рендере `<img loading="lazy" onError={e=>{e.currentTarget.style.display="none"; (e.currentTarget.nextElementSibling as HTMLElement)?.classList.remove("hidden")}}>` + `gradient+emoji` заглушка.
2. Caddy: вынести `handle /magnum/images/* { root * /srv/magnum; file_server }` **без** `try_files /index.html` (в `/opt/magnum-caddy/Caddyfile:8–12`) — честный `404` для отсутствующих файлов, ловится тестами `gallery-real`.
3. Голоса: в `server.ts:267` добавить `const token=extractToken(req); if(!token) return 401; const user=await getUserByToken(token); if(!user) return 401;` + `INSERT INTO magnum_idea_votes(user_id,idea_id) VALUES … ON CONFLICT DO NOTHING` и `WHERE NOT EXISTS`, или хотя бы `rateLimit: 1 vote / user / idea / 24h` + `balance -= 1 coin` для связи с экономикой.

## Доп. наблюдения

- `vitest 2862/2862 ✅` (14 файлов), `bun build` 85/94 ok, `tsc` 0 errors, `dist 27M`, `sitemap 32 urls`.
- `magnum-bun` рестартовался в 14:02:11 (PID 138375) — предыдущие рестарты 13:54:02, 13:55:08 по journalctl; сейчас стабилен.
- `git status` теперь чище: только `?? tests/gallery-api-games.test.ts` (остальные dirty из 13:51 уже закоммичены в `459c489`).
- Требуется `hype`/`review` не блокируют watchdog — 0 рестартов.

---
*watchdog cron 1/10 · 2026-09-01 14:02 UTC · GalleryPage 2570 строк, 210 archive soft-404 + vote auth bypass · рестартов 0*
