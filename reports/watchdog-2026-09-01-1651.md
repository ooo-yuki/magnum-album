# Watchdog 2026-09-01 16:51 UTC — ✅ OK (200×3, active, Up, галерея 9/6 drift без рестарта)

**Ветка:** `master` · **HEAD:** `621ff37` · **Проверок:** 8/8 · **Рестартов:** 0 · **Cron:** `1/10` каждые 5 мин (watchdog MAGNUM 1/10)

## Health

| # | Проверка | Ожидается | Факт | Код/Статус |
|---|----------|-----------|------|------------|
| 1 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | ✅ 200 | `200` `text/html` `~9.9KB` `<!DOCTYPE html> MAGNUM — 5opka` `TLS Caddy 30645 --resolve 127.0.0.1` |
| 2 | `curl -sk --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | ✅ 200 | `200` `application/json` `{"ideas":[...104 total]}` `id:65 votes:142 approved` `id:55 "латунный завод"` `~48KB` JSON valid |
| 3 | `curl -s http://127.0.0.1:3000/magnum/api/ideas` | 200 | ✅ 200 | `200` JSON, идентичен (2) — `Bun.serve :3000` прямой `id:65` |
| 4 | `systemctl is-active magnum-bun` | active | ✅ active | `active (running)` since `2026-09-01 16:49:28 UTC` PID `335979` → `336129` after auto-restart `enabled` `Memory 22M` `Tasks 9` `Uptime 68s` |
| 5 | `docker ps \| grep magnum-caddy` | Up | ✅ Up | `magnum-caddy` `omniroute-caddy:latest` `Up 2 hours` `0.0.0.0:30645-30646->30645-30646/tcp` `25f92f653c48` |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | ⚠️ 9 (факт) | `42-agit-01.jpg 3.4M + 800.webp 119K`, `42-cyber-01.jpg 2.9M + 800.webp 133K`, `42-memphis-01.jpg 2.4M + 800.webp 77K`, `42-y2k-01.jpg 2.4M + 800.webp 67K`, `42-y2k-02-800.webp 67K` orphan — всего 9 (4×jpg + 5×webp) `12M` `dist` и `/srv/magnum` синхронны `16:49` |
| 7 | `git status --short` | — | ⚠️ dirty | `M tests/gallery-gsap-build-1421.test.ts` `M tests/new-coverage-1410.test.ts` `M tests/new-coverage-1502.test.ts` `M tests/new-coverage-1550.test.ts` `M tests/new-coverage-1607.test.ts` `M tests/new-coverage-1625.test.ts` — 6 файлов, `master` `621ff37 feat(games): BACKLOG P0#2…` |
| 8 | `curl /magnum/api/health` (доп) | 200 | ✅ 200 + warnings | `{"ok":true counts:{users:26 coins:25 mining:11 presave:7 ideas:104 transactions:3} warnings:["mining_exchanges: drift","idea_comments: drift","reports: drift","moderation_log: drift"] uptime:68s}` — 4 drift, без `follows/aiUsage` (починены) |

**Доп:** `journalctl -u magnum-bun -n 5` `16:50:12 NeonDbError relation "magnum_moderation_log" does not exist code:42P01 file:parse_relation.c line:1498 routine:parserOpenTable` — 4 таблицы из `healthWarnings` падают, но health возвращает `ok:true` (маскировка). `ss *:3000 LISTEN bun`. Caddy `30645 TLS + :30646 HTTP` оба `reverse_proxy 172.18.0.1:3000`. `--resolve 127.0.0.1` подтверждает терминацию без внешнего DNS.

## Коды ответов

```
https://oooyuki.zomb.top:30645/magnum/             → 200 text/html ~9.9KB TLS Caddy 30645 --resolve 127.0.0.1 Bun.serve прокси
https://oooyuki.zomb.top:30645/magnum/api/ideas    → 200 application/json {"ideas":[{id:65 votes:142 approved "Freakland Create gen3 130"}…104 total]} ~48KB JSON valid
http://127.0.0.1:3000/magnum/api/ideas              → 200 application/json (Bun.serve :3000 прямой, идентичен TLS, head id:65)
http://127.0.0.1:3000/magnum/api/health             → 200 application/json {"ok":true counts:{users:26 coins:25 mining:11 presave:7 ideas:104 follows:0 aiUsage:0} warnings:4 uptime:68s}
systemctl is-active magnum-bun                     → active (running) since 2026-09-01 16:49:28 UTC PID 336129 enabled
docker ps magnum-caddy                             → Up 2 hours omniroute-caddy:latest 25f92f653c48 0.0.0.0:30645-30646->30645-30646/tcp
ls public/images/gallery-42/                       → 9 файлов (4×jpg + 5×webp) base: 42-agit-01/42-cyber-01/42-memphis-01/42-y2k-01 + orphan 42-y2k-02-800.webp 67K
git status --short                                 → M 6 tests (gallery/new-coverage) — не коммичены, master 621ff37
```

## Рестарты

| Сервис | Действие | Причина | Результат |
|--------|----------|---------|-----------|
| magnum-bun | — | не требовался (1-3 все 200, 4 active, health 200) | — |
| magnum-caddy (docker compose) | — | не требовался (Up 2 hours, 30645 LISTEN, Caddyfile unchanged `/opt/magnum-caddy/Caddyfile` → `reverse_proxy 172.18.0.1:3000`) | — |

> Критерий рестарта: любой не-200 или не-active → `systemctl restart magnum-bun; cd /opt/magnum-caddy && docker compose up -d; sleep 3; curl --resolve повтор`. Не выполнялся — все пробы зелёные. `journalctl` 42P01 по 4 таблицам не триггерит рестарт т.к. health ловит `catch` и отдаёт `ok:true` + `warnings` (см. баг 1). Галерея 9≠6 — drift, не рестарт (статические файлы, не прокси).

## Найденные баги (файл:строка)

### 1) `server.ts:2164-2168` + `drizzle/schema.ts:202,211,219,230` + `drizzle/migrations/0012_mining_exchange_comments.sql` `0013_reports_moderation.sql` — дрифт 4 таблиц, health маскирует 200 (P0, эволюция 16:43)

```ts
// server.ts:2164-2168 — health, строки 2164-2168 (смещение после правок 621ff37)
try { const r = await sql`SELECT count(*)::int as c FROM magnum_mining_exchanges`; exchangesCount = Number((r[0] as {c:number}).c); } catch (e) { console.error("[health] mining_exchanges count failed", e); healthWarnings.push("mining_exchanges: drift"); }
try { const r = await sql`SELECT count(*)::int as c FROM magnum_idea_comments`; commentsCount = Number((r[0] as {c:number}).c); } catch (e) { console.error("[health] idea_comments count failed", e); healthWarnings.push("idea_comments: drift"); }
try { const r = await sql`SELECT count(*)::int as c FROM magnum_reports`; reportsCount = Number((r[0] as {c:number}).c); } catch (e) { console.error("[health] reports count failed", e); healthWarnings.push("reports: drift"); }
try { const r = await sql`SELECT count(*)::int as c FROM magnum_moderation_log`; modLogCount = Number((r[0] as {c:number}).c); } catch (e) { console.error("[health] mod_log count failed", e); healthWarnings.push("moderation_log: drift"); }
// server.ts:2169-2170 — теперь OK (починены 621ff37):
try { const r = await sql`SELECT count(*)::int as c FROM magnum_follows`; followsCount = Number((r[0] as {c:number}).c); } catch (e) { healthWarnings.push("magnum_follows 42P01 drift"); }
try { const r = await sql`SELECT count(*)::int as c FROM magnum_ai_usage`; aiUsageCount = Number((r[0] as {c:number}).c); } catch (e) { healthWarnings.push("magnum_ai_usage 42P01 drift"); }
```

`journalctl -u magnum-bun --no-pager -n 10` на `16:50:12 UTC` (PID 336129):

```
NeonDbError: relation "magnum_moderation_log" does not exist  code:"42P01" file:"parse_relation.c" line:"1498" routine:"parserOpenTable"
  at Y (/root/magnum-album/node_modules/@neondatabase/serverless/index.mjs:1298:14)
```

`GET /magnum/api/health` `16:50:31Z` и `16:50:47Z` (два запроса подряд):

```json
{"ok":true,"counts":{"users":26,"coins":25,"mining":11,"presave":7,"ideas":104,"exchanges":0,"ideaComments":0,"reports":0,"moderationLog":0,"follows":0,"aiUsage":0},"warnings":["mining_exchanges: drift","idea_comments: drift","reports: drift","moderation_log: drift"]}
```

`drizzle/schema.ts:202` (`magnumMiningExchanges`) `:211` (`magnumIdeaComments`) `:219` (`magnumReports`) `:230` (`magnumModerationLog`) описаны, миграции `0012`, `0013` лежат в репо, но в прод Neon (`DATABASE_URL` us-east-2 `proud-bar-62331523`) не применены — health глушит `catch` и возвращает `exchanges:0 ideaComments:0 reports:0 moderationLog:0` с `ok:true` + `warnings`, маскируя деградацию. `follows`/`aiUsage` починены коммитом `621ff37` (warnings исчезли), но 4 старых drift остались — каждый `POST /magnum/api/mining/exchange` (`server.ts:1183-1202` создаёт таблицу `CREATE TABLE IF NOT EXISTS magnum_mining_exchanges`), `POST /magnum/api/ideas/:id/comments` (`server.ts:1210`) и модерация `server.ts:1924,1937,1970,1976` упадёт или создаст таблицу лениво, гонка. `chat_messages` (`server.ts:2168`) тоже try/catch но сейчас без ошибки — не в warnings. Фикс см. идею.

### 2) `public/images/gallery-42/42-y2k-02-800.webp:1` + `src/pages/GalleryPage.tsx:45` + `src/lib/galleryTokens.ts:42` — orphan webp, галерея 9≠6 и y2k-02 никогда не отдаётся (P1)

```ts
// src/pages/GalleryPage.tsx:45 — y2k-02 мапится на y2k-01, а не на свой файл
  "y2k-02": "/magnum/images/gallery-42/42-y2k-01-800.webp",  // ← должен быть 42-y2k-02-800.webp
// src/pages/GalleryPage.tsx:44-48 — все y2k дублируют один webp
  "y2k-01": "/magnum/images/gallery-42/42-y2k-01-800.webp",
  "y2k-02": "/magnum/images/gallery-42/42-y2k-01-800.webp",
  "y2k-03": "/magnum/images/gallery-42/42-y2k-01-800.webp",
// src/lib/galleryTokens.ts:42 — канонический REAL_BY_STYLE тоже только 4 ключа, без y2k-02
  "Y2K": "/magnum/images/gallery-42/42-y2k-01-800.webp",
// public/images/gallery-42/ — факт 9 файлов (16:49):
// 42-agit-01.jpg 3.4M + 42-agit-01-800.webp 119K
// 42-cyber-01.jpg 2.9M + 42-cyber-01-800.webp 133K
// 42-memphis-01.jpg 2.4M + 42-memphis-01-800.webp 77K
// 42-y2k-01.jpg 2.4M + 42-y2k-01-800.webp 67K
// 42-y2k-02-800.webp 67K ← orphan, создан 16:49, ни один src не ссылается
```

`ls -1 public/images/gallery-42/` → 9 строк, `ls -1 /srv/magnum/images/gallery-42/` и `dist/images/gallery-42/` — тоже 9 (синхронны). Спека watchdog `ls -lh public/images/gallery-42/ — 6 файлов` ожидает 6 — проверка вечно ⚠️. Реально нужно 8 (4 стиля ×2 jpg+webp) или 5 webp-only. Файл `42-y2k-02-800.webp` (67K, `2026-09-01 16:49`) скопирован в `dist` и `/srv/magnum` но никогда не грузится: `GalleryPage.tsx:45` и `lib/galleryTokens.ts:42` + все `arch-Y2K-*` (`:187,191,195…239`) указывают на `42-y2k-01-800.webp`. `tests/new-coverage-1625.test.ts:26` и `gallery-gsap-build-1421.test.ts` ожидают `y2k-02` id но src у него тот же. Итог: лишний файл жрёт иноды/сборку, LCP не улучшает, 404-тест `server.ts:2675` (`/magnum/images/* must return 404 not SPA fallback`) не ловит orphan т.к. файл существует, но логически — мёртвый актив. Дополнительно `src/pages/games/RouletteGame.tsx:96,99,103,110,111,363` + `BlackjackGame.tsx:221,224,253,549` + `Game2042.tsx:255,256,286` + 9 других игр держат `localStorage` для баланса/best вопреки "без localStorage" (архитектура Neon-only) — см. `server.ts:5` комментарий — но главный файл:строка для отчёта — галерея orphan.

## Идея починки (1)

**Миграционный health-gate + галерея orphan + LS-выпил за 15 мин (закрывает оба бага без рестарта Caddy):** 1) Применить дрифт 4 таблиц: `cd /root/magnum-album && psql $DATABASE_URL_UNPOOLED -f drizzle/migrations/0012_mining_exchange_comments.sql -f drizzle/migrations/0013_reports_moderation.sql` (или `DATABASE_URL=$DATABASE_URL_UNPOOLED bunx drizzle-kit push --force`), проверить `psql -c "SELECT to_regclass('public.magnum_mining_exchanges'), to_regclass('public.magnum_idea_comments'), to_regclass('public.magnum_reports'), to_regclass('public.magnum_moderation_log')"` без null и `curl -s http://127.0.0.1:3000/magnum/api/health | jq .warnings` должен стать `[]` и `counts.exchanges|ideaComments|reports|moderationLog` без 0-drift, затем `systemctl restart magnum-bun`. Патч `server.ts:2164-2171`: заменить `healthWarnings.push("drift")`+`ok:true` на `return Response.json({ok:false, missing:[...], warnings:healthWarnings},{status:500})` если warnings непуст, чтобы watchdog триггерил рестарт/алерт, а не маскировал. Добавить post-deploy `SELECT`-чек в `.github/workflows` после `bun run build.ts`. 2) Галерея: либо `src/pages/GalleryPage.tsx:45` → `"/magnum/images/gallery-42/42-y2k-02-800.webp"` и `src/lib/galleryTokens.ts:42` добавить `"Y2K-02": "...y2k-02..."` + расширить `REAL_FALLBACK`, либо удалить orphan `rm public/images/gallery-42/42-y2k-02-800.webp && rm dist/images/gallery-42/42-y2k-02-800.webp && rm /srv/magnum/images/gallery-42/42-y2k-02-800.webp` и поправить watchdog-спеку `6 файлов` → `8` (4×jpg+4×webp) или webp-only `4`. 3) Заодно `src/pages/games/RouletteGame.tsx:96,110,111,363` `BlackjackGame.tsx:221,253` `Game2042.tsx:255,286` — удалить `localStorage` и заменить на `fetch("/magnum/api/games/submit",{credentials:"include"})` + `GET /magnum/api/balance` / `magnum_game_scores` (как уже сделано для `AiBot.tsx`/`PromoPopup.tsx` в `621ff37`). Это уберёт тихий 0-count на mining/comments/reports/moderation, ложный ⚠️ галереи и нарушение "no localStorage" в играх.

---
*Teams: 8 curl/systemd/docker/ls/git/health · филиал 42, MAGNUM 5opka — проверка 1/10 завершена.*
