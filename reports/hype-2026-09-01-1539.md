# HYPE 2026-09-01 15:39 — MAGNUM 3/10

> Хайп-генератор · бэклог 4 фичи · 3 новые идеи · Neon OK · очередь +5

## POST /magnum/api/ideas

Авторизован как `hypebot42` (id 20). 3 идеи успешно созданы в Neon `magnum_ideas`:

- id 110 `DUEL RUSH 42 — WS 2-4 + rush x7 + heat + ELO` → 201
- id 111 `ECO KEDR 42 — 8Q Кедр/Томь/Кузбасс + bio 7дн` → 201
- id 112 `CHROME VAULT 42 — 12 скинов chrome + prism epic` → 201

`curl -s http://localhost:3000/magnum/api/ideas | jq .ideas[-3:]` подтверждает. Всего идей в БД: 92.

---

## Новые идеи (3)

| # | идея | приоритет | монетки | спека (3 строки) |
|---|------|-----------|---------|-------------------|
| 1 | **DUEL RUSH 42 — WS 2-4 + rush x7 + heat-bar + ELO** | P0 ⚔️ | wager 0/42/142/420, win `+wager*2` +42 ELO, lose −12, draw возврат, топ-3 +1420 + crown | WS арена 2-4 на `Bun.serve ws /magnum/api/ws`: `lobby:create`→ABCD→`join`→`ready`→10с rush-дуэль, rush <0.25с +12% на клик капа x7, heat-bar `0→100%` + `pulse 0.3s` на rush≥3, CPS>20 suspect бан + throttle 30/сек, heartbeat 25с + реконнект + fallback hot-seat. Сезон 7дн `magnum_leaderboard(game=duel42)` топ-3 `conic-gold + crown`. |
| 2 | **ECO KEDR 42 — 8Q Кедр/Тайга/Томь/Кузбасс + bio 7дн** | P1 🌱 | 42 Нормис 0-3 / 142 Братуха 4-6 / 420 Легенда 7-8 (1×/сутки) +1420 босс 8/8, freeze 420 | 8Q про Кедр/Тайгу/Томь/Кузбасс/42+эко + факты MAGNUM 8K/200K VPN 28.04 CLAY РЗТ73 NOVA РЗТ80/XXL86, экран `n/8` + «А знаешь ли…» + conic бейдж. Недельный bio-стрик 7дн `magnum-eco-challenge:{weekId,streak}` пропуск→сгорает freeze 420 1×/нед, босс-вс 8/8. Шаринг OG 1080×1080 canvas + Web Share API +42. |
| 3 | **CHROME VAULT 42 — 12 скинов chrome + prism epic** | P0 🛒 | common 42 / uncommon 142 / rare 420 / epic 1420, крафт 42, разбор +100 dust | Vault 12 chrome: common 42 `42-chrome/neophyte-steel/fence-raw` #e8e8e8, uncommon 142 `clay-forge/tusa-aqua/coal-ash` #8b4513, rare 420 `meduza-prism/vpn-chrome/nova-pulse` conic-prism, epic 1420 `gold-chrome-spin 3s/diamond-anod/rgb-prism` conic-spin. Покупка `POST /magnum/api/shop/cosmetic/buy` guard `getCoins()>=price` → `magnum_cosmetics` + `magnum_shop_inventory`. Крафт 3×common→uncommon fee 42, разбор rare→dust. |

### Детализация

#### 1. DUEL RUSH 42 — WS 2-4 + rush x7 + heat-bar + ELO
- **Описание:** Закрывает backlog #1 + rush-механика (третья вариация blitz/chain/rush). Хост «Создать арену» → `ws.send({type:"lobby:create",wager})` → сервер `room:ABCD` + `ws.subscribe('room:ABCD')` + `roomPublic()` → до 3 братух `join ABCD` → `ready` → авто-старт 10с rush-дуэль. Ставка 0/42/142/420 из `magnum-coins` (`getCoins()>=wager` guard, `addCoins(-wager)` atomic перед WS). Игра: полноэкранный кликер 10с, счёт `raw clicks` + rush: интервал <0.25с → цепочка, каждый следующий +12% к score (1.12→1.24→1.36→1.48→1.60→1.72→1.84 капа x7), heat-bar `width rush/7*100%` + `pulse` на rush≥3, индикатор `RUSH xN` burst. Анти-чит: `CPS>20` или `>150 clicks/10с` → `suspect:true` + бан раунда + не пишет в `magnum_leaderboard`, тост «братуха, ты робот? 🤖». Результат: сервер `broadcast {type:"finish", scores}` → `winner +wager*2 +42 ELO`, `loser −12`, draw возврат. Сезон 7дн `magnum_leaderboard(game=duel42)` Neon + `magnum-duel-history` LS 20 записей, топ-3 `conic-gold + crown 👑`.
- **Цена:** `0` френдли или `42/142/420`; победитель `+wager*2` + `+42 ELO` + `+42 dust` при rush≥4, лузер `−12 ELO`, draw возврат ставок. Топ-3 сезона `+1420` + лимитка `rush-crown-42` + `conic-gold` рамка на неделю. Rush-бонус только score→ELO, не монеты напрямую.
- **GSAP:** `gsap.context` `LobbyGrid` stagger `y:24→0 0.12 power2.out`, `RushStage` `shake x:±6 0.08s` на каждый клик + `scale 1→1.15 0.1`, rush `RUSH xN` `scale 0→1.4→1 back.out(1.7)` + `y:-12` burst + `heat-bar width 0→rush/7*100% 0.3` + `navigator.vibrate(12+N*6)`, `Transit 3-2-1` `scale 2→1 + opacity 1→0 0.5`, конфетти 160 частиц canvas `particle(x,y,vx,vy,life)` + `requestAnimationFrame`, `Leaderboard` `stagger y:10 0.05`, `prefers-reduced-motion` → `animation:none` + `transform:none`.

#### 2. ECO KEDR 42 — 8Q Кедр/Тайга/Томь/Кузбасс + bio 7дн + freeze
- **Описание:** Закрывает backlog #2 + кедровый био-челлендж (вариация TOME/TAYGA — фокус Кедр). База — `EcoQuizPage` 8Q (Кедр/Тайга/Томь/Кузбасс/42/эко) + факты MAGNUM (ТУСА МЕДУЗА 14.08 — 8K клипов/200K просмотров, VPN 28.04 в чартах, CLAY 03.04 РЗТ73, SUPER PUPER NOVA РЗТ80/XXL86). UI: прогресс `n/8` + `progress width n/8*100%` + карточка «А знаешь ли ты…» под вопросом (пропаганда: «Кедр — сердце Тайги, 42 братухи сажают кедры Кузбасса»). Результат: уровни Нормис 0-3 / Братуха 4-6 / Легенда 7-8 → `conic-gradient` бейдж + `addCoins(42/142/420)` + шаринг `Я — Легенда Кедра 42!` + график `magnum-eco-history` 10 попыток. Челлендж 7дн: LS `magnum-eco-challenge:{weekId:YYYY-Www, streak, lastDate}` — каждый день 1 задание (раздельный сбор, берег Томи, кедровая аллея Кемерово) + факт MAGNUM, пропуск→стрик сгорает, заморозка `🧊 420` 1×/нед (`magnum-eco-freeze-used` guard weekId). Финал воскресенье — босс-квиз 8Q → `streak*42+420` если `7/7` + `+1420` за `8/8` босс + OG шаринг 1080×1080 canvas (градиент + стата + `toDataURL`) + Web Share API + `magnum-share-claimed` +42 1×.
- **Цена:** `42` (Нормис) / `142` (Братуха) / `420` (Легенда) — 1×/сутки guard `lastDate`, `1420` за босс `8/8` + бейдж `Эко-Кедр 42` + `conic-gold` рамка. Freeze `420`, шаринг `+42`.
- **GSAP:** `QuestionCard` stagger `y:18→0 0.1 power2.out`, `progress` tween `width 0→n/8*100% 0.4 power2.out`, `BadgeReveal` `scale 0→1 0.6 spring back.out(1.7)` + `conic spin 3s linear infinite`, `StreakCalendar` `stagger y:10 0.06`, `prefers-reduced-motion` → `opacity 0→1 0.2` без spin.

#### 3. CHROME VAULT 42 — 12 скинов chrome + prism epic + dust
- **Описание:** Закрывает backlog #3 + chrome-призм + craft. Каталог `COSMETICS_CATALOG` 12: common 42 — `42-chrome` 🤍 `linear-gradient #fff,#e8e8e8`, `neophyte-steel` 🩶 `#9ca3af→#6b7280`, `fence-raw` 🏗️ `#e5e7eb`; uncommon 142 — `clay-forge` 🧱 `#8b4513→#d2691e`, `tusa-aqua` 🪼 `#14b8a6→#06b6d4`, `coal-ash` ⛏️ `#1f2937→#4b5563`; rare 420 — `meduza-prism` 🪼 `conic-gradient prism`, `vpn-chrome` 📡 `neon #0ff glow`, `nova-pulse` 💜 `#7c3aed→#ec4899`; epic 1420 — `gold-chrome-spin` 👑 `conic-gold spin 3s`, `diamond-anod` 🏴 `#000→#333 + gloss`, `rgb-prism` 🌈 `rgb-shift 2s`. Покупка: `getCoins()>=price` guard → `addCoins(-price)` atomic + `POST /magnum/api/shop/cosmetic/buy` Neon, инвентарь `magnum_cosmetics[]` + `magnum_shop_inventory` LS + `storage` sync. Экипировка: `equip` → один `equipped=true` per `slot` (frame/banner/title), `EquippedFrame` glow `box-shadow 0 0 24 gold`. Крафт: `FORGE` 3×common→1 uncommon fee 42, `3×uncommon→rare fee 142`, разбор `rare→+100 dust` / `epic→+420 dust` (dust `magnum-forge-dust`). Cross: `verified=1` → `-42` скидка на 1 покупку/нед + glow в дуэли/эко.
- **Цена:** `common 42` · `uncommon 142` · `rare 420` · `epic 1420` (строго по спеке), крафт `42/142`, разбор `+100/+420 dust`. Старт 1000 = 23×common или 2×rare+1 uncommon. Скидка 42 за Эко-Легенда 42.
- **GSAP:** `gsap.context` `ShopGrid` stagger `y:20→0 0.08 ease power2.out`, `SkinCard` hover `scale 1.02 + glow shadow 0 0 16`, purchase `confetti 80 canvas` + `scale 0→1 spring back.out(1.7)`, `rarity badge` epic `shimmer spin 3s linear infinite` + `conic-gold`, `ForgeReveal` `scale 0→1 + rotate 5→0 0.5`, `prefers-reduced-motion` → `animation:none` + `transform:none`.

---

## Интеграция

- Файлы: `src/pages/games/DuelRush.tsx` + `src/lib/duelRush.ts` + `src/lib/ws.ts` + `server.ts:websocket lobby 2-4 rush x7`, `src/pages/EcoKedrPage.tsx` + `src/lib/ecoKedr.ts` + `src/components/EcoBadge.tsx` + `src/components/ShareCard.tsx`, `src/lib/cosmetics.ts` (CATALOG 12 chrome + FORGE) + `src/pages/ShopChromePage.tsx` + `src/components/SkinCard.tsx` + `src/components/EquippedFrame.tsx` + `src/components/ForgeModal.tsx`
- LS: `magnum-duel-history/leaderboard/rush`, `magnum-eco-challenge/history/freeze-used/share-claimed`, `magnum_cosmetics/inventory/equipped/forge-dust`, `magnum-frame-verified/frame-date`, `magnum-coins`, `magnum-arena-season`
- Neon: `magnum_cosmetics`, `magnum_shop_inventory`, `magnum_frames`, `magnum_leaderboard(game=duel42)`, `magnum_eco_challenge`, `magnum_ideas` (ids 110-112)
- Кросс: победа дуэли rush→`StreakCalendar` + `RecapQuest` + `PresaveRatingPage`, эко-Кедр Легенда→скидка 42 в forge + verified glow, покупка epic→`EquippedFrame` glow в дуэли + эко бейдж
- Rate: `ws 30/сек` throttle + `ideas:post 10/мин` + `magnum-eco 1/сутки` + `forge 20/мин`

> Следующий тик 15:53 — ротация vault + проверка голосов `magnum_ideas` (ids 110-112) + season reset 7дн.
