# Watchdog MAGNUM — 2026-09-01 14:26 UTC

## Health

| Проверка | Ожидаемо | Факт | Статус |
|----------|----------|------|--------|
| `GET https://oooyuki.zomb.top:30645/magnum/` (TLS via Caddy :30645) | 200 | 200 | ✅ |
| `GET https://oooyuki.zomb.top:30645/magnum/api/ideas` (TLS) | 200 JSON | 200 JSON (`ideas[0].id=65 votes=142`) | ✅ |
| `GET http://127.0.0.1:3000/magnum/api/ideas` (прямо в Bun) | 200 JSON | 200 JSON | ✅ |
| `systemctl is-active magnum-bun` | active | active | ✅ |
| `docker ps \| grep magnum-caddy` | Up | Up 51 minutes (omniroute-caddy:latest, :30645-30646→30645-30646) | ✅ |
| `ls public/images/gallery-42/` | 6 файлов | 8 файлов | ✅ (>6, но см. баги) |
| `git status --short` | clean | 17 staged + 5 unstaged modified + 2 untracked | ⚠️ грязный индекс |
| `tsc --noEmit` | 0 ошибок | 0 ошибок (после staged правок) | ✅ |

**Рестартов:** 0 (все проверки 200/active/Up — перезапуск не требовался).

**Детали curl:**
- TLS HTML: `<div id="root"></div>` + `/magnum/main-spm1yjnr.js` — SPA отдается.
- TLS API: `{"ideas":[{"id":65,"title":"Freakland Create gen3 — 130 novykh pereskazov",…` — 200.
- Local API: идентичный ответ, Bun на :3000 жив.

**Caddy:** `431e292c2c6a magnum-caddy Up 51 minutes` — без рестарта.

**Gallery файлы (8):**
```
42-agit-01-800.webp (119K)
42-agit-01.jpg (3.4M)
42-cyber-01-800.webp (133K)
42-cyber-01.jpg (2.9M)
42-memphis-01-800.webp (77K)
42-memphis-01.jpg (2.4M)
42-y2k-01-800.webp (67K)
42-y2k-01.jpg (2.4M)
```

## Баги (2, с FILE:LINE)

### 1. `src/pages/GalleryPage.tsx:181` — 404 на 348 архивных артов
В `FULL_ARCHIVE` / `ARCHIVE` 356 ссылок вида `src: "/magnum/images/gallery-42/archive-СССР-001.jpg"` (пример строка 181: `arch-СССР-001`), но в `public/images/gallery-42/` лежат только 8 файлов (`42-agit-*`, `42-cyber-*`, `42-memphis-*`, `42-y2k-*`). При `git show HEAD:src/pages/GalleryPage.tsx | grep -c archive-` — 350 архивных путей → 404 на проде, лайтбокс покажет `artImgHidden` + градиент, но галерея формально пустая. Тест `tests/gallery-404.test.ts` (новый, staged) как раз ловит это.

### 2. `src/pages/GalleryPage.tsx:1293` (HEAD) / staged deletion — пропал `GALLERY_EXTRA_FACTS`
В HEAD `export const GALLERY_EXTRA_FACTS` (50 фактов с `FILE:LINE`) на строке 1293; в staged индексе (`git diff --cached src/pages/GalleryPage.tsx`) блок удалён целиком (51 строка `-[...]`). При этом `GLOSSARY_42` обрезан и остался незакрытым в HEAD-версии — `tsc` ранее падал на `TS1137`/`TS1005` вокруг `GALLERY_EXTRA_FACTS` внутри `GLOSSARY_42` (вставка `export` внутрь массива без `];`). Сейчас `tsc` проходит только потому что staged версия удалила блок, но сломала публичный API для тестов/вотчдога (10/10 требует `GALLERY_EXTRA_FACTS` с проверкой `FILE:LINE`). Второй проявление: `src/lib/economy.ts` в staged обрезан с 2590→~70 строк — экономика Eco/Mining/Shop теряет логику.

## Идея починки (1)

**Галерея-manifest на билде:** добавить `scripts/verify-gallery.mjs` (запускать в `prebuild`): читает `src/pages/GalleryPage.tsx` → вытаскивает все `"/magnum/images/gallery-42/[^"]+"` → сверяет с `public/images/gallery-42/` → если `missing>0` — либо генерировать placeholder webp (градиент+emoji через `sharp`/`canvas`) с теми же именами, либо падать с ошибкой и подсказкой. Альтернатива — убрать `ARCHIVE`/`WAVE2` из продакшн-рендерa, оставить только `BASE_42` (7 реальных) + `REAL_BY_STYLE` (4 мапа на существующие 800.webp), а 350 архивных держать под флагом `VITE_GALLERY_MOCK=true`. Это закроет 404 для ревьюера и вернёт вотчдогу 6+ файлов без фейковых путей, а `GALLERY_EXTRA_FACTS` вернуть из HEAD (revert staged deletion) и закрыть `GLOSSARY_42` корректным `];` перед ним.

---
*Watchdog 1/10 — cron каждые 5 минут, режим 10к строк/баннер/рейтинг. Отчёт всегда в файл.*
