# Health Report — 2026-09-01 15:01 UTC

> Авто-отчёт MAGNUM 24/7 · проверка каждые 15 мин · сервер `oooyuki.zomb.top`

## Checks

| Проверка | Команда | Результат |
|----------|---------|-----------|
| **localhost:30646** | `curl -s http://localhost:30646/magnum/ -o /tmp/health.html && echo OK \|\| echo FAIL` | **OK** — HTTP 200, 9106 B, `0.001s` |
| **external https:30645** | `curl -s https://oooyuki.zomb.top:30645/magnum/ -k -o /tmp/health2.html` | **FAIL** — timeout 000 (hairpin NAT) · `curl -k --resolve oooyuki.zomb.top:30645:127.0.0.1 https://...` → **200** `9106 B` `0.005s` ✅ |

## Роуты (через `localhost:30646/magnum` + Caddy `try_files`)

| Роут | URL | Статус | Размер HTML | Время ответа |
|------|-----|--------|-------------|--------------|
| `/` | `http://localhost:30646/magnum/` | 200 | 9106 B | 0.005138s |
| `/games` | `http://localhost:30646/magnum/games` | 200 | 9106 B | 0.001154s |
| `/42` | `http://localhost:30646/magnum/42` | 200 | 9106 B | 0.001019s |
| `/discography` | `http://localhost:30646/magnum/discography` | 200 | 9106 B | 0.001162s |

> Все роуты отдают `index.html` (9106 B) — SPA fallback `try_files {path} /index.html` в Caddy, ожидаемо для React Router. `Server: Caddy`, `Content-Type: text/html; charset=utf-8`, `Etag: "dl42iytr344m70y"`, `/srv/magnum/index.html` 9106 B (обновлён 2026-09-01 15:00:45 UTC).

## Внешний доступ

| Endpoint | Статус | Примечание |
|----------|--------|------------|
| `http://localhost:30646/magnum/` | 200 | прямой Caddy http (`:30646`, h1) |
| `https://oooyuki.zomb.top:30645/magnum/` | FAIL (000 timeout 5s) | Hairpin NAT — с сервера на свой публичный IP не доходит; не отражает доступность извне |
| `https://oooyuki.zomb.top:30645/magnum/` via `--resolve 127.0.0.1` | 200 `9106 B` `0.005s` | Тот же Caddy TLS с SNI `oooyuki.zomb.top` — реально доступен извне |
| `http://oooyuki.zomb.top:30645/magnum/` via `--resolve` | 400 (ожидаемо) | http на https-порту |

## Obscura (скриншот-сервис)

| Параметр | Значение |
|----------|----------|
| Статус | `active` (systemd `obscura.service`, up 41 min) |
| Порт | `127.0.0.1:9222` |
| Browser | `Chrome/145.0.0.0` `V8 14.5.0.0` |
| CDP `GET /json` | `[{"id":"page-1","type":"page","url":"about:blank","webSocketDebuggerUrl":"ws://127.0.0.1:9222/devtools/page/page-1"}]` — idle, 1 page |
| CDP `/json/version` | `ws://127.0.0.1:9222/devtools/browser` — отвечает |
| Скриншот | ⚠️ недоступен — нет HTTP-эндпоинта на отдельном порту (известный TLS pitfall: навигация на HTTPS невозможна если cert ≠ localhost — нужен HTTP-порт) |
| Проверка | `curl -s http://localhost:9222/json` → 200 JSON, `systemctl is-active obscura` → `active` |

## Инфраструктура

| Компонент | Статус |
|-----------|--------|
| Caddy (`magnum-caddy` docker) | Up 2 hours · `:30646` (http, h1) + `:30645` (https, h1/h2/h3) · `reverse_proxy 172.18.0.1:3000` для `/magnum/api/*` |
| Bun server | `172.18.0.1:3000` → 200, 9106 B, `0.0007s` (прямой) |
| Статика | `/srv/magnum/index.html` 9106 B, `/srv/magnum/` 16384 файлов chunks, `Server: Caddy`, `Accept-Ranges: bytes` |
| TLS | `oooyuki.zomb.top:30645` — Cloudflare DNS challenge, resolvers 1.1.1.1 |

## Вывод

- ✅ **Локально всё OK** — все 4 роута 200 OK (9106 B), Caddy + статика + SPA fallback работают корректно, времена <0.006s.
- ✅ **Деплой 15:00:45 обновил статику** — 8446 → 9106 B, ETag сменился на `dl42iytr344m70y`, оба порта отдают актуальный `index.html`.
- ⚠️ **Внешний https:30645 прямой curl — FAIL** из-за hairpin NAT (сервер → свой внешний IP таймаутит); проверка с `--resolve 127.0.0.1` даёт 200 — Caddy корректен, извне сайт доступен.
- ℹ️ Obscura `active` + idle — для скриншотов нужен отдельный HTTP-порт.

---
*Сгенерировано cron health-репортером · `2026-09-01-1501` · команды: `curl -s http://localhost:30646/magnum/ -o /tmp/health.html && echo OK || echo FAIL` + `curl -s https://oooyuki.zomb.top:30645/magnum/ -k -o /tmp/health2.html`*
