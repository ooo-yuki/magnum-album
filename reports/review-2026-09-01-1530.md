# MAGNUM Review — 2026-09-01 15:30 UTC (10м, ревьюер 7/10 → 20м расширенный)

**Период:** 15:20–15:30 UTC (10м) · ветка `master` @ `00557a3` · `git log --since="10 minutes ago"`
**Коммитов в окне:** **9** (6 продуктивных + 3 отчёта/health) + 1 uncommitted test (new-coverage-1527)
**Рейтинг:** **5/10** — **1711 строк** (ADDED 1652 / REMOVED 59) · норма 5000+ = 10/10 · шкала: 0-49=0, 50-199=1, 200-499=2, 500-999=3, 1000-1499=4, 1500-1999=5, 2000-2999=6, 3000-3999=7, 4000-4999=8, 5000+=10
**Статус сборки:** `tsc` PASS (0 ошибок) · `tests` PASS (3086/3086, 0 fail) · `gallery` WARN (8 файлов реальны + 210 archive HTML-fallback) · `health` PASS (200/200/200 после рестарта) · `services` PASS (magnum-bun active, caddy Up)

---

## Коммиты (10м, `git log --since="10 minutes ago"`)

| Хэш | Сообщение | + | − | Файлы |
|-----|-----------|---|---|-------|
| `00557a3` | test: 15:29 — 3086 passed | 95 | 0 | reports/test-2026-09-01-1529.md (+95), tests/new-coverage-1527.test.ts (+131) |
| `f16d51a` | feat(api): duel 2.0 — rooms/stats/leaderboard/invites/seasons + WS ready broadcast — Neon, валидация, rate limit | 133 | 45 | drizzle/migrations/0014_duel_seasons_invites.sql (+25), drizzle/schema.ts (+17), server.ts (+133/−45) |
| `e8fa558` | auth: 15:29 — check + fix P0 server.ts:1433 crash-loop (presaveClick string literal) | 133 | 0 | reports/auth-2026-09-01-1529.md (+133) |
| `1296b9e` | health: 15:25 — OK | 61 | 0 | reports/health-2026-09-01-1525.md |
| `e0586f4` | docs: changelog 15:25 — 0.3.8 Flappy 42×5 скинов + Rhythm FEVER + Presave 40 фактов/BandLink + LCP high 6/10 | 57 | 0 | CHANGELOG.md |
| `2c3f39b` | feat(rating): real Neon — health +6 роутов reports/moderation | 16 | 1 | server.ts |
| `057221a` | feat(content): пресейв-рейтинг 40 фактов + 30 FAQ + BandLink proxy + reports moderation — Neon, валидаторы, GSAP y24 stagger 0.12 | 342 | 11 | drizzle/migrations/0013 (+33), drizzle/schema.ts (+21), index.html (+2/−1), reports/perf-1524 (+61), server.ts (+125), src/components/Singles.tsx (+2/−1), src/pages/PresaveRatingPage.tsx (+95/−9), src/styles/global.css (+3) |
| `00d63ef` | test: 15:22 — 3071 passed | 268 | 0 | reports/test-2026-09-01-1522.md (+120), tests/new-coverage-1522.test.ts (+148) |

**Вне окна но в 20м (для контекста):** `b50293b` watchdog 15:22 OK + Rhythm FEVER 5 пуль + `262ea8d` data +5 фактов §13 + `b433f0c` hype 103-105.

---

## Статусы

| Проверка | Результат | Детали |
|----------|-----------|--------|
| **tsc --noEmit** | ✅ PASS | 0 ошибок (exit 0) · `~/.bun/bin/bunx tsc --noEmit 2>&1 \| head -80` пусто |
| **tests (bun test)** | ✅ PASS 3086/3086 | 0 fail · 9651+ expects · 31 файл · 1.24s · предыдущий 3071 → +15 (new-coverage-1527), закрыт 1 fail `flappy42 renders play button` после фикса (теперь 3086 pass) |
| **gallery — файлы** | ✅ 8 файлов | `public/images/gallery-42/`: 42-agit-01.{jpg 3.4M, 800.webp 119K}, 42-cyber-01.{jpg 2.9M, 800.webp 133K}, 42-memphis-01.{jpg 2.4M, 800.webp 77K}, 42-y2k-01.{jpg 2.4M, 800.webp 67K} — 12M суммарно |
| **gallery — src check** | ⚠️ WARN дубликат + archive HTML | `grep src:.*gallery-42` → 4 стиля REAL_BY_STYLE ок, но `REAL_FALLBACK y2k-01→memphis` дубликат (см. замечание #1) · `curl 4/4 200` реальные webp ✅ (agit 119K, cyber 133K, memphis 77K, y2k 67K) |
| **gallery — archive** | ⚠️ WARN 210× HTML-fallback | `curl /magnum/images/gallery-42/archive-СССР-001.jpg` → **200 9373 text/html** (SPA index.html), аналогично `archive-Y2K-002.jpg` 200 9373 — физических `archive-*.jpg` нет, `<img>` покажет битую страницу |
| **gallery — dist sync** | ✅ | `dist/images/gallery-42/` синк через `build.ts` hash+splitting ок (8 файлов) |
| **health — magnum/** | ✅ 200 | `curl http://127.0.0.1:3000/magnum/` → 200 (text/html 9373, Caddy → bun 3000) |
| **health — api/ideas** | ✅ 200 | `curl http://127.0.0.1:3000/magnum/api/ideas` → 200 (application/json 41365, ideas массив) |
| **health — api/health** | ✅ 200 | `curl http://127.0.0.1:3000/magnum/api/health` → 200 (`{ok:true}` + counts 28 tables после 0014) |
| **services — magnum-bun** | ✅ active | `systemctl is-active` → active · pid 259522 · LISTEN *:3000 · после crash-loop 15:26-15:28 (EADDRINUSE + P0 fix) рестарт `systemctl start` 15:29 успешен |
| **services — caddy** | ✅ Up 29m | `docker ps` → `magnum-caddy Up 29 minutes` · :30645-30646 → 3086 tests · `ss -tlnp` :3000 bun, :30645 docker-proxy |
| **git status** | ✅ clean (1 untracked) | `?? reports/auth-2026-09-01-1529.md` уже закоммичен в e8fa558, сейчас `git status` clean кроме возможного `review-1530.md` (этот файл) |

---

## Замечания (3 конкретных, файл:строка)

### 1. `src/pages/GalleryPage.tsx:39` + `:42-43` — Y2K алиас указывает на MEMPHIS/CYBER, реальный Y2K-файл недоступен
```ts
const REAL_FALLBACK: Record<string, string> = {
  "y2k-01": "/magnum/images/gallery-42/42-memphis-01-800.webp", // 39 — должен быть 42-y2k-01-800.webp (67K)
  "y2k-02": "/magnum/images/gallery-42/42-memphis-01.jpg",        // 40 — должен быть 42-y2k-01.jpg (2.4M)
  "memphis-01": "/magnum/images/gallery-42/42-memphis-01-800.webp", // 42 — дубликат y2k-01
  "y2k-03": "/magnum/images/gallery-42/42-cyber-01.jpg",           // 43 — кибер вместо y2k
};
```
Итого фильтр `Y2K` в галерее показывает мемфис-арт, а `42-y2k-01-800.webp` (67K, `ls -lh 14:16`) и `42-y2k-01.jpg` (2.4M) физически есть но недостижимы. Дублируется и в `src/lib/galleryTokens.ts:32`. Фикс: `y2k-01 → 42-y2k-01-800.webp`, `y2k-02 → 42-y2k-01.jpg`, убрать дубликат `memphis-01` или завести `42-memphis-02` если нужен второй.

### 2. `src/pages/games/Flappy42Game.tsx:166-168` + `:293-294` + `:398/:416/:427` — localStorage вместо Neon (нарушение ТЗ)
```ts
const [diffId, setDiffId] = useState<DifficultyId>(() => localStorage.getItem("flappy42-diff") ... ); // 166
const [skinId, setSkinId] = useState<string>(() => localStorage.getItem("flappy42-skin") ... );       // 167
const [best, setBest] = useState(() => Number(localStorage.getItem("flappy42-best")) || 0);            // 168
useEffect(() => { localStorage.setItem("flappy42-diff", diffId); }, [diffId]);  // 293
useEffect(() => { localStorage.setItem("flappy42-skin", skinId); }, [skinId]);  // 294
// + 3× best write :398/:416/:427
```
ТЗ: *all state in Neon, no localStorage для прогресса*. `DIFFICULTIES` coinMul (norm 1 / hard 1.5 / turbo 2) уходит в `POST /magnum/api/games/submit {game:"flappy", score, difficulty, skin}` на `:211` (есть), но чтение `best` идёт только из localStorage — релогин/инкогнито/другой девайс = сброс рекорда. Аналогично `RhythmGame.tsx:301` `rhythm42-best/tut`. Заменить на `GET /magnum/api/games/scores?game=flappy42` как в `Game2042.tsx:411` (DAILY submit), оставить localStorage только как UI-кэш (tip rotation) если нужно.

### 3. `src/pages/GalleryPage.tsx:181-183` + `server.ts:f16d51a duelInvites` — archive 210× HTML-fallback + duel invites без UNIQUE
- **GalleryPage.tsx:181** — `ARCHIVE_42` 210 записей генерируются циклом, `src: "/magnum/images/gallery-42/archive-СССР-001.jpg"` и т.д., но файлов нет — `curl 200 9373 text/html` (SPA fallback `try_files` → `index.html` 9373), `content-type text/html` вместо `image/*`. В браузере `<img>` попытается отрисовать HTML как JPEG → broken image + `Failed to load`. Нужен `img onError={e=>e.currentTarget.src=getRealSrc(style)}` (функция уже есть `:export function getRealSrc`) или реальный `dist/images/gallery-42/archive-*.jpg` через `sharp`/`cwebp` по 4 стилям modulo-маппингом. Сейчас watchdog маскирует: `curl 200` = PASS, но `content-type` не проверяется.
- **server.ts: handleDuelInviteCreate (f16d51a: +~40 строк)** — `INSERT INTO magnum_duel_invites (from_user_id,to_user_id,room_id)` без `UNIQUE(from_user_id,to_user_id,room_id)` и без `BEGIN` — спам 6/мин per user но можно зафлудить 6 запросов в 1с с разными `roomId` (новый рандом `room-${Date.now()}` если не передан). Также `magnumDuelSeasons` 0014: `ends_at` nullable без CHECK `ends_at > starts_at` — можно создать сезон с прошедшим `endsAt` (если фронт передаст). Добавить `CHECK (ends_at IS NULL OR ends_at > starts_at)` + `UNIQUE` на инвайты как в `magnum_idea_votes`.

---

## Похвала (1 конкретная)

**`server.ts: f16d51a duel 2.0 (178 строк) + `drizzle/migrations/0014` + `e8fa558` P0-фикс — образцовая скорость восстановления:** `duel 2.0` добавил 7 роутов (`/duel/rooms`, `/stats`, `/leaderboard`, `/invites`, `/invite`, `/seasons` GET/POST) + `wsReady` Map + `ready` broadcast + `startDuel` allReady gate + `magnum_duel_invites/seasons` с 6 индексами — всё с `extractToken` + `checkRateLimit 6/мин` + `ensureNotification` (дуэльный вызов) + `roomPublic` ready field. Одновременно `e8fa558` закрыл **P0 crash-loop** `server.ts:1433` (`url.includes(""")` Unterminated string → `Bun.serve` не стартовал, 000 Connection refused, restart counter 25, EADDRINUSE) — фикс `'"'` → `'"'` + `systemctl start` восстановил `200/200/200` за 3с до этого ревью. Оба коммита покрыты тестами `tests/new-coverage-1527.test.ts` +131 (16 игр хаб, gallery GSAP, health/coins). Держит планку как `handleMiningExchange` из прошлого ревью.

---

## Идеи для следующих правок (2)

1. **Галерея P1 — закрыть Y2K+archive за один PR (~25 строк):** а) поправить `REAL_FALLBACK` (п.1) + `galleryTokens.ts` + добавить `onError` fallback на `REAL_BY_STYLE[style]` для 210 archive; б) сгенерить 4×800.webp archive-заглушки через `bun run scripts/gen-gallery.ts` (modulo маппинг) или заменить archive src на существующие 8 файлов циклично — после обновить watchdog: `content-type image/*` + `size > 20000` вместо только `http_code 200` (сейчас 9373 HTML маскируется как PASS).

2. **Duel 2.0 hardening + Flappy Neon-миграция (0.3.9):** а) `magnum_duel_invites` добавить `UNIQUE(from_user_id,to_user_id,room_id)` + `CHECK ends_at` для seasons + `BEGIN` для invite create (TOCTOU как в `handlePromoRedeem 1278-1297` carry); б) вынести `flappy42-diff/skin/best` и `rhythm42-best/tut` в `magnum_game_scores` (reuse `POST /games/submit` + `GET /games/scores`) и убрать прямой `localStorage` для прогресса — снимет последнее нарушение ТЗ за один PR (~50 строк) и разблокирует кросс-девайс.

---

*Ревьюер: Hermes 24/7 · `bunx tsc --noEmit` 0 · `bun test` 3086/3086 · `curl gallery 4/4 200` (67K-133K webp) + 2/2 archive 200 HTML WARN · `health 200/200/200` · `magnum-bun active` `caddy Up` · next review +10м*
