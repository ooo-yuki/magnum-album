# Watchdog 2026-09-01 13:51 UTC — ✅ OK (all 200, no restart)

**Ветка:** `master` · **HEAD:** `8c241b3` · **Проверок:** 8/8 · **Рестартов:** 0

## Health

| # | Проверка | Ожидается | Факт | Код/Статус |
|---|----------|-----------|------|------------|
| 1 | `curl --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | ✅ 200 | `200` `text/html` `<!DOCTYPE html>…MAGNUM` |
| 2 | `curl --resolve … https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | ✅ 200 | `200` `{"ideas":[{"id":65,…65}]}` ideas>0 |
| 3 | `curl http://127.0.0.1:3000/magnum/api/ideas` | 200 | ✅ 200 | `200` JSON, совпадает с (2) |
| 4 | `systemctl is-active magnum-bun` | active | ✅ active | `active (running)` since 13:27:47 UTC, PID 104511, 81.6M, `enabled` |
| 5 | `docker ps \| grep magnum-caddy` | Up | ✅ Up 24m | `431e292c2c6a omniroute-caddy:latest Up 24 minutes 0.0.0.0:30645-30646->30645-30646/tcp` |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | ✅ 6 | `42-agit-01-800.webp 119K`, `42-agit-01.jpg 3.4M`, `42-cyber-01-800.webp 133K`, `42-cyber-01.jpg 2.9M`, `42-memphis-01-800.webp 77K`, `42-memphis-01.jpg 2.4M` |
| 7 | `git status --short` | — | ⚠️ dirty | `D reports/health-2026-09-01-1342.md`, `M research.md`, `M src/pages/GalleryPage.tsx`, `M tests/content.test.ts`, `M tests/massive42.test.ts`, `M tests/massive42_2.test.ts`, `M tests/setup.ts`, `?? bunfig.toml docs/SPEC-42.md docs/spec-audit-2026-09-01.md tests/bun-setup.ts tests/gallery-real.test.ts` |
| 8 | `tsc --noEmit` + `vitest run` | 0 errors / green | ✅ | `tsc 0 errors`, `vitest 13 passed, 2850 passed` (massive42 1745 ✅ после фикса скобки) |

**Доп:** `curl http://127.0.0.1:3000/magnum/` → 200, `ss -tlnp *:3000 bun pid 104511`, Caddy `handle /magnum/api/* → 172.18.0.1:3000`, `handle_path /magnum/* → try_files {path} /index.html + file_server`.

## Коды ответов

```
https://oooyuki.zomb.top:30645/magnum/          → 200 (TLS CF, Caddy 30645)
https://oooyuki.zomb.top:30645/magnum/api/ideas  → 200 JSON {ideas:[65,…]}
http://127.0.0.1:3000/magnum/api/ideas           → 200 JSON (Bun.serve)
http://127.0.0.1:3000/magnum/                     → 200 text/html
```

## Рестарты

| Сервис | Действие | Причина | Результат |
|--------|----------|---------|-----------|
| magnum-bun | — | не требовался (active, все 200) | — |
| magnum-caddy (docker compose) | — | не требовался (Up 24m, 30645 LISTEN) | — |

> Повторная проверка после рестарта не выполнялась — все пробы зелёные.

## Найденные баги (файл:строка)

### 1) `src/pages/GalleryPage.tsx:182–532` — 210× `archive-*.jpg` soft-404 через SPA fallback
`BASE_ARTS` (7) и `MOCK_POOL` (4) уже переведены на `REAL_BY_STYLE`/`REAL_FALLBACK` (`42-agit-01-800.webp` 119K и т.д.) — ✅ `curl 3/3 200 image/webp`. Но массив `ARCHIVE_42` (строка 180, 210 элементов `arch-СССР-001`…`arch-мемфис-210`) всё ещё хранит `src: "/magnum/images/gallery-42/archive-*.jpg"` — файлов с такими именами на диске нет. Caddy `try_files {path} /index.html` отдаёт для них `200 text/html` (7192 байт `index.html`) вместо 404 — в `<img>` битая картинка, `curl 200` маскирует ошибку. Проверка: `ls public/images/gallery-42/` — только 6 реальных файлов, `archive-*` нет; `curl …/archive-СССР-001.jpg -i` покажет `content-type: text/html`.

### 2) `src/pages/GalleryPage.tsx:182` — архив игнорирует `REAL_BY_STYLE` (неконсистентность маппинга)
Реальный стиль → реальный файл уже заведён (`REAL_BY_STYLE: СССР→42-agit-01-800.webp, Y2K→42-memphis-01-800.webp, киберпанк→42-cyber-01-800.webp, мемфис→42-memphis-01-800.webp`), но `ARCHIVE_42` не использует его — каждый элемент хардкодит уникальный `archive-{style}-{n}.jpg`. Из-за кириллицы в пути (`archive-СССР-…`) ещё и риск double-encode в браузере. Фолбэк `gradient+emoji` не срабатывает, т.к. `onError` на `<img>` не скрывает битую картинку.

## Идея починки (1)

**Галерея — dead archive → живой циклический маппинг + честный 404 для статики.** В `src/pages/GalleryPage.tsx:178–185` заменить генерацию `ARCHIVE_42`:
```ts
const ARCHIVE_42: Art42[] = Array.from({length:210}, (_,i)=>{
  const styles: Style42[] = ["СССР","Y2K","киберпанк","мемфис"];
  const s = styles[i%4];
  return { id:`arch-${s}-${String(i+1).padStart(3,"0")}`, style:s, src: REAL_BY_STYLE[s], … }
});
```
Добавить в рендере `src/pages/GalleryPage.tsx` (у ` <img>` ~строка 600): `loading="lazy"` + `onError={e=>{e.currentTarget.style.display="none"; e.currentTarget.nextElementSibling?.classList.remove("hidden")}}` и показывать `gradient+emoji` как заглушку. В ` /opt/magnum-caddy/Caddyfile` и/или `server.ts` вынести статику в отдельный `handle /magnum/images/* { root * /srv/magnum; file_server }` **без** `try_files /index.html`, чтобы несуществующие файлы отдавали честный `404`, а не `200 html` — тогда `curl` и тесты `gallery-real` ловят проблему. Альтернатива — сгенерить 12 превью `400.webp` и маппить архив на них по `i%12`.

## Доп. наблюдения

- `vitest run` теперь **зелёный 2850/2850** (13 files) — `tests/massive42.test.ts:703` починен, `tests/massive42_2.test.ts` тоже. `tsc 0 errors`.
- `git status` dirty — 5 модифицированных + 5 untracked (`bunfig.toml`, `docs/SPEC-42.md`, `docs/spec-audit-2026-09-01.md`, `tests/bun-setup.ts`, `tests/gallery-real.test.ts`) + удалённый `reports/health-2026-09-01-1342.md` (был в коммите `28583d9`, сейчас `D`). Рекомендация — закоммитить текущие Gallery/test фиксы отдельным коммитом.
- Caddy `Up 24m` стабилен, Bun `81.6M` — норма.

---
*watchdog cron 1/10 · 2026-09-01 13:51 UTC · src 2570 строк GalleryPage, 210 archive soft-404 · рестартов 0*
