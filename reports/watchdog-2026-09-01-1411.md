# Watchdog 2026-09-01 14:11 UTC — ✅ OK (all 200, no restart)

**Ветка:** `master` · **HEAD:** `edb467c` · **Проверок:** 8/8 · **Рестартов:** 0 · **Cron:** `1/10` каждые 5 мин

## Health

| # | Проверка | Ожидается | Факт | Код/Статус |
|---|----------|-----------|------|------------|
| 1 | `curl --resolve oooyuki.zomb.top:30645:127.0.0.1 https://oooyuki.zomb.top:30645/magnum/` | 200 | ✅ 200 | `200` `text/html` `MAGNUM — 5opka` 7192B |
| 2 | `curl --resolve … https://oooyuki.zomb.top:30645/magnum/api/ideas` | 200 JSON | ✅ 200 | `200` `application/json` `ideas:[{id:65,votes:142,approved},…]` |
| 3 | `curl http://127.0.0.1:3000/magnum/api/ideas` | 200 | ✅ 200 | `200` JSON, идентичен (2) — Bun.serve :3000 |
| 4 | `systemctl is-active magnum-bun` | active | ✅ active | `active (running)` since 14:08:57 UTC, PID 146737, 30.4M |
| 5 | `docker ps \| grep magnum-caddy` | Up | ✅ Up 44m | `431e292c2c6a omniroute-caddy:latest Up 44 minutes` :30645/:30646 |
| 6 | `ls -lh public/images/gallery-42/` | 6 файлов | ✅ 6 | `42-agit-01-800.webp 119K`, `42-agit-01.jpg 3.4M`, `42-cyber-01-800.webp 133K`, `42-cyber-01.jpg 2.9M`, `42-memphis-01-800.webp 77K`, `42-memphis-01.jpg 2.4M` |
| 7 | `git status --short` | — | ⚠️ 2 untracked | `?? reports/auth-2026-09-01-1411.md`, `?? tests/new-coverage-1410.test.ts` |
| 8 | extra `curl http://127.0.0.1:3000/magnum/` | 200 | ✅ 200 | local file_server OK |

**Доп:** `Caddy handle /magnum/api/* → 172.18.0.1:3000`, `handle_path /magnum/* → try_files {path} /index.html + file_server` (30645 TLS CF DNS, 30646 http). `ss *:3000 LISTEN bun`.

## Коды ответов

```
https://oooyuki.zomb.top:30645/magnum/             → 200 text/html (TLS CF, resolve 127.0.0.1)
https://oooyuki.zomb.top:30645/magnum/api/ideas    → 200 application/json {ideas:[{id:65,votes:142},...]}
http://127.0.0.1:3000/magnum/api/ideas              → 200 application/json (Bun.serve, совпадает с TLS)
http://127.0.0.1:3000/magnum/                        → 200 text/html
/magnum/images/gallery-42/42-agit-01-800.webp       → 200 image/webp (проверено в review-1408)
```

## Рестарты

| Сервис | Действие | Причина | Результат |
|--------|----------|---------|-----------|
| magnum-bun | — | не требовался (active, все 200) | — |
| magnum-caddy (docker compose) | — | не требовался (Up 44m) | — |

> Повторная проверка после рестарта не выполнялась — все пробы зелёные. Критерий рестарта: любой не-200 или не-active → `systemctl restart magnum-bun; cd /opt/magnum-caddy && docker compose up -d`.

## Найденные баги (файл:строка)

### 1) `server.ts:1031–1038` — WebSocket `/magnum/api/ws` пускает анонима без токена (auth bypass, критично)

```ts
// server.ts:1031
const username = (url.searchParams.get("username") || ...).slice(0,24) || `Братуха_${rand}`;
let authName = null; const token = extractToken(req);
if (token) { try { u = await getUserByToken(token); if(u) authName=u.username; } catch {} }
const finalName = authName ?? username; // ← fallback на рандом если нет токена
server.upgrade(req, { data: { username: finalName } })
```

Токен опционален. Подтверждено `auth-2026-09-01-1411.md`: `curl -H "Sec-WebSocket-Key: ..." http://127.0.0.1:3000/magnum/api/ws` без cookie/header отдаёт `101 Switching Protocols` + `{"type":"room","you":"Братуха_198"}` — аноним в дуэли, рейтинге, без регистрации. `handleIdeasPost/handleCoinsAdd` требуют `401`, WS — нет. Связка `?username=` из query позволяет спуфить имя.

### 2) `server.ts:294–303` — `handleIdeasVote` без авторизации, накрутка голосов

```ts
async function handleIdeasVote(req, idStr) {
  const id = Number(idStr);
  if (!checkRateLimit(`ideas:vote:${ip}:${id}`, 12, 60_000)) return 429;
  const rows = await sql`UPDATE magnum_ideas SET votes = votes+1 WHERE id=${id} RETURNING *`;
}
```

`handleIdeasPost` (строка 267) требует `extractToken → getUserByToken → 401`, а `handleIdeasVote` — только IP rate-limit 12/мин. Любой аноним в цикле `POST /magnum/api/ideas/:id/vote` накрутит `votes` до ∞, обходя экономику `coins→votes`. Нет таблицы `magnum_idea_votes(user_id,idea_id)`, нет `UNIQUE(user_id,idea_id)`, нет списания монет. Рядом `SHOP_PRICES`/`coins` требуют auth — идеи нет.

## Идея починки (1)

**Закрыть auth-дыру WS + голоса одним патчем (30 строк, без миграции ломающей prod):**

1. WS: в `server.ts:1031` заменить опциональный токен на обязательный — `const token=extractToken(req); if(!token) return Response.json({error:"unauthorized"}, {status:401}); const user=await getUserByToken(token); if(!user) return 401; const finalName=user.username;` Убрать `url.searchParams.get("username")` и `Братуха_XXX` fallback. Если нужен гостевой режим — отдельный `WS /magnum/api/ws/guest` с `rateLimit 5/min` и `verified=false`.
2. Голоса: в `server.ts:294` добавить `token+user` guard как в `handleIdeasPost`, сменить `checkRateLimit` ключ на `ideas:vote:${user.id}:${id}` (1 голос/юзер/идея/24ч) + `INSERT INTO magnum_idea_votes(user_id,idea_id) ON CONFLICT DO NOTHING` → `UPDATE ... WHERE NOT EXISTS (SELECT 1 FROM magnum_idea_votes WHERE ...)`. Списывать 1 coin за голос (связь с экономикой) или `votes` только через `coins`.
3. Миграция: `CREATE TABLE IF NOT EXISTS magnum_idea_votes(user_id int REFERENCES magnum_users(id), idea_id int REFERENCES magnum_ideas(id), created_at timestamptz default now(), PRIMARY KEY(user_id,idea_id)); CREATE INDEX ON magnum_ideas(votes DESC);`

> Галерея `ARCHIVE_42` (280 `archive-*.jpg`) уже починена runtime-патчем `src/pages/GalleryPage.tsx:785–786` (`for (a of ARCHIVE_42) a.src = REAL_BY_STYLE[a.style]`) — `curl archive-*.jpg` теперь не бьёт в soft-404, все 280 src → 3 реальные webp. Caddy `try_files` без `file_server` раздельного handle пока остаётся, но не критично после JS-фикса.

## Доп. наблюдения

- `vitest` ранее 2868 passed (15 файлов, review-1408) · `tsc 0` · `bun build` 27M · `dist` жив.
- `magnum-bun` PID 146737 с 14:08:57 (рестарт после 14:06), `magnum-caddy` Up 44m — стабильны.
- `git log --oneline -3`: `edb467c review: 14:08 — rating 2/10`, `3e8b412 review: 14:09 — rating 6/10`, `4e18115 feat(games): 2042 MAGNUM upgrade`.
- Новый `auth-2026-09-01-1411.md` подтверждает WS 101-аноним — следующий watchdog должен проверить `curl -H "Sec-WebSocket-Key: ..." ` → 401 после фикса.
- Требуется `hype`/`health`/`review` не блокируют watchdog — рестартов 0, все 200.

---
*watchdog cron 1/10 · 2026-09-01 14:11 UTC · WS anon 101 + vote anon bypass · рестартов 0 · gallery 6/6 200 · все API 200*
