# BACKLOG 42 — 2026-09-01 16:23 UTC · 10м тик

> Собрано кроном backlog-keeper · P0 auth/404/деньги выше хайпа · 10 топ-задач · `git log --since="10 minutes ago"` + `reports/*` + `docs/*`

| # | приоритет | задача | откуда | файл:строка | монетизация / пресейв |
|---|-----------|--------|--------|-------------|------------------------|
| 1 | **P0** | VIP не сияет — покупка `title-vip` (1240) не даёт сияющую обводку: `AuthStatus` фетчит несуществующий `GET /shop/subscriptions` вместо `GET /frame/status` → `tier` всегда null, `glowVip/glowVipPlus/glowPro` никогда не показывается | director/auth | `src/components/AuthStatus.tsx:83` stale `fetch("/magnum/api/shop/subscriptions")` / `src/components/AuthStatus.tsx:104-144` tier glow / `server.ts:2576-2593` нет handler | **прямые деньги** — VIP 1240 legendary, обводки = оплата → ощущение обмана, отток; без fix конверсия VIP = 0 · `reports/director-2026-09-01-1620.md:Сц.4` + `reports/auth-2026-09-01-1620.md:Баг` |
| 2 | **P0** | Мобильный drawer «Пресейв →» ведёт на `music.yandex.ru/artist/7544304` вместо BandLink `music.thefence.me/psmagnum` — главный CTA на телефоне сливает конверсию | director | `src/components/Layout.tsx:185` `drawerCta href` yandex / эталон `src/components/CTA.tsx:7` `PRESAVE_URL` + `src/components/Hero.tsx:8` | **пресейв** — единственный CTA в шторке,追踪 `presaveTracker.ts:8` не триггерит yandex → статистика занижается, `GET /magnum/api/bandlink hasPresave:true` не конвертит · `reports/director-2026-09-01-1620.md:Сц.5` |
| 3 | **P0** | `POST /magnum/api/ai` регрессия: `8afd888` anon 8/мин закрыт перефиксом `server.ts:2231` — теперь все anon → `401` (требует токен), хотя по спеку anon текст 8/мин должен проходить, только `image` требует auth; ledger `magnum_ai_usage` никогда не пишет `NULL` (аноним) | review/watchdog/auth | `server.ts:2231-2241` `handleAi` `extractToken→401` / `server.ts:2273` `INSERT ... VALUES (${aiUser?.id ?? null})` / `drizzle/migrations/0017_ai_guard.sql:2` `user_id SET NULL` | **биллинг MIMO** `mimo-v2-flash max 400` — гости блочатся перед промо, DDoS аудит теряется (`aiUsage:0` в health), image без auth должен быть 401 · `reports/review-2026-09-01-1613.md:Замечание #1` + `reports/watchdog-2026-09-01-1611.md:Баг #1` |
| 4 | **P1** | `magnum:need-auth` не открывает логин — `ShopPage/MiningPage` диспатчат `magnum:need-auth` при 401, а `AuthStatus` слушает только `magnum:auth` (успешный вход) → тост «Войди, братуха» без модалки, лишний клик | director | `src/components/AuthStatus.tsx:120-130` нет listener / `src/pages/ShopPage.tsx:434,453,480` + `src/pages/MiningPage.tsx:413,491` dispatch | **конверсия авторизации** — магазин/майнинг/дуэль закрыты без логина (`AuthStatus` гейтит), трение на входе бьёт покупки bundle/PRISM · `reports/director-2026-09-01-1620.md:Сц.1` |
| 5 | **P1** | Пустые `catch {}` глотают `getUserByToken` ошибки молча (8 мест) — Neon таймаут маскируется как anon, нет лога/метрики, `journalctl` чист | watchdog | `server.ts:128` + `server.ts:302,865,908,963` `} catch {}` / `src/pages/ShopPage.tsx:270` | нет прямых денег, но бьёт auth-наблюдаемость → VIP/монеты внезапно anon, `POST /ideas/:id/vote` после anon-block начнёт отдавать 401 всем · `reports/watchdog-2026-09-01-1618.md:Баг #1` |
| 6 | **P1** | `AiBot.tsx:80` `as any` тройной парсинг inventory — при `401 {error}` или `200 {inventory:null}` → `arr=null → .map` краш `TypeError`, весь чат MAGNUM падает | watchdog | `src/components/AiBot.tsx:80` `as any` + `src/pages/ShopPage.tsx:384` `batch:any` / `tsconfig strict` | **косвенно деньги** — `AiBot` ведёт на пресейв/BandLink + показывает баланс `magnum_coins`, краш блокирует CTA · `reports/watchdog-2026-09-01-1618.md:Баг #2` |
| 7 | **P1** | Галерея `REAL_FALLBACK` дубль `y2k-03` копирует `y2k-01` (2 из 3 Y2K карточек один webp 67K) + `src/lib/galleryTokens.ts:32` дублирует `GalleryPage.tsx:30` `REAL_BY_STYLE` — дрейф при переименовании, бедный Y2K фильтр | review | `src/pages/GalleryPage.tsx:43` `y2k-03 → 42-y2k-01-800.webp` дубль / `src/lib/galleryTokens.ts:32` дубликат | доверие пресейва — фильтр Y2K показывает повторы, `BASE_ARTS` 7 карточек выглядят бедно, LCP без виртуализации 210 DOM нод · `reports/review-2026-09-01-1613.md:Замечание #2` |
| 8 | **P0** | DUEL NITRO 42 — WS 2-4 + nitro x9 + overheat + ghost trail | hype-queue | `docs/hype-queue.md:39` `queued 16:16` / `Bun.serve /magnum/api/ws` | **wager 0/42/142/420 win +wager*2 +42 ELO топ-3 +1420 + crown** · `magnum_leaderboard(game=duel42)` сезон 7дн, overheat 3с→1с кулдаун -50%, CPS>20 suspect throttle 30/сек heartbeat 25с, GSAP y24 stagger 0.12 shake x±6 ghost red pulse confetti 160 |
| 9 | **P0** | OBSIDIAN FORGE 42 — 12 скинов obsidian + molten epic 1420 + dust крафт/разбор | hype-queue | `docs/hype-queue.md:41` `queued 16:16` / `magnum_cosmetics` + `magnum_shop_inventory` + `magnum-forge-dust` | **common 42 / uncommon 142 / rare 420 / epic 1420**, крафт 42 разбор +100 dust, `COSMETICS_CATALOG` 12 obsidian `gold-obsidian-spin epic 1420 spin 3s`, GSAP stagger y20 0.08 molten shimmer · `docs/SPEC-42.md:§9` |
| 10 | **P0** | FRAME NITRO GOLD — mimo-v2.5 vision + conic-molten + nitro glow + cross -42 obsidian/duel | hype-queue | `docs/hype-queue.md:42` `queued 16:16` / `POST /magnum/api/ai` → `magnum_frames` + LS `magnum-frame-verified=1` | **бесплатно verified**, cross **-42** в obsidian/duel · `conic-molten spin 3s + shadow 0 0 16 molten`, GSAP scale spring glow pulse 2s · `docs/SPEC-42.md:§11` + `docs/spec-audit-2026-09-01.md:P0-5` |

> Источники: `reports/review-2026-09-01-1613.md:Замечания #1-3`, `reports/director-2026-09-01-1620.md:Сц.1/4/5`, `reports/auth-2026-09-01-1620.md:Баг`, `reports/watchdog-2026-09-01-1618.md:Баг #1-2`, `reports/watchdog-2026-09-01-1611.md:Баг #1-2` (учтён как база), `docs/spec-audit-2026-09-01.md:P0-5/P1-3`, `docs/hype-queue.md:39/41/42`, `docs/SPEC-42.md:§3/§6/§9/§11` · `reports/watchdog-2026-09-01-1604.md` галерея soft-404 — **закрыт** `1a835a2` (`server.ts:2765` images 404-guard + `GET /magnum/images/gallery-42/notfound.jpg → 404` live) · `reports/review-2026-09-01-1559.md:Замечание #3` DDL/bundle — **закрыт** `1a835a2` (`chat hot-path + anon vote block + validator sync`) · `tsc 0` `tests 3141 PASS` `gallery 8 файлов` `health 200/200/200` `magnum-bun active`

### Примечания сортировки

- P0 деньги/пресейв/auth (№1-3) выше любого хайпа, даже P0 NITRO — VIP и BandLink бьют выручку сильнее фич; `P0 auth` №3 выше P1 `need-auth` №4 сознательно (биллинг > UX).
- Галерея P0 → P1: `server.ts:2765` теперь отдаёт честный `404` для `archive-*.jpg` (проверено `director-2026-09-01-1620: notfound.jpg → 404`), `REAL_BY_STYLE` 4 стиля честно `200×4` (agit 119K, cyber 133K, memphis 77K, y2k 67K) — остался только дубль `y2k-03` (№7, P1).
- P0 №1 VIP: `server.ts` нет `GET /shop/subscriptions` (grep 0), фронт `AuthStatus.tsx:83` фетчит SPA `200 text/html` → `r.json()` падает → `catch(()=>{})` → `tier=null` → `glowVip` никогда — покупка `title-vip` даёт только бейдж, не `tier` (director Сц.4). Фикс: `AuthStatus.tsx:83 → fetch("/magnum/api/frame/status")` + парс `{tier}` или добавить `GET /shop/subscriptions → 410 Gone`.
- После закрытия P0 №1-3 — брать P1 баги №4-7 (need-auth, catch, any, gallery) по 15 строк каждый, затем P0 хайп №8-10 ротация NITRO→OBSIDIAN→FRAME (каждый 80-200 строк, меню + баннер + рейтинг).

### Сверка с SPEC-42 и spec-audit

- `docs/spec-audit-2026-09-01.md:P0-1/P0-2` (`shop/state`, `coins/set` 404) — **закрыты** в `server.ts:2442` `handleShopState` + `server.ts:2341` `handleCoinsSet` (есть алиасы), не в топ-10.
- `spec-audit:P0-4` GSAP a11y — **закрыт** (`src/pages/games/*.tsx` теперь `prefers-reduced-motion` gate + `gsap.context`), не в топ-10.
- `spec-audit:P0-3` CI 404 — **закрыт** `server.ts:2765` images 404-guard + `tests/gallery-404.test.ts` (есть в `new-coverage-1607`), покрывается задачей №7.
- `spec-audit:P0-5` Brat-Bot → Frame — покрывается задачей №10 (mimo-v2.5 → `magnum_frames` + LS `magnum-frame-verified=1`).
- `spec-audit:P1-3` двойная экономика — **закрыт** коммитом `70cb919 PRISM` (`magnum_dust` + единый каталог), не в топ-10.
